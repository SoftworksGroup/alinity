SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pRegistrantIdentifier#Update]
	 @RegistrantIdentifierSID        int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@RegistrantSID                  int               = null -- table column values to update:
	,@IdentifierValue                varchar(50)       = null
	,@IdentifierTypeSID              int               = null
	,@EffectiveDate                  date              = null
	,@ExpiryDate                     date              = null
	,@UserDefinedColumns             xml               = null
	,@RegistrantIdentifierXID        varchar(150)      = null
	,@LegacyKey                      nvarchar(50)      = null
	,@UpdateUser                     nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                       timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected                   tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied                  bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                       xml               = null -- other values defining context for the update (if any)
	,@OrgSID                         int               = null -- not a base table column
	,@IdentifierTypeLabel            nvarchar(35)      = null -- not a base table column
	,@IdentifierTypeCategory         nvarchar(65)      = null -- not a base table column
	,@IsOtherRegistration            bit               = null -- not a base table column
	,@DisplayRank                    tinyint           = null -- not a base table column
	,@EditMask                       varchar(50)       = null -- not a base table column
	,@IdentifierCode                 varchar(15)       = null -- not a base table column
	,@IdentifierTypeIsDefault        bit               = null -- not a base table column
	,@IdentifierTypeRowGUID          uniqueidentifier  = null -- not a base table column
	,@PersonSID                      int               = null -- not a base table column
	,@RegistrantNo                   varchar(50)       = null -- not a base table column
	,@YearOfInitialEmployment        smallint          = null -- not a base table column
	,@IsOnPublicRegistry             bit               = null -- not a base table column
	,@CityNameOfBirth                nvarchar(30)      = null -- not a base table column
	,@CountrySID                     int               = null -- not a base table column
	,@DirectedAuditYearCompetence    smallint          = null -- not a base table column
	,@DirectedAuditYearPracticeHours smallint          = null -- not a base table column
	,@LateFeeExclusionYear           smallint          = null -- not a base table column
	,@IsRenewalAutoApprovalBlocked   bit               = null -- not a base table column
	,@RenewalExtensionExpiryTime     datetime          = null -- not a base table column
	,@ArchivedTime                   datetimeoffset(7) = null -- not a base table column
	,@RegistrantRowGUID              uniqueidentifier  = null -- not a base table column
	,@IsDeleteEnabled                bit               = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : dbo.pRegistrantIdentifier#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the dbo.RegistrantIdentifier table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the dbo.RegistrantIdentifier table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vRegistrantIdentifier entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pRegistrantIdentifier procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fRegistrantIdentifierCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @RegistrantIdentifierSID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@RegistrantIdentifierSID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @IdentifierValue = ltrim(rtrim(@IdentifierValue))
		set @RegistrantIdentifierXID = ltrim(rtrim(@RegistrantIdentifierXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @IdentifierTypeLabel = ltrim(rtrim(@IdentifierTypeLabel))
		set @IdentifierTypeCategory = ltrim(rtrim(@IdentifierTypeCategory))
		set @EditMask = ltrim(rtrim(@EditMask))
		set @IdentifierCode = ltrim(rtrim(@IdentifierCode))
		set @RegistrantNo = ltrim(rtrim(@RegistrantNo))
		set @CityNameOfBirth = ltrim(rtrim(@CityNameOfBirth))

		-- set zero length strings to null to avoid storing them in the record

		if len(@IdentifierValue) = 0 set @IdentifierValue = null
		if len(@RegistrantIdentifierXID) = 0 set @RegistrantIdentifierXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@IdentifierTypeLabel) = 0 set @IdentifierTypeLabel = null
		if len(@IdentifierTypeCategory) = 0 set @IdentifierTypeCategory = null
		if len(@EditMask) = 0 set @EditMask = null
		if len(@IdentifierCode) = 0 set @IdentifierCode = null
		if len(@RegistrantNo) = 0 set @RegistrantNo = null
		if len(@CityNameOfBirth) = 0 set @CityNameOfBirth = null

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @RegistrantSID                  = isnull(@RegistrantSID,ri.RegistrantSID)
				,@IdentifierValue                = isnull(@IdentifierValue,ri.IdentifierValue)
				,@IdentifierTypeSID              = isnull(@IdentifierTypeSID,ri.IdentifierTypeSID)
				,@EffectiveDate                  = isnull(@EffectiveDate,ri.EffectiveDate)
				,@ExpiryDate                     = isnull(@ExpiryDate,ri.ExpiryDate)
				,@UserDefinedColumns             = isnull(@UserDefinedColumns,ri.UserDefinedColumns)
				,@RegistrantIdentifierXID        = isnull(@RegistrantIdentifierXID,ri.RegistrantIdentifierXID)
				,@LegacyKey                      = isnull(@LegacyKey,ri.LegacyKey)
				,@UpdateUser                     = isnull(@UpdateUser,ri.UpdateUser)
				,@IsReselected                   = isnull(@IsReselected,ri.IsReselected)
				,@IsNullApplied                  = isnull(@IsNullApplied,ri.IsNullApplied)
				,@zContext                       = isnull(@zContext,ri.zContext)
				,@OrgSID                         = isnull(@OrgSID,ri.OrgSID)
				,@IdentifierTypeLabel            = isnull(@IdentifierTypeLabel,ri.IdentifierTypeLabel)
				,@IdentifierTypeCategory         = isnull(@IdentifierTypeCategory,ri.IdentifierTypeCategory)
				,@IsOtherRegistration            = isnull(@IsOtherRegistration,ri.IsOtherRegistration)
				,@DisplayRank                    = isnull(@DisplayRank,ri.DisplayRank)
				,@EditMask                       = isnull(@EditMask,ri.EditMask)
				,@IdentifierCode                 = isnull(@IdentifierCode,ri.IdentifierCode)
				,@IdentifierTypeIsDefault        = isnull(@IdentifierTypeIsDefault,ri.IdentifierTypeIsDefault)
				,@IdentifierTypeRowGUID          = isnull(@IdentifierTypeRowGUID,ri.IdentifierTypeRowGUID)
				,@PersonSID                      = isnull(@PersonSID,ri.PersonSID)
				,@RegistrantNo                   = isnull(@RegistrantNo,ri.RegistrantNo)
				,@YearOfInitialEmployment        = isnull(@YearOfInitialEmployment,ri.YearOfInitialEmployment)
				,@IsOnPublicRegistry             = isnull(@IsOnPublicRegistry,ri.IsOnPublicRegistry)
				,@CityNameOfBirth                = isnull(@CityNameOfBirth,ri.CityNameOfBirth)
				,@CountrySID                     = isnull(@CountrySID,ri.CountrySID)
				,@DirectedAuditYearCompetence    = isnull(@DirectedAuditYearCompetence,ri.DirectedAuditYearCompetence)
				,@DirectedAuditYearPracticeHours = isnull(@DirectedAuditYearPracticeHours,ri.DirectedAuditYearPracticeHours)
				,@LateFeeExclusionYear           = isnull(@LateFeeExclusionYear,ri.LateFeeExclusionYear)
				,@IsRenewalAutoApprovalBlocked   = isnull(@IsRenewalAutoApprovalBlocked,ri.IsRenewalAutoApprovalBlocked)
				,@RenewalExtensionExpiryTime     = isnull(@RenewalExtensionExpiryTime,ri.RenewalExtensionExpiryTime)
				,@ArchivedTime                   = isnull(@ArchivedTime,ri.ArchivedTime)
				,@RegistrantRowGUID              = isnull(@RegistrantRowGUID,ri.RegistrantRowGUID)
				,@IsDeleteEnabled                = isnull(@IsDeleteEnabled,ri.IsDeleteEnabled)
			from
				dbo.vRegistrantIdentifier ri
			where
				ri.RegistrantIdentifierSID = @RegistrantIdentifierSID

		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		-- Tim Edlund | Mar 2019
		-- If the identifier is a registration and an expiry
		-- is entered without an effective date, assume a
		-- 1 year term.

		if @IsOtherRegistration = @ON and @EffectiveDate is null and @ExpiryDate is not null
		begin
			set @EffectiveDate = dateadd(day, 1, dateadd(year, -1, @ExpiryDate));
		end;
		--! </PreUpdate>
	
		-- call the extended version of the procedure (if it exists) for "update.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pRegistrantIdentifier'
		)
		begin
		
			exec @errorNo = ext.pRegistrantIdentifier
				 @Mode                           = 'update.pre'
				,@RegistrantIdentifierSID        = @RegistrantIdentifierSID
				,@RegistrantSID                  = @RegistrantSID output
				,@IdentifierValue                = @IdentifierValue output
				,@IdentifierTypeSID              = @IdentifierTypeSID output
				,@EffectiveDate                  = @EffectiveDate output
				,@ExpiryDate                     = @ExpiryDate output
				,@UserDefinedColumns             = @UserDefinedColumns output
				,@RegistrantIdentifierXID        = @RegistrantIdentifierXID output
				,@LegacyKey                      = @LegacyKey output
				,@UpdateUser                     = @UpdateUser
				,@RowStamp                       = @RowStamp
				,@IsReselected                   = @IsReselected
				,@IsNullApplied                  = @IsNullApplied
				,@zContext                       = @zContext
				,@OrgSID                         = @OrgSID
				,@IdentifierTypeLabel            = @IdentifierTypeLabel
				,@IdentifierTypeCategory         = @IdentifierTypeCategory
				,@IsOtherRegistration            = @IsOtherRegistration
				,@DisplayRank                    = @DisplayRank
				,@EditMask                       = @EditMask
				,@IdentifierCode                 = @IdentifierCode
				,@IdentifierTypeIsDefault        = @IdentifierTypeIsDefault
				,@IdentifierTypeRowGUID          = @IdentifierTypeRowGUID
				,@PersonSID                      = @PersonSID
				,@RegistrantNo                   = @RegistrantNo
				,@YearOfInitialEmployment        = @YearOfInitialEmployment
				,@IsOnPublicRegistry             = @IsOnPublicRegistry
				,@CityNameOfBirth                = @CityNameOfBirth
				,@CountrySID                     = @CountrySID
				,@DirectedAuditYearCompetence    = @DirectedAuditYearCompetence
				,@DirectedAuditYearPracticeHours = @DirectedAuditYearPracticeHours
				,@LateFeeExclusionYear           = @LateFeeExclusionYear
				,@IsRenewalAutoApprovalBlocked   = @IsRenewalAutoApprovalBlocked
				,@RenewalExtensionExpiryTime     = @RenewalExtensionExpiryTime
				,@ArchivedTime                   = @ArchivedTime
				,@RegistrantRowGUID              = @RegistrantRowGUID
				,@IsDeleteEnabled                = @IsDeleteEnabled
		
		end

		-- update the record

		update
			dbo.RegistrantIdentifier
		set
			 RegistrantSID = @RegistrantSID
			,IdentifierValue = @IdentifierValue
			,IdentifierTypeSID = @IdentifierTypeSID
			,EffectiveDate = @EffectiveDate
			,ExpiryDate = @ExpiryDate
			,UserDefinedColumns = @UserDefinedColumns
			,RegistrantIdentifierXID = @RegistrantIdentifierXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			RegistrantIdentifierSID = @RegistrantIdentifierSID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from dbo.RegistrantIdentifier where RegistrantIdentifierSID = @registrantIdentifierSID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'dbo.RegistrantIdentifier'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'dbo.RegistrantIdentifier'
					,@Arg2        = @registrantIdentifierSID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'dbo.RegistrantIdentifier'
				,@Arg3        = @rowsAffected
				,@Arg4        = @registrantIdentifierSID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>
	
		-- call the extended version of the procedure for update.post - if it exists
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pRegistrantIdentifier'
		)
		begin
		
			exec @errorNo = ext.pRegistrantIdentifier
				 @Mode                           = 'update.post'
				,@RegistrantIdentifierSID        = @RegistrantIdentifierSID
				,@RegistrantSID                  = @RegistrantSID
				,@IdentifierValue                = @IdentifierValue
				,@IdentifierTypeSID              = @IdentifierTypeSID
				,@EffectiveDate                  = @EffectiveDate
				,@ExpiryDate                     = @ExpiryDate
				,@UserDefinedColumns             = @UserDefinedColumns
				,@RegistrantIdentifierXID        = @RegistrantIdentifierXID
				,@LegacyKey                      = @LegacyKey
				,@UpdateUser                     = @UpdateUser
				,@RowStamp                       = @RowStamp
				,@IsReselected                   = @IsReselected
				,@IsNullApplied                  = @IsNullApplied
				,@zContext                       = @zContext
				,@OrgSID                         = @OrgSID
				,@IdentifierTypeLabel            = @IdentifierTypeLabel
				,@IdentifierTypeCategory         = @IdentifierTypeCategory
				,@IsOtherRegistration            = @IsOtherRegistration
				,@DisplayRank                    = @DisplayRank
				,@EditMask                       = @EditMask
				,@IdentifierCode                 = @IdentifierCode
				,@IdentifierTypeIsDefault        = @IdentifierTypeIsDefault
				,@IdentifierTypeRowGUID          = @IdentifierTypeRowGUID
				,@PersonSID                      = @PersonSID
				,@RegistrantNo                   = @RegistrantNo
				,@YearOfInitialEmployment        = @YearOfInitialEmployment
				,@IsOnPublicRegistry             = @IsOnPublicRegistry
				,@CityNameOfBirth                = @CityNameOfBirth
				,@CountrySID                     = @CountrySID
				,@DirectedAuditYearCompetence    = @DirectedAuditYearCompetence
				,@DirectedAuditYearPracticeHours = @DirectedAuditYearPracticeHours
				,@LateFeeExclusionYear           = @LateFeeExclusionYear
				,@IsRenewalAutoApprovalBlocked   = @IsRenewalAutoApprovalBlocked
				,@RenewalExtensionExpiryTime     = @RenewalExtensionExpiryTime
				,@ArchivedTime                   = @ArchivedTime
				,@RegistrantRowGUID              = @RegistrantRowGUID
				,@IsDeleteEnabled                = @IsDeleteEnabled
		
		end

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.RegistrantIdentifierSID
			from
				dbo.vRegistrantIdentifier ent
			where
				ent.RegistrantIdentifierSID = @RegistrantIdentifierSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.RegistrantIdentifierSID
				,ent.RegistrantSID
				,ent.IdentifierValue
				,ent.IdentifierTypeSID
				,ent.EffectiveDate
				,ent.ExpiryDate
				,ent.UserDefinedColumns
				,ent.RegistrantIdentifierXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.OrgSID
				,ent.IdentifierTypeLabel
				,ent.IdentifierTypeCategory
				,ent.IsOtherRegistration
				,ent.DisplayRank
				,ent.EditMask
				,ent.IdentifierCode
				,ent.IdentifierTypeIsDefault
				,ent.IdentifierTypeRowGUID
				,ent.PersonSID
				,ent.RegistrantNo
				,ent.YearOfInitialEmployment
				,ent.IsOnPublicRegistry
				,ent.CityNameOfBirth
				,ent.CountrySID
				,ent.DirectedAuditYearCompetence
				,ent.DirectedAuditYearPracticeHours
				,ent.LateFeeExclusionYear
				,ent.IsRenewalAutoApprovalBlocked
				,ent.RenewalExtensionExpiryTime
				,ent.ArchivedTime
				,ent.RegistrantRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
			from
				dbo.vRegistrantIdentifier ent
			where
				ent.RegistrantIdentifierSID = @RegistrantIdentifierSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
