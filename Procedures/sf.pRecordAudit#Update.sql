SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [sf].[pRecordAudit#Update]
	 @RecordAuditSID                 int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@ApplicationUserSessionSID      int               = null -- table column values to update:
	,@ApplicationPageSID             int               = null
	,@AuditActionSID                 int               = null
	,@IsGlassBreak                   bit               = null
	,@ContextGUID                    uniqueidentifier  = null
	,@UserDefinedColumns             xml               = null
	,@RecordAuditXID                 varchar(150)      = null
	,@LegacyKey                      nvarchar(50)      = null
	,@UpdateUser                     nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                       timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected                   tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied                  bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                       xml               = null -- other values defining context for the update (if any)
	,@ApplicationPageLabel           nvarchar(35)      = null -- not a base table column
	,@ApplicationPageURI             varchar(150)      = null -- not a base table column
	,@ApplicationRoute               varchar(150)      = null -- not a base table column
	,@IsSearchPage                   bit               = null -- not a base table column
	,@ApplicationEntitySID           int               = null -- not a base table column
	,@ApplicationPageRowGUID         uniqueidentifier  = null -- not a base table column
	,@ApplicationUserSID             int               = null -- not a base table column
	,@ApplicationUserSessionIsActive bit               = null -- not a base table column
	,@IPAddress                      varchar(45)       = null -- not a base table column
	,@ApplicationUserSessionRowGUID  uniqueidentifier  = null -- not a base table column
	,@AuditActionSCD                 varchar(25)       = null -- not a base table column
	,@AuditActionName                nvarchar(50)      = null -- not a base table column
	,@AuditActionIsDefault           bit               = null -- not a base table column
	,@AuditActionRowGUID             uniqueidentifier  = null -- not a base table column
	,@IsDeleteEnabled                bit               = null -- not a base table column
	,@PersonSID                      int               = null -- not a base table column
	,@UserName                       nvarchar(75)      = null -- not a base table column
	,@LastReviewTime                 datetimeoffset(7) = null -- not a base table column
	,@LastReviewUser                 nvarchar(75)      = null -- not a base table column
	,@ApplicationEntitySCD           varchar(50)       = null -- not a base table column
	,@AuditTimeClientTZ              varchar(19)       = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : sf.pRecordAudit#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the sf.RecordAudit table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : March 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the sf.RecordAudit table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vRecordAudit entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pRecordAudit procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fRecordAuditCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @RecordAuditSID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@RecordAuditSID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @RecordAuditXID = ltrim(rtrim(@RecordAuditXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @ApplicationPageLabel = ltrim(rtrim(@ApplicationPageLabel))
		set @ApplicationPageURI = ltrim(rtrim(@ApplicationPageURI))
		set @ApplicationRoute = ltrim(rtrim(@ApplicationRoute))
		set @IPAddress = ltrim(rtrim(@IPAddress))
		set @AuditActionSCD = ltrim(rtrim(@AuditActionSCD))
		set @AuditActionName = ltrim(rtrim(@AuditActionName))
		set @UserName = ltrim(rtrim(@UserName))
		set @LastReviewUser = ltrim(rtrim(@LastReviewUser))
		set @ApplicationEntitySCD = ltrim(rtrim(@ApplicationEntitySCD))
		set @AuditTimeClientTZ = ltrim(rtrim(@AuditTimeClientTZ))

		-- set zero length strings to null to avoid storing them in the record

		if len(@RecordAuditXID) = 0 set @RecordAuditXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@ApplicationPageLabel) = 0 set @ApplicationPageLabel = null
		if len(@ApplicationPageURI) = 0 set @ApplicationPageURI = null
		if len(@ApplicationRoute) = 0 set @ApplicationRoute = null
		if len(@IPAddress) = 0 set @IPAddress = null
		if len(@AuditActionSCD) = 0 set @AuditActionSCD = null
		if len(@AuditActionName) = 0 set @AuditActionName = null
		if len(@UserName) = 0 set @UserName = null
		if len(@LastReviewUser) = 0 set @LastReviewUser = null
		if len(@ApplicationEntitySCD) = 0 set @ApplicationEntitySCD = null
		if len(@AuditTimeClientTZ) = 0 set @AuditTimeClientTZ = null

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @ApplicationUserSessionSID      = isnull(@ApplicationUserSessionSID,ra.ApplicationUserSessionSID)
				,@ApplicationPageSID             = isnull(@ApplicationPageSID,ra.ApplicationPageSID)
				,@AuditActionSID                 = isnull(@AuditActionSID,ra.AuditActionSID)
				,@IsGlassBreak                   = isnull(@IsGlassBreak,ra.IsGlassBreak)
				,@ContextGUID                    = isnull(@ContextGUID,ra.ContextGUID)
				,@UserDefinedColumns             = isnull(@UserDefinedColumns,ra.UserDefinedColumns)
				,@RecordAuditXID                 = isnull(@RecordAuditXID,ra.RecordAuditXID)
				,@LegacyKey                      = isnull(@LegacyKey,ra.LegacyKey)
				,@UpdateUser                     = isnull(@UpdateUser,ra.UpdateUser)
				,@IsReselected                   = isnull(@IsReselected,ra.IsReselected)
				,@IsNullApplied                  = isnull(@IsNullApplied,ra.IsNullApplied)
				,@zContext                       = isnull(@zContext,ra.zContext)
				,@ApplicationPageLabel           = isnull(@ApplicationPageLabel,ra.ApplicationPageLabel)
				,@ApplicationPageURI             = isnull(@ApplicationPageURI,ra.ApplicationPageURI)
				,@ApplicationRoute               = isnull(@ApplicationRoute,ra.ApplicationRoute)
				,@IsSearchPage                   = isnull(@IsSearchPage,ra.IsSearchPage)
				,@ApplicationEntitySID           = isnull(@ApplicationEntitySID,ra.ApplicationEntitySID)
				,@ApplicationPageRowGUID         = isnull(@ApplicationPageRowGUID,ra.ApplicationPageRowGUID)
				,@ApplicationUserSID             = isnull(@ApplicationUserSID,ra.ApplicationUserSID)
				,@ApplicationUserSessionIsActive = isnull(@ApplicationUserSessionIsActive,ra.ApplicationUserSessionIsActive)
				,@IPAddress                      = isnull(@IPAddress,ra.IPAddress)
				,@ApplicationUserSessionRowGUID  = isnull(@ApplicationUserSessionRowGUID,ra.ApplicationUserSessionRowGUID)
				,@AuditActionSCD                 = isnull(@AuditActionSCD,ra.AuditActionSCD)
				,@AuditActionName                = isnull(@AuditActionName,ra.AuditActionName)
				,@AuditActionIsDefault           = isnull(@AuditActionIsDefault,ra.AuditActionIsDefault)
				,@AuditActionRowGUID             = isnull(@AuditActionRowGUID,ra.AuditActionRowGUID)
				,@IsDeleteEnabled                = isnull(@IsDeleteEnabled,ra.IsDeleteEnabled)
				,@PersonSID                      = isnull(@PersonSID,ra.PersonSID)
				,@UserName                       = isnull(@UserName,ra.UserName)
				,@LastReviewTime                 = isnull(@LastReviewTime,ra.LastReviewTime)
				,@LastReviewUser                 = isnull(@LastReviewUser,ra.LastReviewUser)
				,@ApplicationEntitySCD           = isnull(@ApplicationEntitySCD,ra.ApplicationEntitySCD)
				,@AuditTimeClientTZ              = isnull(@AuditTimeClientTZ,ra.AuditTimeClientTZ)
			from
				sf.vRecordAudit ra
			where
				ra.RecordAuditSID = @RecordAuditSID

		end
		
		-- look up the FK value matching the system code if set; this allows
		-- FK values value to be set/updated based on system code values
		
		if @AuditActionSCD is not null and @AuditActionSID = (select x.AuditActionSID from sf.RecordAudit x where x.RecordAuditSID = @RecordAuditSID)
		begin
		
			select
				@AuditActionSID = x.AuditActionSID
			from
				sf.AuditAction x
			where
				x.AuditActionSCD = @AuditActionSCD
		
		end

		-- block changing FK values to parent records which are inactive (UI should prevent this)
		
		if (select x.ApplicationUserSessionSID from sf.RecordAudit x where x.RecordAuditSID = @RecordAuditSID) <> @ApplicationUserSessionSID
		begin
			if (select x.IsActive from sf.ApplicationUserSession x where x.ApplicationUserSessionSID = @ApplicationUserSessionSID) = @OFF
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'AssignmentToInactiveParent'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 assigned is marked inactive. Leave the record unchanged or set to an active %1.'
					,@Arg1        = N'application user session'
				
				raiserror(@errorText, 16, 1)
				
			end
		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		--  insert pre-update logic here ...
		--! </PreUpdate>

		-- update the record

		update
			sf.RecordAudit
		set
			 ApplicationUserSessionSID = @ApplicationUserSessionSID
			,ApplicationPageSID = @ApplicationPageSID
			,AuditActionSID = @AuditActionSID
			,IsGlassBreak = @IsGlassBreak
			,ContextGUID = @ContextGUID
			,UserDefinedColumns = @UserDefinedColumns
			,RecordAuditXID = @RecordAuditXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			RecordAuditSID = @RecordAuditSID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from sf.RecordAudit where RecordAuditSID = @recordAuditSID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'sf.RecordAudit'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'sf.RecordAudit'
					,@Arg2        = @recordAuditSID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'sf.RecordAudit'
				,@Arg3        = @rowsAffected
				,@Arg4        = @recordAuditSID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.RecordAuditSID
			from
				sf.vRecordAudit ent
			where
				ent.RecordAuditSID = @RecordAuditSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.RecordAuditSID
				,ent.ApplicationUserSessionSID
				,ent.ApplicationPageSID
				,ent.AuditActionSID
				,ent.IsGlassBreak
				,ent.ContextGUID
				,ent.UserDefinedColumns
				,ent.RecordAuditXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.ApplicationPageLabel
				,ent.ApplicationPageURI
				,ent.ApplicationRoute
				,ent.IsSearchPage
				,ent.ApplicationEntitySID
				,ent.ApplicationPageRowGUID
				,ent.ApplicationUserSID
				,ent.ApplicationUserSessionIsActive
				,ent.IPAddress
				,ent.ApplicationUserSessionRowGUID
				,ent.AuditActionSCD
				,ent.AuditActionName
				,ent.AuditActionIsDefault
				,ent.AuditActionRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
				,ent.PersonSID
				,ent.UserName
				,ent.LastReviewTime
				,ent.LastReviewUser
				,ent.ApplicationEntitySCD
				,ent.AuditTimeClientTZ
			from
				sf.vRecordAudit ent
			where
				ent.RecordAuditSID = @RecordAuditSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
