SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pRegistrationChangeRequirement#Default]
	 @zContext                                xml               = null                -- default values provided from client-tier (if any)
	,@SetFKDefaults                           bit               = 0                   -- when 1, mandatory FK's are returned as -1 instead of NULL
as
/*********************************************************************************************************************************
Procedure : dbo.pRegistrationChangeRequirement#Default
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : provides a blank row with default values for presentation in the UI for "new" dbo.RegistrationChangeRequirement records
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used in maintenance of the dbo.RegistrationChangeRequirement table. When a new record is to be added from the UI, this procedure
is called to return a blank record with default values. If the client-tier is providing the context for the insert, such as a parent
key value for the new record, it must be passed in the @zContext XML parameter. Multiple values may be passed. The standard format
is: <Parameters MyParameter="1000001"/>.

The @SetFKDefaults parameter can be set to 1 to cause the procedure to return mandatory FK values as -1 rather than NULL. This avoids
the need to create complex types for the procedure on architectures which are not using RIA services.

Note that default values for text, ntext and binary type columns is not supported.  These data types are not permitted as local
variables in the current version of SQL Server and should be replaced by varchar(max) and nvarchar(max) where possible.

Some default values are built-in to the shell of the sproc.  The base table column defaults set in the variable declarations below
were obtained from database default constraints which existed at the time the procedure was generated. The declarations include all
columns of the vRegistrationChangeRequirement entity view, however, only some values (as noted above) are eligible for default setting.  The other
parameters are included for setting context for the table-specific or client-specific logic of the procedure (if any). Default values
returning a question mark "?", system date, or 0 are provided for non-base table columns which are mandatory.  This is done to avoid
compilation errors from the Entity Framework, however, the values will not be applied since they are not in the base table row.

Two levels of customization of the procedure shell are supported. Table-specific logic can be added through the tagged section and a
call to an extended procedure supports client-specific customization. Logic implemented within the code tags is part of the base
product and applies to all client configurations. Client-specific customizations must be implemented in the ext.pRegistrationChangeRequirement
procedure. The extended procedure is only called where it exists in database. The parameter "@Mode" is set to "default.pre" to
advise ext.pRegistrationChangeRequirement of the context of the call. All other parameters are also passed, however, only those parameters eligible
for default setting are passed for "output". All parameters corresponding to entity view columns are returned through a SELECT statement.

In order to simplify working with the XML parameter values, logic in the procedure parses the XML and assigns values to variables where
the variable name matches the column name in the XML (assumes single row).  The variables are then available to the table-specific and
client-specific logic.  The @zContext parameter is also passed, unmodified, to the extended procedure to support situations where values
are passed that are not mapped to column names.


-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@ON                                      bit = cast(1 as bit)				-- constant for bit comparisons
		,@OFF                                     bit = cast(0 as bit)				-- constant for bit comparisons
		,@registrationChangeRequirementSID        int               = -1			-- specific default required by EF - do not override
		,@registrationChangeSID                   int               = null		-- no default provided from DB constraint - OK to override
		,@registrationRequirementSID              int               = null		-- no default provided from DB constraint - OK to override
		,@personDocSID                            int               = null		-- no default provided from DB constraint - OK to override
		,@registrantExamSID                       int               = null		-- no default provided from DB constraint - OK to override
		,@expiryMonths                            smallint          = (0)			-- default provided from DB constraint - OK to override
		,@requirementStatusSID                    int               = null		-- no default provided from DB constraint - OK to override
		,@requirementSequence                     int               = (10)		-- default provided from DB constraint - OK to override
		,@userDefinedColumns                      xml               = null		-- no default provided from DB constraint - OK to override
		,@registrationChangeRequirementXID        varchar(150)      = null		-- no default provided from DB constraint - OK to override
		,@legacyKey                               nvarchar(50)      = null		-- no default provided from DB constraint - OK to override
		,@isDeleted                               bit               = (0)			-- default provided from DB constraint - OK to override
		,@createUser                              nvarchar(75)      = suser_sname()											-- default value ignored (value set by UI)
		,@createTime                              datetimeoffset(7) = sysdatetimeoffset()								-- default value ignored (set to system time)
		,@updateUser                              nvarchar(75)      = suser_sname()											-- default value ignored (value set by UI)
		,@updateTime                              datetimeoffset(7) = sysdatetimeoffset()								-- default value ignored (set to system time)
		,@rowGUID                                 uniqueidentifier  = newid()	-- default value ignored (value set by system)
		,@rowStamp                                timestamp         = null		-- default value ignored (value set by system)
		,@registrationSID                         int               = 0				-- not a base table column (default ignored)
		,@practiceRegisterSectionSID              int               = 0				-- not a base table column (default ignored)
		,@registrationYear                        smallint          = 0				-- not a base table column (default ignored)
		,@nextFollowUp                            date												-- not a base table column (default ignored)
		,@registrationEffective                   date												-- not a base table column (default ignored)
		,@reservedRegistrantNo                    varchar(50)									-- not a base table column (default ignored)
		,@reasonSID                               int													-- not a base table column (default ignored)
		,@registrationChangeInvoiceSID            int													-- not a base table column (default ignored)
		,@complaintSID                            int													-- not a base table column (default ignored)
		,@registrationChangeRowGUID               uniqueidentifier  = newid()	-- not a base table column (default ignored)
		,@registrationRequirementTypeSID          int               = 0				-- not a base table column (default ignored)
		,@registrationRequirementLabel            nvarchar(35)      = N'?'		-- not a base table column (default ignored)
		,@registrationRequirementPersonDocTypeSID int													-- not a base table column (default ignored)
		,@registrationRequirementExamSID          int													-- not a base table column (default ignored)
		,@registrationRequirementExpiryMonths     smallint          = 0				-- not a base table column (default ignored)
		,@registrationRequirementIsActive         bit               = 0				-- not a base table column (default ignored)
		,@registrationRequirementRowGUID          uniqueidentifier  = newid()	-- not a base table column (default ignored)
		,@requirementStatusSCD                    varchar(10)       = '?'			-- not a base table column (default ignored)
		,@requirementStatusLabel                  nvarchar(35)      = N'?'		-- not a base table column (default ignored)
		,@isFinal                                 bit               = 0				-- not a base table column (default ignored)
		,@requirementStatusSequence               int               = 0				-- not a base table column (default ignored)
		,@requirementStatusIsDefault              bit               = 0				-- not a base table column (default ignored)
		,@requirementStatusRowGUID                uniqueidentifier  = newid()	-- not a base table column (default ignored)
		,@personSID                               int													-- not a base table column (default ignored)
		,@personDocPersonDocTypeSID               int													-- not a base table column (default ignored)
		,@documentTitle                           nvarchar(100)								-- not a base table column (default ignored)
		,@additionalInfo                          nvarchar(50)								-- not a base table column (default ignored)
		,@archivedTime                            datetimeoffset(7)						-- not a base table column (default ignored)
		,@fileTypeSID                             int													-- not a base table column (default ignored)
		,@fileTypeSCD                             varchar(8)									-- not a base table column (default ignored)
		,@showToRegistrant                        bit													-- not a base table column (default ignored)
		,@applicationGrantSID                     int													-- not a base table column (default ignored)
		,@isRemoved                               bit													-- not a base table column (default ignored)
		,@expiryDate                              date												-- not a base table column (default ignored)
		,@applicationReportSID                    int													-- not a base table column (default ignored)
		,@reportEntitySID                         int													-- not a base table column (default ignored)
		,@personDocCancelledTime                  datetimeoffset(7)						-- not a base table column (default ignored)
		,@personDocProcessedTime                  datetimeoffset(7)						-- not a base table column (default ignored)
		,@contextLink                             uniqueidentifier						-- not a base table column (default ignored)
		,@personDocRowGUID                        uniqueidentifier						-- not a base table column (default ignored)
		,@registrantSID                           int													-- not a base table column (default ignored)
		,@registrantExamExamSID                   int													-- not a base table column (default ignored)
		,@examDate                                date												-- not a base table column (default ignored)
		,@examResultDate                          date												-- not a base table column (default ignored)
		,@passingScore                            int													-- not a base table column (default ignored)
		,@score                                   int													-- not a base table column (default ignored)
		,@examStatusSID                           int													-- not a base table column (default ignored)
		,@schedulingPreferences                   nvarchar(1000)							-- not a base table column (default ignored)
		,@assignedLocation                        varchar(15)									-- not a base table column (default ignored)
		,@examReference                           varchar(25)									-- not a base table column (default ignored)
		,@examOfferingSID                         int													-- not a base table column (default ignored)
		,@registrantExamInvoiceSID                int													-- not a base table column (default ignored)
		,@confirmedTime                           datetimeoffset(7)						-- not a base table column (default ignored)
		,@registrantExamCancelledTime             datetimeoffset(7)						-- not a base table column (default ignored)
		,@registrantExamProcessedTime             datetimeoffset(7)						-- not a base table column (default ignored)
		,@registrantExamRowGUID                   uniqueidentifier						-- not a base table column (default ignored)
		,@isDeleteEnabled                         bit													-- not a base table column (default ignored)
		,@isReselected                            tinyint           = 1				-- specific default required by EF - do not override
		,@isNullApplied                           bit               = 1				-- specific default required by EF - do not override
		,@isMandatory                             bit               = 0				-- not a base table column (default ignored)

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end
		-- set mandatory FK values to -1 where requested
		
		if @SetFKDefaults = @ON
		begin
			set @registrationChangeSID = -1
			set @registrationRequirementSID = -1
			set @requirementStatusSID = -1
		end

		-- assign literal defaults passed through @zContext where
		-- provided otherwise leave database default in place
		
		select
			 @registrationChangeSID             = isnull(context.node.value('@RegistrationChangeSID'           ,'int'         ),@registrationChangeSID)
			,@registrationRequirementSID        = isnull(context.node.value('@RegistrationRequirementSID'      ,'int'         ),@registrationRequirementSID)
			,@personDocSID                      = isnull(context.node.value('@PersonDocSID'                    ,'int'         ),@personDocSID)
			,@registrantExamSID                 = isnull(context.node.value('@RegistrantExamSID'               ,'int'         ),@registrantExamSID)
			,@expiryMonths                      = isnull(context.node.value('@ExpiryMonths'                    ,'smallint'    ),@expiryMonths)
			,@requirementStatusSID              = isnull(context.node.value('@RequirementStatusSID'            ,'int'         ),@requirementStatusSID)
			,@requirementSequence               = isnull(context.node.value('@RequirementSequence'             ,'int'         ),@requirementSequence)
			,@registrationChangeRequirementXID  = isnull(context.node.value('@RegistrationChangeRequirementXID','varchar(150)'),@registrationChangeRequirementXID)
			,@legacyKey                         = isnull(context.node.value('@LegacyKey'                       ,'nvarchar(50)'),@legacyKey)
		from
			@zContext.nodes('Parameters') as context(node)
		
		-- set default value on foreign keys where configured
		-- and where no DB or literal value was passed for it
		
		if isnull(@requirementStatusSID,0) = 0 select @requirementStatusSID = x.RequirementStatusSID from dbo.RequirementStatus x where x.IsDefault = @ON

		--! <Overrides>
		--  Tim Edlund | Apr 2018
		-- Set the next sequence number to +10
		-- from the current maximum and round

		if @registrationChangeSID is not null and @requirementSequence = 10
		begin

			select
				@requirementSequence = max(rcr.RequirementSequence)
			from
				dbo.RegistrationChangeRequirement rcr
			where
				rcr.RegistrationChangeSID = @registrationChangeSID;

			if @@rowcount = 0
			begin
				set @requirementSequence = 10
			end
			else
			begin
				set @requirementSequence = round(@requirementSequence + 10 , -1)
			end

		end;
		--! </Overrides>
	
		-- call the extended version of the procedure (if it exists) for "default.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pRegistrationChangeRequirement'
		)
		begin
		
			exec @errorNo = ext.pRegistrationChangeRequirement
				 @Mode                                    = 'default.pre'
				,@RegistrationChangeRequirementSID = @registrationChangeRequirementSID
				,@RegistrationChangeSID = @registrationChangeSID output
				,@RegistrationRequirementSID = @registrationRequirementSID output
				,@PersonDocSID = @personDocSID output
				,@RegistrantExamSID = @registrantExamSID output
				,@ExpiryMonths = @expiryMonths output
				,@RequirementStatusSID = @requirementStatusSID output
				,@RequirementSequence = @requirementSequence output
				,@UserDefinedColumns = @userDefinedColumns output
				,@RegistrationChangeRequirementXID = @registrationChangeRequirementXID output
				,@LegacyKey = @legacyKey output
				,@IsDeleted = @isDeleted
				,@CreateUser = @createUser
				,@CreateTime = @createTime
				,@UpdateUser = @updateUser
				,@UpdateTime = @updateTime
				,@RowGUID = @rowGUID
				,@RowStamp = @rowStamp
				,@RegistrationSID = @registrationSID
				,@PracticeRegisterSectionSID = @practiceRegisterSectionSID
				,@RegistrationYear = @registrationYear
				,@NextFollowUp = @nextFollowUp
				,@RegistrationEffective = @registrationEffective
				,@ReservedRegistrantNo = @reservedRegistrantNo
				,@ReasonSID = @reasonSID
				,@RegistrationChangeInvoiceSID = @registrationChangeInvoiceSID
				,@ComplaintSID = @complaintSID
				,@RegistrationChangeRowGUID = @registrationChangeRowGUID
				,@RegistrationRequirementTypeSID = @registrationRequirementTypeSID
				,@RegistrationRequirementLabel = @registrationRequirementLabel
				,@RegistrationRequirementPersonDocTypeSID = @registrationRequirementPersonDocTypeSID
				,@RegistrationRequirementExamSID = @registrationRequirementExamSID
				,@RegistrationRequirementExpiryMonths = @registrationRequirementExpiryMonths
				,@RegistrationRequirementIsActive = @registrationRequirementIsActive
				,@RegistrationRequirementRowGUID = @registrationRequirementRowGUID
				,@RequirementStatusSCD = @requirementStatusSCD
				,@RequirementStatusLabel = @requirementStatusLabel
				,@IsFinal = @isFinal
				,@RequirementStatusSequence = @requirementStatusSequence
				,@RequirementStatusIsDefault = @requirementStatusIsDefault
				,@RequirementStatusRowGUID = @requirementStatusRowGUID
				,@PersonSID = @personSID
				,@PersonDocPersonDocTypeSID = @personDocPersonDocTypeSID
				,@DocumentTitle = @documentTitle
				,@AdditionalInfo = @additionalInfo
				,@ArchivedTime = @archivedTime
				,@FileTypeSID = @fileTypeSID
				,@FileTypeSCD = @fileTypeSCD
				,@ShowToRegistrant = @showToRegistrant
				,@ApplicationGrantSID = @applicationGrantSID
				,@IsRemoved = @isRemoved
				,@ExpiryDate = @expiryDate
				,@ApplicationReportSID = @applicationReportSID
				,@ReportEntitySID = @reportEntitySID
				,@PersonDocCancelledTime = @personDocCancelledTime
				,@PersonDocProcessedTime = @personDocProcessedTime
				,@ContextLink = @contextLink
				,@PersonDocRowGUID = @personDocRowGUID
				,@RegistrantSID = @registrantSID
				,@RegistrantExamExamSID = @registrantExamExamSID
				,@ExamDate = @examDate
				,@ExamResultDate = @examResultDate
				,@PassingScore = @passingScore
				,@Score = @score
				,@ExamStatusSID = @examStatusSID
				,@SchedulingPreferences = @schedulingPreferences
				,@AssignedLocation = @assignedLocation
				,@ExamReference = @examReference
				,@ExamOfferingSID = @examOfferingSID
				,@RegistrantExamInvoiceSID = @registrantExamInvoiceSID
				,@ConfirmedTime = @confirmedTime
				,@RegistrantExamCancelledTime = @registrantExamCancelledTime
				,@RegistrantExamProcessedTime = @registrantExamProcessedTime
				,@RegistrantExamRowGUID = @registrantExamRowGUID
				,@IsDeleteEnabled = @isDeleteEnabled
				,@IsReselected = @isReselected
				,@IsNullApplied = @isNullApplied
				,@zContext = @zContext output
				,@IsMandatory = @isMandatory
		
		end

		select
			 @registrationChangeRequirementSID RegistrationChangeRequirementSID
			,@registrationChangeSID RegistrationChangeSID
			,@registrationRequirementSID RegistrationRequirementSID
			,@personDocSID PersonDocSID
			,@registrantExamSID RegistrantExamSID
			,@expiryMonths ExpiryMonths
			,@requirementStatusSID RequirementStatusSID
			,@requirementSequence RequirementSequence
			,@userDefinedColumns UserDefinedColumns
			,@registrationChangeRequirementXID RegistrationChangeRequirementXID
			,@legacyKey LegacyKey
			,@isDeleted IsDeleted
			,@createUser CreateUser
			,@createTime CreateTime
			,@updateUser UpdateUser
			,@updateTime UpdateTime
			,@rowGUID RowGUID
			,@rowStamp RowStamp
			,@registrationSID RegistrationSID
			,@practiceRegisterSectionSID PracticeRegisterSectionSID
			,@registrationYear RegistrationYear
			,@nextFollowUp NextFollowUp
			,@registrationEffective RegistrationEffective
			,@reservedRegistrantNo ReservedRegistrantNo
			,@reasonSID ReasonSID
			,@registrationChangeInvoiceSID RegistrationChangeInvoiceSID
			,@complaintSID ComplaintSID
			,@registrationChangeRowGUID RegistrationChangeRowGUID
			,@registrationRequirementTypeSID RegistrationRequirementTypeSID
			,@registrationRequirementLabel RegistrationRequirementLabel
			,@registrationRequirementPersonDocTypeSID RegistrationRequirementPersonDocTypeSID
			,@registrationRequirementExamSID RegistrationRequirementExamSID
			,@registrationRequirementExpiryMonths RegistrationRequirementExpiryMonths
			,@registrationRequirementIsActive RegistrationRequirementIsActive
			,@registrationRequirementRowGUID RegistrationRequirementRowGUID
			,@requirementStatusSCD RequirementStatusSCD
			,@requirementStatusLabel RequirementStatusLabel
			,@isFinal IsFinal
			,@requirementStatusSequence RequirementStatusSequence
			,@requirementStatusIsDefault RequirementStatusIsDefault
			,@requirementStatusRowGUID RequirementStatusRowGUID
			,@personSID PersonSID
			,@personDocPersonDocTypeSID PersonDocPersonDocTypeSID
			,@documentTitle DocumentTitle
			,@additionalInfo AdditionalInfo
			,@archivedTime ArchivedTime
			,@fileTypeSID FileTypeSID
			,@fileTypeSCD FileTypeSCD
			,@showToRegistrant ShowToRegistrant
			,@applicationGrantSID ApplicationGrantSID
			,@isRemoved IsRemoved
			,@expiryDate ExpiryDate
			,@applicationReportSID ApplicationReportSID
			,@reportEntitySID ReportEntitySID
			,@personDocCancelledTime PersonDocCancelledTime
			,@personDocProcessedTime PersonDocProcessedTime
			,@contextLink ContextLink
			,@personDocRowGUID PersonDocRowGUID
			,@registrantSID RegistrantSID
			,@registrantExamExamSID RegistrantExamExamSID
			,@examDate ExamDate
			,@examResultDate ExamResultDate
			,@passingScore PassingScore
			,@score Score
			,@examStatusSID ExamStatusSID
			,@schedulingPreferences SchedulingPreferences
			,@assignedLocation AssignedLocation
			,@examReference ExamReference
			,@examOfferingSID ExamOfferingSID
			,@registrantExamInvoiceSID RegistrantExamInvoiceSID
			,@confirmedTime ConfirmedTime
			,@registrantExamCancelledTime RegistrantExamCancelledTime
			,@registrantExamProcessedTime RegistrantExamProcessedTime
			,@registrantExamRowGUID RegistrantExamRowGUID
			,@isDeleteEnabled IsDeleteEnabled
			,@isReselected IsReselected
			,@isNullApplied IsNullApplied
			,@zContext zContext
			,@isMandatory IsMandatory

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
