SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pLearningPlanActivity#Default]
	 @zContext                       xml               = null                -- default values provided from client-tier (if any)
	,@SetFKDefaults                  bit               = 0                   -- when 1, mandatory FK's are returned as -1 instead of NULL
as
/*********************************************************************************************************************************
Procedure : dbo.pLearningPlanActivity#Default
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : provides a blank row with default values for presentation in the UI for "new" dbo.LearningPlanActivity records
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used in maintenance of the dbo.LearningPlanActivity table. When a new record is to be added from the UI, this procedure
is called to return a blank record with default values. If the client-tier is providing the context for the insert, such as a parent
key value for the new record, it must be passed in the @zContext XML parameter. Multiple values may be passed. The standard format
is: <Parameters MyParameter="1000001"/>.

The @SetFKDefaults parameter can be set to 1 to cause the procedure to return mandatory FK values as -1 rather than NULL. This avoids
the need to create complex types for the procedure on architectures which are not using RIA services.

Note that default values for text, ntext and binary type columns is not supported.  These data types are not permitted as local
variables in the current version of SQL Server and should be replaced by varchar(max) and nvarchar(max) where possible.

Some default values are built-in to the shell of the sproc.  The base table column defaults set in the variable declarations below
were obtained from database default constraints which existed at the time the procedure was generated. The declarations include all
columns of the vLearningPlanActivity entity view, however, only some values (as noted above) are eligible for default setting.  The other
parameters are included for setting context for the table-specific or client-specific logic of the procedure (if any). Default values
returning a question mark "?", system date, or 0 are provided for non-base table columns which are mandatory.  This is done to avoid
compilation errors from the Entity Framework, however, the values will not be applied since they are not in the base table row.

Two levels of customization of the procedure shell are supported. Table-specific logic can be added through the tagged section and a
call to an extended procedure supports client-specific customization. Logic implemented within the code tags is part of the base
product and applies to all client configurations. Client-specific customizations must be implemented in the ext.pLearningPlanActivity
procedure. The extended procedure is only called where it exists in database. The parameter "@Mode" is set to "default.pre" to
advise ext.pLearningPlanActivity of the context of the call. All other parameters are also passed, however, only those parameters eligible
for default setting are passed for "output". All parameters corresponding to entity view columns are returned through a SELECT statement.

In order to simplify working with the XML parameter values, logic in the procedure parses the XML and assigns values to variables where
the variable name matches the column name in the XML (assumes single row).  The variables are then available to the table-specific and
client-specific logic.  The @zContext parameter is also passed, unmodified, to the extended procedure to support situations where values
are passed that are not mapped to column names.


-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@ON                             bit = cast(1 as bit)									-- constant for bit comparisons
		,@OFF                            bit = cast(0 as bit)									-- constant for bit comparisons
		,@learningPlanActivitySID        int               = -1								-- specific default required by EF - do not override
		,@registrantLearningPlanSID      int               = null							-- no default provided from DB constraint - OK to override
		,@competenceTypeActivitySID      int               = null							-- no default provided from DB constraint - OK to override
		,@unitValue                      decimal(5,2)      = (1.0)						-- default provided from DB constraint - OK to override
		,@carryOverUnitValue             decimal(5,2)      = (0.0)						-- default provided from DB constraint - OK to override
		,@activityDate                   date              = null							-- no default provided from DB constraint - OK to override
		,@learningClaimTypeSID           int               = null							-- no default provided from DB constraint - OK to override
		,@learningPlanActivityCategory   nvarchar(65)      = null							-- no default provided from DB constraint - OK to override
		,@activityDescription            nvarchar(max)     = null							-- no default provided from DB constraint - OK to override
		,@plannedCompletion              date              = null							-- no default provided from DB constraint - OK to override
		,@orgSID                         int               = null							-- no default provided from DB constraint - OK to override
		,@isSubjectToReview              bit               = CONVERT(bit,(0))	-- default provided from DB constraint - OK to override
		,@isArchived                     bit               = CONVERT(bit,(0))	-- default provided from DB constraint - OK to override
		,@userDefinedColumns             xml               = null							-- no default provided from DB constraint - OK to override
		,@learningPlanActivityXID        varchar(150)      = null							-- no default provided from DB constraint - OK to override
		,@legacyKey                      nvarchar(50)      = null							-- no default provided from DB constraint - OK to override
		,@isDeleted                      bit               = (0)							-- default provided from DB constraint - OK to override
		,@createUser                     nvarchar(75)      = suser_sname()		-- default value ignored (value set by UI)
		,@createTime                     datetimeoffset(7) = sysdatetimeoffset()												-- default value ignored (set to system time)
		,@updateUser                     nvarchar(75)      = suser_sname()		-- default value ignored (value set by UI)
		,@updateTime                     datetimeoffset(7) = sysdatetimeoffset()												-- default value ignored (set to system time)
		,@rowGUID                        uniqueidentifier  = newid()					-- default value ignored (value set by system)
		,@rowStamp                       timestamp         = null							-- default value ignored (value set by system)
		,@competenceTypeSID              int               = 0								-- not a base table column (default ignored)
		,@competenceActivitySID          int               = 0								-- not a base table column (default ignored)
		,@effectiveTime                  datetime          = sf.fNow()				-- not a base table column (default ignored)
		,@expiryTime                     datetime															-- not a base table column (default ignored)
		,@competenceTypeActivityRowGUID  uniqueidentifier  = newid()					-- not a base table column (default ignored)
		,@learningClaimTypeLabel         nvarchar(35)      = N'?'							-- not a base table column (default ignored)
		,@learningClaimTypeCategory      nvarchar(65)													-- not a base table column (default ignored)
		,@isValidForRenewal              bit               = 0								-- not a base table column (default ignored)
		,@isComplete                     bit               = 0								-- not a base table column (default ignored)
		,@isWithdrawn                    bit               = 0								-- not a base table column (default ignored)
		,@learningClaimTypeIsDefault     bit               = 0								-- not a base table column (default ignored)
		,@learningClaimTypeIsActive      bit               = 0								-- not a base table column (default ignored)
		,@learningClaimTypeRowGUID       uniqueidentifier  = newid()					-- not a base table column (default ignored)
		,@registrantSID                  int               = 0								-- not a base table column (default ignored)
		,@registrationYear               smallint          = 0								-- not a base table column (default ignored)
		,@learningModelSID               int               = 0								-- not a base table column (default ignored)
		,@formVersionSID                 int               = 0								-- not a base table column (default ignored)
		,@lastValidateTime               datetimeoffset(7)										-- not a base table column (default ignored)
		,@nextFollowUp                   date																	-- not a base table column (default ignored)
		,@reasonSID                      int																	-- not a base table column (default ignored)
		,@isAutoApprovalEnabled          bit               = 0								-- not a base table column (default ignored)
		,@parentRowGUID                  uniqueidentifier											-- not a base table column (default ignored)
		,@registrantLearningPlanRowGUID  uniqueidentifier  = newid()					-- not a base table column (default ignored)
		,@parentOrgSID                   int																	-- not a base table column (default ignored)
		,@orgTypeSID                     int																	-- not a base table column (default ignored)
		,@orgName                        nvarchar(150)												-- not a base table column (default ignored)
		,@orgLabel                       nvarchar(35)													-- not a base table column (default ignored)
		,@streetAddress1                 nvarchar(75)													-- not a base table column (default ignored)
		,@streetAddress2                 nvarchar(75)													-- not a base table column (default ignored)
		,@streetAddress3                 nvarchar(75)													-- not a base table column (default ignored)
		,@citySID                        int																	-- not a base table column (default ignored)
		,@postalCode                     varchar(10)													-- not a base table column (default ignored)
		,@regionSID                      int																	-- not a base table column (default ignored)
		,@phone                          varchar(25)													-- not a base table column (default ignored)
		,@fax                            varchar(25)													-- not a base table column (default ignored)
		,@webSite                        varchar(250)													-- not a base table column (default ignored)
		,@emailAddress                   varchar(150)													-- not a base table column (default ignored)
		,@insuranceOrgSID                int																	-- not a base table column (default ignored)
		,@insurancePolicyNo              varchar(25)													-- not a base table column (default ignored)
		,@insuranceAmount                decimal(11,2)												-- not a base table column (default ignored)
		,@isEmployer                     bit																	-- not a base table column (default ignored)
		,@isCredentialAuthority          bit																	-- not a base table column (default ignored)
		,@isInsurer                      bit																	-- not a base table column (default ignored)
		,@isInsuranceCertificateRequired bit																	-- not a base table column (default ignored)
		,@isPublic                       nchar(10)														-- not a base table column (default ignored)
		,@orgIsActive                    bit																	-- not a base table column (default ignored)
		,@isAdminReviewRequired          bit																	-- not a base table column (default ignored)
		,@lastVerifiedTime               datetimeoffset(7)										-- not a base table column (default ignored)
		,@orgRowGUID                     uniqueidentifier											-- not a base table column (default ignored)
		,@isDeleteEnabled                bit																	-- not a base table column (default ignored)
		,@isReselected                   tinyint           = 1								-- specific default required by EF - do not override
		,@isNullApplied                  bit               = 1								-- specific default required by EF - do not override

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end
		-- set mandatory FK values to -1 where requested
		
		if @SetFKDefaults = @ON
		begin
			set @registrantLearningPlanSID = -1
			set @competenceTypeActivitySID = -1
			set @learningClaimTypeSID = -1
		end

		-- assign literal defaults passed through @zContext where
		-- provided otherwise leave database default in place
		
		select
			 @registrantLearningPlanSID     = isnull(context.node.value('@RegistrantLearningPlanSID'   ,'int'          ),@registrantLearningPlanSID)
			,@competenceTypeActivitySID     = isnull(context.node.value('@CompetenceTypeActivitySID'   ,'int'          ),@competenceTypeActivitySID)
			,@unitValue                     = isnull(context.node.value('@UnitValue'                   ,'decimal(5,2)' ),@unitValue)
			,@carryOverUnitValue            = isnull(context.node.value('@CarryOverUnitValue'          ,'decimal(5,2)' ),@carryOverUnitValue)
			,@activityDate                  = isnull(context.node.value('@ActivityDate'                ,'date'         ),@activityDate)
			,@learningClaimTypeSID          = isnull(context.node.value('@LearningClaimTypeSID'        ,'int'          ),@learningClaimTypeSID)
			,@learningPlanActivityCategory  = isnull(context.node.value('@LearningPlanActivityCategory','nvarchar(65)' ),@learningPlanActivityCategory)
			,@activityDescription           = isnull(context.node.value('@ActivityDescription'         ,'nvarchar(max)'),@activityDescription)
			,@plannedCompletion             = isnull(context.node.value('@PlannedCompletion'           ,'date'         ),@plannedCompletion)
			,@orgSID                        = isnull(context.node.value('@OrgSID'                      ,'int'          ),@orgSID)
			,@isSubjectToReview             = isnull(context.node.value('@IsSubjectToReview'           ,'bit'          ),@isSubjectToReview)
			,@isArchived                    = isnull(context.node.value('@IsArchived'                  ,'bit'          ),@isArchived)
			,@learningPlanActivityXID       = isnull(context.node.value('@LearningPlanActivityXID'     ,'varchar(150)' ),@learningPlanActivityXID)
			,@legacyKey                     = isnull(context.node.value('@LegacyKey'                   ,'nvarchar(50)' ),@legacyKey)
		from
			@zContext.nodes('Parameters') as context(node)
		
		-- set default value on foreign keys where configured
		-- and where no DB or literal value was passed for it
		
		if isnull(@learningClaimTypeSID,0) = 0 select @learningClaimTypeSID = x.LearningClaimTypeSID from dbo.LearningClaimType x where x.IsDefault = @ON

		--! <Overrides>
		--  insert default value logic here ...
		--! </Overrides>
	
		-- call the extended version of the procedure (if it exists) for "default.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pLearningPlanActivity'
		)
		begin
		
			exec @errorNo = ext.pLearningPlanActivity
				 @Mode                           = 'default.pre'
				,@LearningPlanActivitySID = @learningPlanActivitySID
				,@RegistrantLearningPlanSID = @registrantLearningPlanSID output
				,@CompetenceTypeActivitySID = @competenceTypeActivitySID output
				,@UnitValue = @unitValue output
				,@CarryOverUnitValue = @carryOverUnitValue output
				,@ActivityDate = @activityDate output
				,@LearningClaimTypeSID = @learningClaimTypeSID output
				,@LearningPlanActivityCategory = @learningPlanActivityCategory output
				,@ActivityDescription = @activityDescription output
				,@PlannedCompletion = @plannedCompletion output
				,@OrgSID = @orgSID output
				,@IsSubjectToReview = @isSubjectToReview output
				,@IsArchived = @isArchived output
				,@UserDefinedColumns = @userDefinedColumns output
				,@LearningPlanActivityXID = @learningPlanActivityXID output
				,@LegacyKey = @legacyKey output
				,@IsDeleted = @isDeleted
				,@CreateUser = @createUser
				,@CreateTime = @createTime
				,@UpdateUser = @updateUser
				,@UpdateTime = @updateTime
				,@RowGUID = @rowGUID
				,@RowStamp = @rowStamp
				,@CompetenceTypeSID = @competenceTypeSID
				,@CompetenceActivitySID = @competenceActivitySID
				,@EffectiveTime = @effectiveTime
				,@ExpiryTime = @expiryTime
				,@CompetenceTypeActivityRowGUID = @competenceTypeActivityRowGUID
				,@LearningClaimTypeLabel = @learningClaimTypeLabel
				,@LearningClaimTypeCategory = @learningClaimTypeCategory
				,@IsValidForRenewal = @isValidForRenewal
				,@IsComplete = @isComplete
				,@IsWithdrawn = @isWithdrawn
				,@LearningClaimTypeIsDefault = @learningClaimTypeIsDefault
				,@LearningClaimTypeIsActive = @learningClaimTypeIsActive
				,@LearningClaimTypeRowGUID = @learningClaimTypeRowGUID
				,@RegistrantSID = @registrantSID
				,@RegistrationYear = @registrationYear
				,@LearningModelSID = @learningModelSID
				,@FormVersionSID = @formVersionSID
				,@LastValidateTime = @lastValidateTime
				,@NextFollowUp = @nextFollowUp
				,@ReasonSID = @reasonSID
				,@IsAutoApprovalEnabled = @isAutoApprovalEnabled
				,@ParentRowGUID = @parentRowGUID
				,@RegistrantLearningPlanRowGUID = @registrantLearningPlanRowGUID
				,@ParentOrgSID = @parentOrgSID
				,@OrgTypeSID = @orgTypeSID
				,@OrgName = @orgName
				,@OrgLabel = @orgLabel
				,@StreetAddress1 = @streetAddress1
				,@StreetAddress2 = @streetAddress2
				,@StreetAddress3 = @streetAddress3
				,@CitySID = @citySID
				,@PostalCode = @postalCode
				,@RegionSID = @regionSID
				,@Phone = @phone
				,@Fax = @fax
				,@WebSite = @webSite
				,@EmailAddress = @emailAddress
				,@InsuranceOrgSID = @insuranceOrgSID
				,@InsurancePolicyNo = @insurancePolicyNo
				,@InsuranceAmount = @insuranceAmount
				,@IsEmployer = @isEmployer
				,@IsCredentialAuthority = @isCredentialAuthority
				,@IsInsurer = @isInsurer
				,@IsInsuranceCertificateRequired = @isInsuranceCertificateRequired
				,@IsPublic = @isPublic
				,@OrgIsActive = @orgIsActive
				,@IsAdminReviewRequired = @isAdminReviewRequired
				,@LastVerifiedTime = @lastVerifiedTime
				,@OrgRowGUID = @orgRowGUID
				,@IsDeleteEnabled = @isDeleteEnabled
				,@IsReselected = @isReselected
				,@IsNullApplied = @isNullApplied
				,@zContext = @zContext output
		
		end

		select
			 @learningPlanActivitySID LearningPlanActivitySID
			,@registrantLearningPlanSID RegistrantLearningPlanSID
			,@competenceTypeActivitySID CompetenceTypeActivitySID
			,@unitValue UnitValue
			,@carryOverUnitValue CarryOverUnitValue
			,@activityDate ActivityDate
			,@learningClaimTypeSID LearningClaimTypeSID
			,@learningPlanActivityCategory LearningPlanActivityCategory
			,@activityDescription ActivityDescription
			,@plannedCompletion PlannedCompletion
			,@orgSID OrgSID
			,@isSubjectToReview IsSubjectToReview
			,@isArchived IsArchived
			,@userDefinedColumns UserDefinedColumns
			,@learningPlanActivityXID LearningPlanActivityXID
			,@legacyKey LegacyKey
			,@isDeleted IsDeleted
			,@createUser CreateUser
			,@createTime CreateTime
			,@updateUser UpdateUser
			,@updateTime UpdateTime
			,@rowGUID RowGUID
			,@rowStamp RowStamp
			,@competenceTypeSID CompetenceTypeSID
			,@competenceActivitySID CompetenceActivitySID
			,@effectiveTime EffectiveTime
			,@expiryTime ExpiryTime
			,@competenceTypeActivityRowGUID CompetenceTypeActivityRowGUID
			,@learningClaimTypeLabel LearningClaimTypeLabel
			,@learningClaimTypeCategory LearningClaimTypeCategory
			,@isValidForRenewal IsValidForRenewal
			,@isComplete IsComplete
			,@isWithdrawn IsWithdrawn
			,@learningClaimTypeIsDefault LearningClaimTypeIsDefault
			,@learningClaimTypeIsActive LearningClaimTypeIsActive
			,@learningClaimTypeRowGUID LearningClaimTypeRowGUID
			,@registrantSID RegistrantSID
			,@registrationYear RegistrationYear
			,@learningModelSID LearningModelSID
			,@formVersionSID FormVersionSID
			,@lastValidateTime LastValidateTime
			,@nextFollowUp NextFollowUp
			,@reasonSID ReasonSID
			,@isAutoApprovalEnabled IsAutoApprovalEnabled
			,@parentRowGUID ParentRowGUID
			,@registrantLearningPlanRowGUID RegistrantLearningPlanRowGUID
			,@parentOrgSID ParentOrgSID
			,@orgTypeSID OrgTypeSID
			,@orgName OrgName
			,@orgLabel OrgLabel
			,@streetAddress1 StreetAddress1
			,@streetAddress2 StreetAddress2
			,@streetAddress3 StreetAddress3
			,@citySID CitySID
			,@postalCode PostalCode
			,@regionSID RegionSID
			,@phone Phone
			,@fax Fax
			,@webSite WebSite
			,@emailAddress EmailAddress
			,@insuranceOrgSID InsuranceOrgSID
			,@insurancePolicyNo InsurancePolicyNo
			,@insuranceAmount InsuranceAmount
			,@isEmployer IsEmployer
			,@isCredentialAuthority IsCredentialAuthority
			,@isInsurer IsInsurer
			,@isInsuranceCertificateRequired IsInsuranceCertificateRequired
			,@isPublic IsPublic
			,@orgIsActive OrgIsActive
			,@isAdminReviewRequired IsAdminReviewRequired
			,@lastVerifiedTime LastVerifiedTime
			,@orgRowGUID OrgRowGUID
			,@isDeleteEnabled IsDeleteEnabled
			,@isReselected IsReselected
			,@isNullApplied IsNullApplied
			,@zContext zContext

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
