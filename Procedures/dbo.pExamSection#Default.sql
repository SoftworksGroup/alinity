SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pExamSection#Default]
	 @zContext                  xml               = null                -- default values provided from client-tier (if any)
	,@SetFKDefaults             bit               = 0                   -- when 1, mandatory FK's are returned as -1 instead of NULL
as
/*********************************************************************************************************************************
Procedure : dbo.pExamSection#Default
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : provides a blank row with default values for presentation in the UI for "new" dbo.ExamSection records
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used in maintenance of the dbo.ExamSection table. When a new record is to be added from the UI, this procedure
is called to return a blank record with default values. If the client-tier is providing the context for the insert, such as a parent
key value for the new record, it must be passed in the @zContext XML parameter. Multiple values may be passed. The standard format
is: <Parameters MyParameter="1000001"/>.

The @SetFKDefaults parameter can be set to 1 to cause the procedure to return mandatory FK values as -1 rather than NULL. This avoids
the need to create complex types for the procedure on architectures which are not using RIA services.

Note that default values for text, ntext and binary type columns is not supported.  These data types are not permitted as local
variables in the current version of SQL Server and should be replaced by varchar(max) and nvarchar(max) where possible.

Some default values are built-in to the shell of the sproc.  The base table column defaults set in the variable declarations below
were obtained from database default constraints which existed at the time the procedure was generated. The declarations include all
columns of the vExamSection entity view, however, only some values (as noted above) are eligible for default setting.  The other
parameters are included for setting context for the table-specific or client-specific logic of the procedure (if any). Default values
returning a question mark "?", system date, or 0 are provided for non-base table columns which are mandatory.  This is done to avoid
compilation errors from the Entity Framework, however, the values will not be applied since they are not in the base table row.

Two levels of customization of the procedure shell are supported. Table-specific logic can be added through the tagged section and a
call to an extended procedure supports client-specific customization. Logic implemented within the code tags is part of the base
product and applies to all client configurations. Client-specific customizations must be implemented in the ext.pExamSection
procedure. The extended procedure is only called where it exists in database. The parameter "@Mode" is set to "default.pre" to
advise ext.pExamSection of the context of the call. All other parameters are also passed, however, only those parameters eligible
for default setting are passed for "output". All parameters corresponding to entity view columns are returned through a SELECT statement.

In order to simplify working with the XML parameter values, logic in the procedure parses the XML and assigns values to variables where
the variable name matches the column name in the XML (assumes single row).  The variables are then available to the table-specific and
client-specific logic.  The @zContext parameter is also passed, unmodified, to the extended procedure to support situations where values
are passed that are not mapped to column names.


-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@ON                        bit = cast(1 as bit)											-- constant for bit comparisons
		,@OFF                       bit = cast(0 as bit)											-- constant for bit comparisons
		,@examSectionSID            int               = -1										-- specific default required by EF - do not override
		,@examSID                   int               = null									-- no default provided from DB constraint - OK to override
		,@sequence                  smallint          = (0)										-- default provided from DB constraint - OK to override
		,@sectionTitle              nvarchar(85)      = null									-- no default provided from DB constraint - OK to override
		,@sectionText               nvarchar(max)     = null									-- no default provided from DB constraint - OK to override
		,@randomQuestionCount       smallint          = (0)										-- default provided from DB constraint - OK to override
		,@weightPerQuestion         smallint          = (0)										-- default provided from DB constraint - OK to override
		,@minimumCorrect            smallint          = (0)										-- default provided from DB constraint - OK to override
		,@userDefinedColumns        xml               = null									-- no default provided from DB constraint - OK to override
		,@examSectionXID            varchar(150)      = null									-- no default provided from DB constraint - OK to override
		,@legacyKey                 nvarchar(50)      = null									-- no default provided from DB constraint - OK to override
		,@isDeleted                 bit               = (0)										-- default provided from DB constraint - OK to override
		,@createUser                nvarchar(75)      = suser_sname()					-- default value ignored (value set by UI)
		,@createTime                datetimeoffset(7) = sysdatetimeoffset()		-- default value ignored (set to system time)
		,@updateUser                nvarchar(75)      = suser_sname()					-- default value ignored (value set by UI)
		,@updateTime                datetimeoffset(7) = sysdatetimeoffset()		-- default value ignored (set to system time)
		,@rowGUID                   uniqueidentifier  = newid()								-- default value ignored (value set by system)
		,@rowStamp                  timestamp         = null									-- default value ignored (value set by system)
		,@examName                  nvarchar(50)      = N'?'									-- not a base table column (default ignored)
		,@examCategory              nvarchar(65)															-- not a base table column (default ignored)
		,@passingScore              int																				-- not a base table column (default ignored)
		,@effectiveTime             datetime          = sf.fNow()							-- not a base table column (default ignored)
		,@expiryTime                datetime																	-- not a base table column (default ignored)
		,@isOnlineExam              bit               = 0											-- not a base table column (default ignored)
		,@isEnabledOnPortal         bit               = 0											-- not a base table column (default ignored)
		,@examSequence              int               = 0											-- not a base table column (default ignored)
		,@cultureSID                int               = 0											-- not a base table column (default ignored)
		,@lastVerifiedTime          datetimeoffset(7)													-- not a base table column (default ignored)
		,@minLagDaysBetweenAttempts smallint																	-- not a base table column (default ignored)
		,@maxAttemptsPerYear        tinyint																		-- not a base table column (default ignored)
		,@vendorExamID              varchar(25)																-- not a base table column (default ignored)
		,@examRowGUID               uniqueidentifier  = newid()								-- not a base table column (default ignored)
		,@isDeleteEnabled           bit																				-- not a base table column (default ignored)
		,@isReselected              tinyint           = 1											-- specific default required by EF - do not override
		,@isNullApplied             bit               = 1											-- specific default required by EF - do not override

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end
		-- set mandatory FK values to -1 where requested
		
		if @SetFKDefaults = @ON
		begin
			set @examSID = -1
		end

		-- assign literal defaults passed through @zContext where
		-- provided otherwise leave database default in place
		
		select
			 @examSID              = isnull(context.node.value('@ExamSID'            ,'int'          ),@examSID)
			,@sequence             = isnull(context.node.value('@Sequence'           ,'smallint'     ),@sequence)
			,@sectionTitle         = isnull(context.node.value('@SectionTitle'       ,'nvarchar(85)' ),@sectionTitle)
			,@sectionText          = isnull(context.node.value('@SectionText'        ,'nvarchar(max)'),@sectionText)
			,@randomQuestionCount  = isnull(context.node.value('@RandomQuestionCount','smallint'     ),@randomQuestionCount)
			,@weightPerQuestion    = isnull(context.node.value('@WeightPerQuestion'  ,'smallint'     ),@weightPerQuestion)
			,@minimumCorrect       = isnull(context.node.value('@MinimumCorrect'     ,'smallint'     ),@minimumCorrect)
			,@examSectionXID       = isnull(context.node.value('@ExamSectionXID'     ,'varchar(150)' ),@examSectionXID)
			,@legacyKey            = isnull(context.node.value('@LegacyKey'          ,'nvarchar(50)' ),@legacyKey)
		from
			@zContext.nodes('Parameters') as context(node)
		

		--! <Overrides>
		--  insert default value logic here ...
		--! </Overrides>
	
		-- call the extended version of the procedure (if it exists) for "default.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pExamSection'
		)
		begin
		
			exec @errorNo = ext.pExamSection
				 @Mode                      = 'default.pre'
				,@ExamSectionSID = @examSectionSID
				,@ExamSID = @examSID output
				,@Sequence = @sequence output
				,@SectionTitle = @sectionTitle output
				,@SectionText = @sectionText output
				,@RandomQuestionCount = @randomQuestionCount output
				,@WeightPerQuestion = @weightPerQuestion output
				,@MinimumCorrect = @minimumCorrect output
				,@UserDefinedColumns = @userDefinedColumns output
				,@ExamSectionXID = @examSectionXID output
				,@LegacyKey = @legacyKey output
				,@IsDeleted = @isDeleted
				,@CreateUser = @createUser
				,@CreateTime = @createTime
				,@UpdateUser = @updateUser
				,@UpdateTime = @updateTime
				,@RowGUID = @rowGUID
				,@RowStamp = @rowStamp
				,@ExamName = @examName
				,@ExamCategory = @examCategory
				,@PassingScore = @passingScore
				,@EffectiveTime = @effectiveTime
				,@ExpiryTime = @expiryTime
				,@IsOnlineExam = @isOnlineExam
				,@IsEnabledOnPortal = @isEnabledOnPortal
				,@ExamSequence = @examSequence
				,@CultureSID = @cultureSID
				,@LastVerifiedTime = @lastVerifiedTime
				,@MinLagDaysBetweenAttempts = @minLagDaysBetweenAttempts
				,@MaxAttemptsPerYear = @maxAttemptsPerYear
				,@VendorExamID = @vendorExamID
				,@ExamRowGUID = @examRowGUID
				,@IsDeleteEnabled = @isDeleteEnabled
				,@IsReselected = @isReselected
				,@IsNullApplied = @isNullApplied
				,@zContext = @zContext output
		
		end

		select
			 @examSectionSID ExamSectionSID
			,@examSID ExamSID
			,@sequence Sequence
			,@sectionTitle SectionTitle
			,@sectionText SectionText
			,@randomQuestionCount RandomQuestionCount
			,@weightPerQuestion WeightPerQuestion
			,@minimumCorrect MinimumCorrect
			,@userDefinedColumns UserDefinedColumns
			,@examSectionXID ExamSectionXID
			,@legacyKey LegacyKey
			,@isDeleted IsDeleted
			,@createUser CreateUser
			,@createTime CreateTime
			,@updateUser UpdateUser
			,@updateTime UpdateTime
			,@rowGUID RowGUID
			,@rowStamp RowStamp
			,@examName ExamName
			,@examCategory ExamCategory
			,@passingScore PassingScore
			,@effectiveTime EffectiveTime
			,@expiryTime ExpiryTime
			,@isOnlineExam IsOnlineExam
			,@isEnabledOnPortal IsEnabledOnPortal
			,@examSequence ExamSequence
			,@cultureSID CultureSID
			,@lastVerifiedTime LastVerifiedTime
			,@minLagDaysBetweenAttempts MinLagDaysBetweenAttempts
			,@maxAttemptsPerYear MaxAttemptsPerYear
			,@vendorExamID VendorExamID
			,@examRowGUID ExamRowGUID
			,@isDeleteEnabled IsDeleteEnabled
			,@isReselected IsReselected
			,@isNullApplied IsNullApplied
			,@zContext zContext

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
