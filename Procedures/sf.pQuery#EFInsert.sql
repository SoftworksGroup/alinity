SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [sf].[pQuery#EFInsert]
	 @QueryCategorySID         int               = null											-- required! if not passed value must be set in custom logic prior to insert
	,@ApplicationPageSID       int               = null											-- required! if not passed value must be set in custom logic prior to insert
	,@QueryLabel               nvarchar(35)      = null											-- required! if not passed value must be set in custom logic prior to insert
	,@ToolTip                  nvarchar(250)     = null											
	,@LastExecuteTime          datetimeoffset(7) = null											-- default: sysdatetimeoffset()
	,@LastExecuteUser          nvarchar(75)      = null											-- default: suser_sname()
	,@ExecuteCount             int               = null											-- default: (0)
	,@QuerySQL                 nvarchar(max)     = null											
	,@QueryParameters          xml               = null											
	,@QueryCode                varchar(30)       = null											-- default: 'None'
	,@IsActive                 bit               = null											-- default: (1)
	,@IsApplicationPageDefault bit               = null											-- default: (0)
	,@UserDefinedColumns       xml               = null											
	,@QueryXID                 varchar(150)      = null											
	,@LegacyKey                nvarchar(50)      = null											
	,@CreateUser               nvarchar(75)      = null											-- default: suser_sname()
	,@IsReselected             tinyint           = null											-- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@zContext                 xml               = null											-- other values defining context for the insert (if any)
	,@ApplicationPageLabel     nvarchar(35)      = null											-- not a base table column (default ignored)
	,@ApplicationPageURI       varchar(150)      = null											-- not a base table column (default ignored)
	,@ApplicationRoute         varchar(150)      = null											-- not a base table column (default ignored)
	,@IsSearchPage             bit               = null											-- not a base table column (default ignored)
	,@ApplicationEntitySID     int               = null											-- not a base table column (default ignored)
	,@ApplicationPageRowGUID   uniqueidentifier  = null											-- not a base table column (default ignored)
	,@QueryCategoryLabel       nvarchar(35)      = null											-- not a base table column (default ignored)
	,@QueryCategoryCode        varchar(30)       = null											-- not a base table column (default ignored)
	,@DisplayOrder             int               = null											-- not a base table column (default ignored)
	,@QueryCategoryIsActive    bit               = null											-- not a base table column (default ignored)
	,@QueryCategoryIsDefault   bit               = null											-- not a base table column (default ignored)
	,@QueryCategoryRowGUID     uniqueidentifier  = null											-- not a base table column (default ignored)
	,@IsDeleteEnabled          bit               = null											-- not a base table column (default ignored)
	,@ApplicationEntitySCD     varchar(50)       = null											-- not a base table column (default ignored)
	,@ApplicationEntityName    nvarchar(50)      = null											-- not a base table column (default ignored)
as
/*********************************************************************************************************************************
Procedure : sf.pQuery#EFInsert
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : Alternate call syntax for pQuery#Insert for use with MS Entity Framework (does not declare PK output parameter)
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : March 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is a wrapper for the standard insert procedure for the table. It is provided particularly for application using the
Microsoft Entity Framework (EF). The current version of the EF generates an error if an entity attribute is defined as an output
parameter. This procedure does not declare the primary key output parameter but passes all remaining parameters to the standard
insert procedure.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- call the main procedure

		exec @errorNo = sf.pQuery#Insert
			 @QueryCategorySID         = @QueryCategorySID
			,@ApplicationPageSID       = @ApplicationPageSID
			,@QueryLabel               = @QueryLabel
			,@ToolTip                  = @ToolTip
			,@LastExecuteTime          = @LastExecuteTime
			,@LastExecuteUser          = @LastExecuteUser
			,@ExecuteCount             = @ExecuteCount
			,@QuerySQL                 = @QuerySQL
			,@QueryParameters          = @QueryParameters
			,@QueryCode                = @QueryCode
			,@IsActive                 = @IsActive
			,@IsApplicationPageDefault = @IsApplicationPageDefault
			,@UserDefinedColumns       = @UserDefinedColumns
			,@QueryXID                 = @QueryXID
			,@LegacyKey                = @LegacyKey
			,@CreateUser               = @CreateUser
			,@IsReselected             = @IsReselected
			,@zContext                 = @zContext
			,@ApplicationPageLabel     = @ApplicationPageLabel
			,@ApplicationPageURI       = @ApplicationPageURI
			,@ApplicationRoute         = @ApplicationRoute
			,@IsSearchPage             = @IsSearchPage
			,@ApplicationEntitySID     = @ApplicationEntitySID
			,@ApplicationPageRowGUID   = @ApplicationPageRowGUID
			,@QueryCategoryLabel       = @QueryCategoryLabel
			,@QueryCategoryCode        = @QueryCategoryCode
			,@DisplayOrder             = @DisplayOrder
			,@QueryCategoryIsActive    = @QueryCategoryIsActive
			,@QueryCategoryIsDefault   = @QueryCategoryIsDefault
			,@QueryCategoryRowGUID     = @QueryCategoryRowGUID
			,@IsDeleteEnabled          = @IsDeleteEnabled
			,@ApplicationEntitySCD     = @ApplicationEntitySCD
			,@ApplicationEntityName    = @ApplicationEntityName

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
