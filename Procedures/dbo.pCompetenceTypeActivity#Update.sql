SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pCompetenceTypeActivity#Update]
	 @CompetenceTypeActivitySID  int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@CompetenceTypeSID          int               = null -- table column values to update:
	,@CompetenceActivitySID      int               = null
	,@EffectiveTime              datetime          = null
	,@ExpiryTime                 datetime          = null
	,@UserDefinedColumns         xml               = null
	,@CompetenceTypeActivityXID  varchar(150)      = null
	,@LegacyKey                  nvarchar(50)      = null
	,@UpdateUser                 nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                   timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected               tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied              bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                   xml               = null -- other values defining context for the update (if any)
	,@CompetenceActivityLabel    nvarchar(35)      = null -- not a base table column
	,@CompetenceActivityName     nvarchar(65)      = null -- not a base table column
	,@UnitValue                  decimal(5,2)      = null -- not a base table column
	,@CompetenceActivityIsActive bit               = null -- not a base table column
	,@CompetenceActivityRowGUID  uniqueidentifier  = null -- not a base table column
	,@CompetenceTypeLabel        nvarchar(35)      = null -- not a base table column
	,@CompetenceTypeCategory     nvarchar(65)      = null -- not a base table column
	,@CompetenceTypeIsDefault    bit               = null -- not a base table column
	,@CompetenceTypeIsActive     bit               = null -- not a base table column
	,@CompetenceTypeRowGUID      uniqueidentifier  = null -- not a base table column
	,@IsActive                   bit               = null -- not a base table column
	,@IsPending                  bit               = null -- not a base table column
	,@IsDeleteEnabled            bit               = null -- not a base table column
	,@CompetenceActivityXID      varchar(150)      = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : dbo.pCompetenceTypeActivity#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the dbo.CompetenceTypeActivity table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : March 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the dbo.CompetenceTypeActivity table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vCompetenceTypeActivity entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pCompetenceTypeActivity procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fCompetenceTypeActivityCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @CompetenceTypeActivitySID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@CompetenceTypeActivitySID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @CompetenceTypeActivityXID = ltrim(rtrim(@CompetenceTypeActivityXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @CompetenceActivityLabel = ltrim(rtrim(@CompetenceActivityLabel))
		set @CompetenceActivityName = ltrim(rtrim(@CompetenceActivityName))
		set @CompetenceTypeLabel = ltrim(rtrim(@CompetenceTypeLabel))
		set @CompetenceTypeCategory = ltrim(rtrim(@CompetenceTypeCategory))
		set @CompetenceActivityXID = ltrim(rtrim(@CompetenceActivityXID))

		-- set zero length strings to null to avoid storing them in the record

		if len(@CompetenceTypeActivityXID) = 0 set @CompetenceTypeActivityXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@CompetenceActivityLabel) = 0 set @CompetenceActivityLabel = null
		if len(@CompetenceActivityName) = 0 set @CompetenceActivityName = null
		if len(@CompetenceTypeLabel) = 0 set @CompetenceTypeLabel = null
		if len(@CompetenceTypeCategory) = 0 set @CompetenceTypeCategory = null
		if len(@CompetenceActivityXID) = 0 set @CompetenceActivityXID = null
		
		if @EffectiveTime is not null	set @EffectiveTime = cast(cast(@EffectiveTime as date) as datetime)																			-- ensure Effective value has start-of-day time component
		if @ExpiryTime is not null		set @ExpiryTime = cast(convert(varchar(8), cast(@ExpiryTime as date), 112) + ' 23:59:59.99' as datetime)-- ensure Expiry value has end-of-day time component

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)													-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()																-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @CompetenceTypeSID          = isnull(@CompetenceTypeSID,cta.CompetenceTypeSID)
				,@CompetenceActivitySID      = isnull(@CompetenceActivitySID,cta.CompetenceActivitySID)
				,@EffectiveTime              = isnull(@EffectiveTime,cta.EffectiveTime)
				,@ExpiryTime                 = isnull(@ExpiryTime,cta.ExpiryTime)
				,@UserDefinedColumns         = isnull(@UserDefinedColumns,cta.UserDefinedColumns)
				,@CompetenceTypeActivityXID  = isnull(@CompetenceTypeActivityXID,cta.CompetenceTypeActivityXID)
				,@LegacyKey                  = isnull(@LegacyKey,cta.LegacyKey)
				,@UpdateUser                 = isnull(@UpdateUser,cta.UpdateUser)
				,@IsReselected               = isnull(@IsReselected,cta.IsReselected)
				,@IsNullApplied              = isnull(@IsNullApplied,cta.IsNullApplied)
				,@zContext                   = isnull(@zContext,cta.zContext)
				,@CompetenceActivityLabel    = isnull(@CompetenceActivityLabel,cta.CompetenceActivityLabel)
				,@CompetenceActivityName     = isnull(@CompetenceActivityName,cta.CompetenceActivityName)
				,@UnitValue                  = isnull(@UnitValue,cta.UnitValue)
				,@CompetenceActivityIsActive = isnull(@CompetenceActivityIsActive,cta.CompetenceActivityIsActive)
				,@CompetenceActivityRowGUID  = isnull(@CompetenceActivityRowGUID,cta.CompetenceActivityRowGUID)
				,@CompetenceTypeLabel        = isnull(@CompetenceTypeLabel,cta.CompetenceTypeLabel)
				,@CompetenceTypeCategory     = isnull(@CompetenceTypeCategory,cta.CompetenceTypeCategory)
				,@CompetenceTypeIsDefault    = isnull(@CompetenceTypeIsDefault,cta.CompetenceTypeIsDefault)
				,@CompetenceTypeIsActive     = isnull(@CompetenceTypeIsActive,cta.CompetenceTypeIsActive)
				,@CompetenceTypeRowGUID      = isnull(@CompetenceTypeRowGUID,cta.CompetenceTypeRowGUID)
				,@IsActive                   = isnull(@IsActive,cta.IsActive)
				,@IsPending                  = isnull(@IsPending,cta.IsPending)
				,@IsDeleteEnabled            = isnull(@IsDeleteEnabled,cta.IsDeleteEnabled)
				,@CompetenceActivityXID      = isnull(@CompetenceActivityXID,cta.CompetenceActivityXID)
			from
				dbo.vCompetenceTypeActivity cta
			where
				cta.CompetenceTypeActivitySID = @CompetenceTypeActivitySID

		end

		-- block changing FK values to parent records which are inactive (UI should prevent this)
		
		if sf.fIsActive(@EffectiveTime, @ExpiryTime) = @ON
		begin
		
			if (select x.CompetenceActivitySID from dbo.CompetenceTypeActivity x where x.CompetenceTypeActivitySID = @CompetenceTypeActivitySID) <> @CompetenceActivitySID
			begin
			
				if (select x.IsActive from dbo.CompetenceActivity x where x.CompetenceActivitySID = @CompetenceActivitySID) = @OFF
				begin
					
					exec sf.pMessage#Get
						 @MessageSCD  = 'AssignmentToInactiveParent'
						,@MessageText = @errorText output
						,@DefaultText = N'The %1 assigned is marked inactive. Leave the record unchanged or set to an active %1.'
						,@Arg1        = N'competence activity'
					
					raiserror(@errorText, 16, 1)
					
				end
			end
		end
		
		if sf.fIsActive(@EffectiveTime, @ExpiryTime) = @ON
		begin
		
			if (select x.CompetenceTypeSID from dbo.CompetenceTypeActivity x where x.CompetenceTypeActivitySID = @CompetenceTypeActivitySID) <> @CompetenceTypeSID
			begin
			
				if (select x.IsActive from dbo.CompetenceType x where x.CompetenceTypeSID = @CompetenceTypeSID) = @OFF
				begin
					
					exec sf.pMessage#Get
						 @MessageSCD  = 'AssignmentToInactiveParent'
						,@MessageText = @errorText output
						,@DefaultText = N'The %1 assigned is marked inactive. Leave the record unchanged or set to an active %1.'
						,@Arg1        = N'competence type'
					
					raiserror(@errorText, 16, 1)
					
				end
			end
		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		--  insert pre-update logic here ...
		--! </PreUpdate>
	
		-- call the extended version of the procedure (if it exists) for "update.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pCompetenceTypeActivity'
		)
		begin
		
			exec @errorNo = ext.pCompetenceTypeActivity
				 @Mode                       = 'update.pre'
				,@CompetenceTypeActivitySID  = @CompetenceTypeActivitySID
				,@CompetenceTypeSID          = @CompetenceTypeSID output
				,@CompetenceActivitySID      = @CompetenceActivitySID output
				,@EffectiveTime              = @EffectiveTime output
				,@ExpiryTime                 = @ExpiryTime output
				,@UserDefinedColumns         = @UserDefinedColumns output
				,@CompetenceTypeActivityXID  = @CompetenceTypeActivityXID output
				,@LegacyKey                  = @LegacyKey output
				,@UpdateUser                 = @UpdateUser
				,@RowStamp                   = @RowStamp
				,@IsReselected               = @IsReselected
				,@IsNullApplied              = @IsNullApplied
				,@zContext                   = @zContext
				,@CompetenceActivityLabel    = @CompetenceActivityLabel
				,@CompetenceActivityName     = @CompetenceActivityName
				,@UnitValue                  = @UnitValue
				,@CompetenceActivityIsActive = @CompetenceActivityIsActive
				,@CompetenceActivityRowGUID  = @CompetenceActivityRowGUID
				,@CompetenceTypeLabel        = @CompetenceTypeLabel
				,@CompetenceTypeCategory     = @CompetenceTypeCategory
				,@CompetenceTypeIsDefault    = @CompetenceTypeIsDefault
				,@CompetenceTypeIsActive     = @CompetenceTypeIsActive
				,@CompetenceTypeRowGUID      = @CompetenceTypeRowGUID
				,@IsActive                   = @IsActive
				,@IsPending                  = @IsPending
				,@IsDeleteEnabled            = @IsDeleteEnabled
				,@CompetenceActivityXID      = @CompetenceActivityXID
		
		end

		-- update the record

		update
			dbo.CompetenceTypeActivity
		set
			 CompetenceTypeSID = @CompetenceTypeSID
			,CompetenceActivitySID = @CompetenceActivitySID
			,EffectiveTime = @EffectiveTime
			,ExpiryTime = @ExpiryTime
			,UserDefinedColumns = @UserDefinedColumns
			,CompetenceTypeActivityXID = @CompetenceTypeActivityXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			CompetenceTypeActivitySID = @CompetenceTypeActivitySID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from dbo.CompetenceTypeActivity where CompetenceTypeActivitySID = @competenceTypeActivitySID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'dbo.CompetenceTypeActivity'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'dbo.CompetenceTypeActivity'
					,@Arg2        = @competenceTypeActivitySID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'dbo.CompetenceTypeActivity'
				,@Arg3        = @rowsAffected
				,@Arg4        = @competenceTypeActivitySID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>
	
		-- call the extended version of the procedure for update.post - if it exists
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pCompetenceTypeActivity'
		)
		begin
		
			exec @errorNo = ext.pCompetenceTypeActivity
				 @Mode                       = 'update.post'
				,@CompetenceTypeActivitySID  = @CompetenceTypeActivitySID
				,@CompetenceTypeSID          = @CompetenceTypeSID
				,@CompetenceActivitySID      = @CompetenceActivitySID
				,@EffectiveTime              = @EffectiveTime
				,@ExpiryTime                 = @ExpiryTime
				,@UserDefinedColumns         = @UserDefinedColumns
				,@CompetenceTypeActivityXID  = @CompetenceTypeActivityXID
				,@LegacyKey                  = @LegacyKey
				,@UpdateUser                 = @UpdateUser
				,@RowStamp                   = @RowStamp
				,@IsReselected               = @IsReselected
				,@IsNullApplied              = @IsNullApplied
				,@zContext                   = @zContext
				,@CompetenceActivityLabel    = @CompetenceActivityLabel
				,@CompetenceActivityName     = @CompetenceActivityName
				,@UnitValue                  = @UnitValue
				,@CompetenceActivityIsActive = @CompetenceActivityIsActive
				,@CompetenceActivityRowGUID  = @CompetenceActivityRowGUID
				,@CompetenceTypeLabel        = @CompetenceTypeLabel
				,@CompetenceTypeCategory     = @CompetenceTypeCategory
				,@CompetenceTypeIsDefault    = @CompetenceTypeIsDefault
				,@CompetenceTypeIsActive     = @CompetenceTypeIsActive
				,@CompetenceTypeRowGUID      = @CompetenceTypeRowGUID
				,@IsActive                   = @IsActive
				,@IsPending                  = @IsPending
				,@IsDeleteEnabled            = @IsDeleteEnabled
				,@CompetenceActivityXID      = @CompetenceActivityXID
		
		end

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.CompetenceTypeActivitySID
			from
				dbo.vCompetenceTypeActivity ent
			where
				ent.CompetenceTypeActivitySID = @CompetenceTypeActivitySID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.CompetenceTypeActivitySID
				,ent.CompetenceTypeSID
				,ent.CompetenceActivitySID
				,ent.EffectiveTime
				,ent.ExpiryTime
				,ent.UserDefinedColumns
				,ent.CompetenceTypeActivityXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.CompetenceActivityLabel
				,ent.CompetenceActivityName
				,ent.UnitValue
				,ent.CompetenceActivityIsActive
				,ent.CompetenceActivityRowGUID
				,ent.CompetenceTypeLabel
				,ent.CompetenceTypeCategory
				,ent.CompetenceTypeIsDefault
				,ent.CompetenceTypeIsActive
				,ent.CompetenceTypeRowGUID
				,ent.IsActive
				,ent.IsPending
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
				,ent.CompetenceActivityXID
			from
				dbo.vCompetenceTypeActivity ent
			where
				ent.CompetenceTypeActivitySID = @CompetenceTypeActivitySID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
