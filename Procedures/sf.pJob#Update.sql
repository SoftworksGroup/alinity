SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [sf].[pJob#Update]
	 @JobSID                     int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@JobSCD                     varchar(132)      = null -- table column values to update:
	,@JobLabel                   nvarchar(35)      = null
	,@JobDescription             nvarchar(max)     = null
	,@CallSyntaxTemplate         nvarchar(max)     = null
	,@IsCancelEnabled            bit               = null
	,@IsParallelEnabled          bit               = null
	,@IsFullTraceEnabled         bit               = null
	,@IsAlertOnSuccessEnabled    bit               = null
	,@JobScheduleSID             int               = null
	,@JobScheduleSequence        int               = null
	,@IsRunAfterPredecessorsOnly bit               = null
	,@MaxErrorRate               int               = null
	,@MaxRetriesOnFailure        tinyint           = null
	,@TraceLog                   nvarchar(max)     = null
	,@IsActive                   bit               = null
	,@UserDefinedColumns         xml               = null
	,@JobXID                     varchar(150)      = null
	,@LegacyKey                  nvarchar(50)      = null
	,@UpdateUser                 nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                   timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected               tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied              bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                   xml               = null -- other values defining context for the update (if any)
	,@JobScheduleLabel           nvarchar(35)      = null -- not a base table column
	,@IsEnabled                  bit               = null -- not a base table column
	,@IsRunMonday                bit               = null -- not a base table column
	,@IsRunTuesday               bit               = null -- not a base table column
	,@IsRunWednesday             bit               = null -- not a base table column
	,@IsRunThursday              bit               = null -- not a base table column
	,@IsRunFriday                bit               = null -- not a base table column
	,@IsRunSaturday              bit               = null -- not a base table column
	,@IsRunSunday                bit               = null -- not a base table column
	,@RepeatIntervalMinutes      smallint          = null -- not a base table column
	,@StartTime                  time(0)           = null -- not a base table column
	,@EndTime                    time(0)           = null -- not a base table column
	,@StartDate                  date              = null -- not a base table column
	,@EndDate                    date              = null -- not a base table column
	,@JobScheduleRowGUID         uniqueidentifier  = null -- not a base table column
	,@IsDeleteEnabled            bit               = null -- not a base table column
	,@LastJobStatusSCD           varchar(35)       = null -- not a base table column
	,@LastJobStatusLabel         nvarchar(35)      = null -- not a base table column
	,@LastStartTime              datetime          = null -- not a base table column
	,@LastEndTime                datetime          = null -- not a base table column
	,@NextScheduledTime          datetime          = null -- not a base table column
	,@NextScheduledTimeServerTZ  datetimeoffset(7) = null -- not a base table column
	,@MinDuration                int               = null -- not a base table column
	,@MaxDuration                int               = null -- not a base table column
	,@AvgDuration                int               = null -- not a base table column
	,@IsTaskTriggerJob           bit               = null -- not a base table column
	,@LastRunRecords             int               = null -- not a base table column
	,@LastRunProcessed           int               = null -- not a base table column
	,@LastRunErrors              int               = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : sf.pJob#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the sf.Job table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : March 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the sf.Job table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vJob entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pJob procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fJobCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @JobSID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@JobSID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @JobSCD = ltrim(rtrim(@JobSCD))
		set @JobLabel = ltrim(rtrim(@JobLabel))
		set @JobDescription = ltrim(rtrim(@JobDescription))
		set @CallSyntaxTemplate = ltrim(rtrim(@CallSyntaxTemplate))
		set @TraceLog = ltrim(rtrim(@TraceLog))
		set @JobXID = ltrim(rtrim(@JobXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @JobScheduleLabel = ltrim(rtrim(@JobScheduleLabel))
		set @LastJobStatusSCD = ltrim(rtrim(@LastJobStatusSCD))
		set @LastJobStatusLabel = ltrim(rtrim(@LastJobStatusLabel))

		-- set zero length strings to null to avoid storing them in the record

		if len(@JobSCD) = 0 set @JobSCD = null
		if len(@JobLabel) = 0 set @JobLabel = null
		if len(@JobDescription) = 0 set @JobDescription = null
		if len(@CallSyntaxTemplate) = 0 set @CallSyntaxTemplate = null
		if len(@TraceLog) = 0 set @TraceLog = null
		if len(@JobXID) = 0 set @JobXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@JobScheduleLabel) = 0 set @JobScheduleLabel = null
		if len(@LastJobStatusSCD) = 0 set @LastJobStatusSCD = null
		if len(@LastJobStatusLabel) = 0 set @LastJobStatusLabel = null

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @JobSCD                     = isnull(@JobSCD,jb.JobSCD)
				,@JobLabel                   = isnull(@JobLabel,jb.JobLabel)
				,@JobDescription             = isnull(@JobDescription,jb.JobDescription)
				,@CallSyntaxTemplate         = isnull(@CallSyntaxTemplate,jb.CallSyntaxTemplate)
				,@IsCancelEnabled            = isnull(@IsCancelEnabled,jb.IsCancelEnabled)
				,@IsParallelEnabled          = isnull(@IsParallelEnabled,jb.IsParallelEnabled)
				,@IsFullTraceEnabled         = isnull(@IsFullTraceEnabled,jb.IsFullTraceEnabled)
				,@IsAlertOnSuccessEnabled    = isnull(@IsAlertOnSuccessEnabled,jb.IsAlertOnSuccessEnabled)
				,@JobScheduleSID             = isnull(@JobScheduleSID,jb.JobScheduleSID)
				,@JobScheduleSequence        = isnull(@JobScheduleSequence,jb.JobScheduleSequence)
				,@IsRunAfterPredecessorsOnly = isnull(@IsRunAfterPredecessorsOnly,jb.IsRunAfterPredecessorsOnly)
				,@MaxErrorRate               = isnull(@MaxErrorRate,jb.MaxErrorRate)
				,@MaxRetriesOnFailure        = isnull(@MaxRetriesOnFailure,jb.MaxRetriesOnFailure)
				,@TraceLog                   = isnull(@TraceLog,jb.TraceLog)
				,@IsActive                   = isnull(@IsActive,jb.IsActive)
				,@UserDefinedColumns         = isnull(@UserDefinedColumns,jb.UserDefinedColumns)
				,@JobXID                     = isnull(@JobXID,jb.JobXID)
				,@LegacyKey                  = isnull(@LegacyKey,jb.LegacyKey)
				,@UpdateUser                 = isnull(@UpdateUser,jb.UpdateUser)
				,@IsReselected               = isnull(@IsReselected,jb.IsReselected)
				,@IsNullApplied              = isnull(@IsNullApplied,jb.IsNullApplied)
				,@zContext                   = isnull(@zContext,jb.zContext)
				,@JobScheduleLabel           = isnull(@JobScheduleLabel,jb.JobScheduleLabel)
				,@IsEnabled                  = isnull(@IsEnabled,jb.IsEnabled)
				,@IsRunMonday                = isnull(@IsRunMonday,jb.IsRunMonday)
				,@IsRunTuesday               = isnull(@IsRunTuesday,jb.IsRunTuesday)
				,@IsRunWednesday             = isnull(@IsRunWednesday,jb.IsRunWednesday)
				,@IsRunThursday              = isnull(@IsRunThursday,jb.IsRunThursday)
				,@IsRunFriday                = isnull(@IsRunFriday,jb.IsRunFriday)
				,@IsRunSaturday              = isnull(@IsRunSaturday,jb.IsRunSaturday)
				,@IsRunSunday                = isnull(@IsRunSunday,jb.IsRunSunday)
				,@RepeatIntervalMinutes      = isnull(@RepeatIntervalMinutes,jb.RepeatIntervalMinutes)
				,@StartTime                  = isnull(@StartTime,jb.StartTime)
				,@EndTime                    = isnull(@EndTime,jb.EndTime)
				,@StartDate                  = isnull(@StartDate,jb.StartDate)
				,@EndDate                    = isnull(@EndDate,jb.EndDate)
				,@JobScheduleRowGUID         = isnull(@JobScheduleRowGUID,jb.JobScheduleRowGUID)
				,@IsDeleteEnabled            = isnull(@IsDeleteEnabled,jb.IsDeleteEnabled)
				,@LastJobStatusSCD           = isnull(@LastJobStatusSCD,jb.LastJobStatusSCD)
				,@LastJobStatusLabel         = isnull(@LastJobStatusLabel,jb.LastJobStatusLabel)
				,@LastStartTime              = isnull(@LastStartTime,jb.LastStartTime)
				,@LastEndTime                = isnull(@LastEndTime,jb.LastEndTime)
				,@NextScheduledTime          = isnull(@NextScheduledTime,jb.NextScheduledTime)
				,@NextScheduledTimeServerTZ  = isnull(@NextScheduledTimeServerTZ,jb.NextScheduledTimeServerTZ)
				,@MinDuration                = isnull(@MinDuration,jb.MinDuration)
				,@MaxDuration                = isnull(@MaxDuration,jb.MaxDuration)
				,@AvgDuration                = isnull(@AvgDuration,jb.AvgDuration)
				,@IsTaskTriggerJob           = isnull(@IsTaskTriggerJob,jb.IsTaskTriggerJob)
				,@LastRunRecords             = isnull(@LastRunRecords,jb.LastRunRecords)
				,@LastRunProcessed           = isnull(@LastRunProcessed,jb.LastRunProcessed)
				,@LastRunErrors              = isnull(@LastRunErrors,jb.LastRunErrors)
			from
				sf.vJob jb
			where
				jb.JobSID = @JobSID

		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		--  insert pre-update logic here ...
		--! </PreUpdate>

		-- update the record

		update
			sf.Job
		set
			 JobSCD = @JobSCD
			,JobLabel = @JobLabel
			,JobDescription = @JobDescription
			,CallSyntaxTemplate = @CallSyntaxTemplate
			,IsCancelEnabled = @IsCancelEnabled
			,IsParallelEnabled = @IsParallelEnabled
			,IsFullTraceEnabled = @IsFullTraceEnabled
			,IsAlertOnSuccessEnabled = @IsAlertOnSuccessEnabled
			,JobScheduleSID = @JobScheduleSID
			,JobScheduleSequence = @JobScheduleSequence
			,IsRunAfterPredecessorsOnly = @IsRunAfterPredecessorsOnly
			,MaxErrorRate = @MaxErrorRate
			,MaxRetriesOnFailure = @MaxRetriesOnFailure
			,TraceLog = @TraceLog
			,IsActive = @IsActive
			,UserDefinedColumns = @UserDefinedColumns
			,JobXID = @JobXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			JobSID = @JobSID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from sf.Job where JobSID = @jobSID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'sf.Job'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'sf.Job'
					,@Arg2        = @jobSID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'sf.Job'
				,@Arg3        = @rowsAffected
				,@Arg4        = @jobSID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.JobSID
			from
				sf.vJob ent
			where
				ent.JobSID = @JobSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.JobSID
				,ent.JobSCD
				,ent.JobLabel
				,ent.JobDescription
				,ent.CallSyntaxTemplate
				,ent.IsCancelEnabled
				,ent.IsParallelEnabled
				,ent.IsFullTraceEnabled
				,ent.IsAlertOnSuccessEnabled
				,ent.JobScheduleSID
				,ent.JobScheduleSequence
				,ent.IsRunAfterPredecessorsOnly
				,ent.MaxErrorRate
				,ent.MaxRetriesOnFailure
				,ent.TraceLog
				,ent.IsActive
				,ent.UserDefinedColumns
				,ent.JobXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.JobScheduleLabel
				,ent.IsEnabled
				,ent.IsRunMonday
				,ent.IsRunTuesday
				,ent.IsRunWednesday
				,ent.IsRunThursday
				,ent.IsRunFriday
				,ent.IsRunSaturday
				,ent.IsRunSunday
				,ent.RepeatIntervalMinutes
				,ent.StartTime
				,ent.EndTime
				,ent.StartDate
				,ent.EndDate
				,ent.JobScheduleRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
				,ent.LastJobStatusSCD
				,ent.LastJobStatusLabel
				,ent.LastStartTime
				,ent.LastEndTime
				,ent.NextScheduledTime
				,ent.NextScheduledTimeServerTZ
				,ent.MinDuration
				,ent.MaxDuration
				,ent.AvgDuration
				,ent.IsTaskTriggerJob
				,ent.LastRunRecords
				,ent.LastRunProcessed
				,ent.LastRunErrors
			from
				sf.vJob ent
			where
				ent.JobSID = @JobSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
