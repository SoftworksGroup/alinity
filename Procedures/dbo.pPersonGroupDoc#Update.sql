SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pPersonGroupDoc#Update]
	 @PersonGroupDocSID          int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@PersonGroupFolderSID       int               = null -- table column values to update:
	,@DocumentTitle              nvarchar(75)      = null
	,@FileTypeSID                int               = null
	,@FileTypeSCD                varchar(8)        = null
	,@TagList                    xml               = null
	,@DocumentContent            varbinary(max)    = null
	,@DocumentNotes              nvarchar(max)     = null
	,@UserDefinedColumns         xml               = null
	,@PersonGroupDocXID          varchar(150)      = null
	,@LegacyKey                  nvarchar(50)      = null
	,@UpdateUser                 nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                   timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected               tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied              bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                   xml               = null -- other values defining context for the update (if any)
	,@ParentPersonGroupFolderSID int               = null -- not a base table column
	,@PersonGroupSID             int               = null -- not a base table column
	,@FolderName                 nvarchar(35)      = null -- not a base table column
	,@IsRoot                     bit               = null -- not a base table column
	,@PersonGroupFolderRowGUID   uniqueidentifier  = null -- not a base table column
	,@FileTypeFileTypeSCD        varchar(8)        = null -- not a base table column
	,@FileTypeLabel              nvarchar(35)      = null -- not a base table column
	,@MimeType                   varchar(255)      = null -- not a base table column
	,@IsInline                   bit               = null -- not a base table column
	,@FileTypeIsActive           bit               = null -- not a base table column
	,@FileTypeRowGUID            uniqueidentifier  = null -- not a base table column
	,@IsDeleteEnabled            bit               = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : dbo.pPersonGroupDoc#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the dbo.PersonGroupDoc table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the dbo.PersonGroupDoc table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vPersonGroupDoc entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pPersonGroupDoc procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fPersonGroupDocCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @PersonGroupDocSID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@PersonGroupDocSID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @DocumentTitle = ltrim(rtrim(@DocumentTitle))
		set @FileTypeSCD = ltrim(rtrim(@FileTypeSCD))
		set @DocumentNotes = ltrim(rtrim(@DocumentNotes))
		set @PersonGroupDocXID = ltrim(rtrim(@PersonGroupDocXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @FolderName = ltrim(rtrim(@FolderName))
		set @FileTypeFileTypeSCD = ltrim(rtrim(@FileTypeFileTypeSCD))
		set @FileTypeLabel = ltrim(rtrim(@FileTypeLabel))
		set @MimeType = ltrim(rtrim(@MimeType))

		-- set zero length strings to null to avoid storing them in the record

		if len(@DocumentTitle) = 0 set @DocumentTitle = null
		if len(@FileTypeSCD) = 0 set @FileTypeSCD = null
		if len(@DocumentNotes) = 0 set @DocumentNotes = null
		if len(@PersonGroupDocXID) = 0 set @PersonGroupDocXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@FolderName) = 0 set @FolderName = null
		if len(@FileTypeFileTypeSCD) = 0 set @FileTypeFileTypeSCD = null
		if len(@FileTypeLabel) = 0 set @FileTypeLabel = null
		if len(@MimeType) = 0 set @MimeType = null

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @PersonGroupFolderSID       = isnull(@PersonGroupFolderSID,pgd.PersonGroupFolderSID)
				,@DocumentTitle              = isnull(@DocumentTitle,pgd.DocumentTitle)
				,@FileTypeSID                = isnull(@FileTypeSID,pgd.FileTypeSID)
				,@FileTypeSCD                = isnull(@FileTypeSCD,pgd.FileTypeSCD)
				,@TagList                    = isnull(@TagList,pgd.TagList)
				,@DocumentContent            = isnull(@DocumentContent,pgd.DocumentContent)
				,@DocumentNotes              = isnull(@DocumentNotes,pgd.DocumentNotes)
				,@UserDefinedColumns         = isnull(@UserDefinedColumns,pgd.UserDefinedColumns)
				,@PersonGroupDocXID          = isnull(@PersonGroupDocXID,pgd.PersonGroupDocXID)
				,@LegacyKey                  = isnull(@LegacyKey,pgd.LegacyKey)
				,@UpdateUser                 = isnull(@UpdateUser,pgd.UpdateUser)
				,@IsReselected               = isnull(@IsReselected,pgd.IsReselected)
				,@IsNullApplied              = isnull(@IsNullApplied,pgd.IsNullApplied)
				,@zContext                   = isnull(@zContext,pgd.zContext)
				,@ParentPersonGroupFolderSID = isnull(@ParentPersonGroupFolderSID,pgd.ParentPersonGroupFolderSID)
				,@PersonGroupSID             = isnull(@PersonGroupSID,pgd.PersonGroupSID)
				,@FolderName                 = isnull(@FolderName,pgd.FolderName)
				,@IsRoot                     = isnull(@IsRoot,pgd.IsRoot)
				,@PersonGroupFolderRowGUID   = isnull(@PersonGroupFolderRowGUID,pgd.PersonGroupFolderRowGUID)
				,@FileTypeFileTypeSCD        = isnull(@FileTypeFileTypeSCD,pgd.FileTypeFileTypeSCD)
				,@FileTypeLabel              = isnull(@FileTypeLabel,pgd.FileTypeLabel)
				,@MimeType                   = isnull(@MimeType,pgd.MimeType)
				,@IsInline                   = isnull(@IsInline,pgd.IsInline)
				,@FileTypeIsActive           = isnull(@FileTypeIsActive,pgd.FileTypeIsActive)
				,@FileTypeRowGUID            = isnull(@FileTypeRowGUID,pgd.FileTypeRowGUID)
				,@IsDeleteEnabled            = isnull(@IsDeleteEnabled,pgd.IsDeleteEnabled)
			from
				dbo.vPersonGroupDoc pgd
			where
				pgd.PersonGroupDocSID = @PersonGroupDocSID

		end
		
		-- ensure the file type SID value matches the code being set; a trigger
		-- enforces the rule for updates outside of the EF sprocs
		
		select
			@FileTypeSID = ft.FileTypeSID
		from
			sf.FileType ft
		where
			ft.FileTypeSCD = @FileTypeSCD
		and
			ft.IsActive = @ON
		
		if @@rowcount = 0
		begin
		
			exec sf.pMessage#Get
				@MessageSCD  = 'FileTypeNotFound'
				,@MessageText = @errorText output
				,@DefaultText = N'The file type "%1" is not supported. Upload a different file or ask your administrator if this type can be added to the configuration.'
				,@Arg1        = @FileTypeSCD
		
			raiserror(@errorText, 16, 1)
		end
		
		set @TagList = sf.fTagList#SetTagTimes(@TagList)											-- add times to the new tags applied (if any)

		-- block changing FK values to parent records which are inactive (UI should prevent this)
		
		if (select x.FileTypeSID from dbo.PersonGroupDoc x where x.PersonGroupDocSID = @PersonGroupDocSID) <> @FileTypeSID
		begin
			if (select x.IsActive from sf.FileType x where x.FileTypeSID = @FileTypeSID) = @OFF
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'AssignmentToInactiveParent'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 assigned is marked inactive. Leave the record unchanged or set to an active %1.'
					,@Arg1        = N'file type'
				
				raiserror(@errorText, 16, 1)
				
			end
		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		--  insert pre-update logic here ...
		--! </PreUpdate>
	
		-- call the extended version of the procedure (if it exists) for "update.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pPersonGroupDoc'
		)
		begin
		
			exec @errorNo = ext.pPersonGroupDoc
				 @Mode                       = 'update.pre'
				,@PersonGroupDocSID          = @PersonGroupDocSID
				,@PersonGroupFolderSID       = @PersonGroupFolderSID output
				,@DocumentTitle              = @DocumentTitle output
				,@FileTypeSID                = @FileTypeSID output
				,@FileTypeSCD                = @FileTypeSCD output
				,@TagList                    = @TagList output
				,@DocumentContent            = @DocumentContent output
				,@DocumentNotes              = @DocumentNotes output
				,@UserDefinedColumns         = @UserDefinedColumns output
				,@PersonGroupDocXID          = @PersonGroupDocXID output
				,@LegacyKey                  = @LegacyKey output
				,@UpdateUser                 = @UpdateUser
				,@RowStamp                   = @RowStamp
				,@IsReselected               = @IsReselected
				,@IsNullApplied              = @IsNullApplied
				,@zContext                   = @zContext
				,@ParentPersonGroupFolderSID = @ParentPersonGroupFolderSID
				,@PersonGroupSID             = @PersonGroupSID
				,@FolderName                 = @FolderName
				,@IsRoot                     = @IsRoot
				,@PersonGroupFolderRowGUID   = @PersonGroupFolderRowGUID
				,@FileTypeFileTypeSCD        = @FileTypeFileTypeSCD
				,@FileTypeLabel              = @FileTypeLabel
				,@MimeType                   = @MimeType
				,@IsInline                   = @IsInline
				,@FileTypeIsActive           = @FileTypeIsActive
				,@FileTypeRowGUID            = @FileTypeRowGUID
				,@IsDeleteEnabled            = @IsDeleteEnabled
		
		end

		-- update the record

		update
			dbo.PersonGroupDoc
		set
			 PersonGroupFolderSID = @PersonGroupFolderSID
			,DocumentTitle = @DocumentTitle
			,FileTypeSID = @FileTypeSID
			,FileTypeSCD = @FileTypeSCD
			,TagList = @TagList
			,DocumentContent = @DocumentContent
			,DocumentNotes = @DocumentNotes
			,UserDefinedColumns = @UserDefinedColumns
			,PersonGroupDocXID = @PersonGroupDocXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			PersonGroupDocSID = @PersonGroupDocSID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from dbo.PersonGroupDoc where PersonGroupDocSID = @personGroupDocSID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'dbo.PersonGroupDoc'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'dbo.PersonGroupDoc'
					,@Arg2        = @personGroupDocSID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'dbo.PersonGroupDoc'
				,@Arg3        = @rowsAffected
				,@Arg4        = @personGroupDocSID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>
	
		-- call the extended version of the procedure for update.post - if it exists
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pPersonGroupDoc'
		)
		begin
		
			exec @errorNo = ext.pPersonGroupDoc
				 @Mode                       = 'update.post'
				,@PersonGroupDocSID          = @PersonGroupDocSID
				,@PersonGroupFolderSID       = @PersonGroupFolderSID
				,@DocumentTitle              = @DocumentTitle
				,@FileTypeSID                = @FileTypeSID
				,@FileTypeSCD                = @FileTypeSCD
				,@TagList                    = @TagList
				,@DocumentContent            = @DocumentContent
				,@DocumentNotes              = @DocumentNotes
				,@UserDefinedColumns         = @UserDefinedColumns
				,@PersonGroupDocXID          = @PersonGroupDocXID
				,@LegacyKey                  = @LegacyKey
				,@UpdateUser                 = @UpdateUser
				,@RowStamp                   = @RowStamp
				,@IsReselected               = @IsReselected
				,@IsNullApplied              = @IsNullApplied
				,@zContext                   = @zContext
				,@ParentPersonGroupFolderSID = @ParentPersonGroupFolderSID
				,@PersonGroupSID             = @PersonGroupSID
				,@FolderName                 = @FolderName
				,@IsRoot                     = @IsRoot
				,@PersonGroupFolderRowGUID   = @PersonGroupFolderRowGUID
				,@FileTypeFileTypeSCD        = @FileTypeFileTypeSCD
				,@FileTypeLabel              = @FileTypeLabel
				,@MimeType                   = @MimeType
				,@IsInline                   = @IsInline
				,@FileTypeIsActive           = @FileTypeIsActive
				,@FileTypeRowGUID            = @FileTypeRowGUID
				,@IsDeleteEnabled            = @IsDeleteEnabled
		
		end

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.PersonGroupDocSID
			from
				dbo.vPersonGroupDoc ent
			where
				ent.PersonGroupDocSID = @PersonGroupDocSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.PersonGroupDocSID
				,ent.PersonGroupFolderSID
				,ent.DocumentTitle
				,ent.FileTypeSID
				,ent.FileTypeSCD
				,ent.TagList
				,ent.DocumentContent
				,ent.DocumentNotes
				,ent.UserDefinedColumns
				,ent.PersonGroupDocXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.ParentPersonGroupFolderSID
				,ent.PersonGroupSID
				,ent.FolderName
				,ent.IsRoot
				,ent.PersonGroupFolderRowGUID
				,ent.FileTypeFileTypeSCD
				,ent.FileTypeLabel
				,ent.MimeType
				,ent.IsInline
				,ent.FileTypeIsActive
				,ent.FileTypeRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
			from
				dbo.vPersonGroupDoc ent
			where
				ent.PersonGroupDocSID = @PersonGroupDocSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
