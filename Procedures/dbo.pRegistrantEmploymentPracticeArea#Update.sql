SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pRegistrantEmploymentPracticeArea#Update]
	 @RegistrantEmploymentPracticeAreaSID int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@RegistrantEmploymentSID             int               = null -- table column values to update:
	,@PracticeAreaSID                     int               = null
	,@IsPrimary                           bit               = null
	,@UserDefinedColumns                  xml               = null
	,@RegistrantEmploymentPracticeAreaXID varchar(150)      = null
	,@LegacyKey                           nvarchar(50)      = null
	,@UpdateUser                          nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                            timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected                        tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied                       bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                            xml               = null -- other values defining context for the update (if any)
	,@PracticeAreaName                    nvarchar(50)      = null -- not a base table column
	,@PracticeAreaCode                    varchar(20)       = null -- not a base table column
	,@PracticeAreaCategory                nvarchar(65)      = null -- not a base table column
	,@IsPracticeScopeRequired             bit               = null -- not a base table column
	,@PracticeAreaIsDefault               bit               = null -- not a base table column
	,@PracticeAreaIsActive                bit               = null -- not a base table column
	,@PracticeAreaRowGUID                 uniqueidentifier  = null -- not a base table column
	,@RegistrantSID                       int               = null -- not a base table column
	,@OrgSID                              int               = null -- not a base table column
	,@RegistrationYear                    smallint          = null -- not a base table column
	,@EmploymentTypeSID                   int               = null -- not a base table column
	,@EmploymentRoleSID                   int               = null -- not a base table column
	,@PracticeHours                       int               = null -- not a base table column
	,@PracticeScopeSID                    int               = null -- not a base table column
	,@AgeRangeSID                         int               = null -- not a base table column
	,@IsOnPublicRegistry                  bit               = null -- not a base table column
	,@Phone                               varchar(25)       = null -- not a base table column
	,@SiteLocation                        nvarchar(50)      = null -- not a base table column
	,@EffectiveTime                       datetime          = null -- not a base table column
	,@ExpiryTime                          datetime          = null -- not a base table column
	,@Rank                                smallint          = null -- not a base table column
	,@OwnershipPercentage                 smallint          = null -- not a base table column
	,@IsEmployerInsurance                 bit               = null -- not a base table column
	,@InsuranceOrgSID                     int               = null -- not a base table column
	,@InsurancePolicyNo                   varchar(25)       = null -- not a base table column
	,@InsuranceAmount                     decimal(11,2)     = null -- not a base table column
	,@RegistrantEmploymentRowGUID         uniqueidentifier  = null -- not a base table column
	,@IsDeleteEnabled                     bit               = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : dbo.pRegistrantEmploymentPracticeArea#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the dbo.RegistrantEmploymentPracticeArea table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the dbo.RegistrantEmploymentPracticeArea table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vRegistrantEmploymentPracticeArea entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pRegistrantEmploymentPracticeArea procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fRegistrantEmploymentPracticeAreaCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @RegistrantEmploymentPracticeAreaSID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@RegistrantEmploymentPracticeAreaSID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @RegistrantEmploymentPracticeAreaXID = ltrim(rtrim(@RegistrantEmploymentPracticeAreaXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @PracticeAreaName = ltrim(rtrim(@PracticeAreaName))
		set @PracticeAreaCode = ltrim(rtrim(@PracticeAreaCode))
		set @PracticeAreaCategory = ltrim(rtrim(@PracticeAreaCategory))
		set @Phone = ltrim(rtrim(@Phone))
		set @SiteLocation = ltrim(rtrim(@SiteLocation))
		set @InsurancePolicyNo = ltrim(rtrim(@InsurancePolicyNo))

		-- set zero length strings to null to avoid storing them in the record

		if len(@RegistrantEmploymentPracticeAreaXID) = 0 set @RegistrantEmploymentPracticeAreaXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@PracticeAreaName) = 0 set @PracticeAreaName = null
		if len(@PracticeAreaCode) = 0 set @PracticeAreaCode = null
		if len(@PracticeAreaCategory) = 0 set @PracticeAreaCategory = null
		if len(@Phone) = 0 set @Phone = null
		if len(@SiteLocation) = 0 set @SiteLocation = null
		if len(@InsurancePolicyNo) = 0 set @InsurancePolicyNo = null

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @RegistrantEmploymentSID             = isnull(@RegistrantEmploymentSID,repa.RegistrantEmploymentSID)
				,@PracticeAreaSID                     = isnull(@PracticeAreaSID,repa.PracticeAreaSID)
				,@IsPrimary                           = isnull(@IsPrimary,repa.IsPrimary)
				,@UserDefinedColumns                  = isnull(@UserDefinedColumns,repa.UserDefinedColumns)
				,@RegistrantEmploymentPracticeAreaXID = isnull(@RegistrantEmploymentPracticeAreaXID,repa.RegistrantEmploymentPracticeAreaXID)
				,@LegacyKey                           = isnull(@LegacyKey,repa.LegacyKey)
				,@UpdateUser                          = isnull(@UpdateUser,repa.UpdateUser)
				,@IsReselected                        = isnull(@IsReselected,repa.IsReselected)
				,@IsNullApplied                       = isnull(@IsNullApplied,repa.IsNullApplied)
				,@zContext                            = isnull(@zContext,repa.zContext)
				,@PracticeAreaName                    = isnull(@PracticeAreaName,repa.PracticeAreaName)
				,@PracticeAreaCode                    = isnull(@PracticeAreaCode,repa.PracticeAreaCode)
				,@PracticeAreaCategory                = isnull(@PracticeAreaCategory,repa.PracticeAreaCategory)
				,@IsPracticeScopeRequired             = isnull(@IsPracticeScopeRequired,repa.IsPracticeScopeRequired)
				,@PracticeAreaIsDefault               = isnull(@PracticeAreaIsDefault,repa.PracticeAreaIsDefault)
				,@PracticeAreaIsActive                = isnull(@PracticeAreaIsActive,repa.PracticeAreaIsActive)
				,@PracticeAreaRowGUID                 = isnull(@PracticeAreaRowGUID,repa.PracticeAreaRowGUID)
				,@RegistrantSID                       = isnull(@RegistrantSID,repa.RegistrantSID)
				,@OrgSID                              = isnull(@OrgSID,repa.OrgSID)
				,@RegistrationYear                    = isnull(@RegistrationYear,repa.RegistrationYear)
				,@EmploymentTypeSID                   = isnull(@EmploymentTypeSID,repa.EmploymentTypeSID)
				,@EmploymentRoleSID                   = isnull(@EmploymentRoleSID,repa.EmploymentRoleSID)
				,@PracticeHours                       = isnull(@PracticeHours,repa.PracticeHours)
				,@PracticeScopeSID                    = isnull(@PracticeScopeSID,repa.PracticeScopeSID)
				,@AgeRangeSID                         = isnull(@AgeRangeSID,repa.AgeRangeSID)
				,@IsOnPublicRegistry                  = isnull(@IsOnPublicRegistry,repa.IsOnPublicRegistry)
				,@Phone                               = isnull(@Phone,repa.Phone)
				,@SiteLocation                        = isnull(@SiteLocation,repa.SiteLocation)
				,@EffectiveTime                       = isnull(@EffectiveTime,repa.EffectiveTime)
				,@ExpiryTime                          = isnull(@ExpiryTime,repa.ExpiryTime)
				,@Rank                                = isnull(@Rank,repa.Rank)
				,@OwnershipPercentage                 = isnull(@OwnershipPercentage,repa.OwnershipPercentage)
				,@IsEmployerInsurance                 = isnull(@IsEmployerInsurance,repa.IsEmployerInsurance)
				,@InsuranceOrgSID                     = isnull(@InsuranceOrgSID,repa.InsuranceOrgSID)
				,@InsurancePolicyNo                   = isnull(@InsurancePolicyNo,repa.InsurancePolicyNo)
				,@InsuranceAmount                     = isnull(@InsuranceAmount,repa.InsuranceAmount)
				,@RegistrantEmploymentRowGUID         = isnull(@RegistrantEmploymentRowGUID,repa.RegistrantEmploymentRowGUID)
				,@IsDeleteEnabled                     = isnull(@IsDeleteEnabled,repa.IsDeleteEnabled)
			from
				dbo.vRegistrantEmploymentPracticeArea repa
			where
				repa.RegistrantEmploymentPracticeAreaSID = @RegistrantEmploymentPracticeAreaSID

		end

		-- block changing FK values to parent records which are inactive (UI should prevent this)
		
		if (select x.PracticeAreaSID from dbo.RegistrantEmploymentPracticeArea x where x.RegistrantEmploymentPracticeAreaSID = @RegistrantEmploymentPracticeAreaSID) <> @PracticeAreaSID
		begin
			if (select x.IsActive from dbo.PracticeArea x where x.PracticeAreaSID = @PracticeAreaSID) = @OFF
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'AssignmentToInactiveParent'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 assigned is marked inactive. Leave the record unchanged or set to an active %1.'
					,@Arg1        = N'practice area'
				
				raiserror(@errorText, 16, 1)
				
			end
		end
		
		-- unset previous primary if record is being marked as the new primary
		
		if @IsPrimary = @ON
		begin
		
			select @recordSID = x.RegistrantEmploymentPracticeAreaSID from dbo.RegistrantEmploymentPracticeArea x where x.IsPrimary = @ON and x.RegistrantEmploymentSID = @RegistrantEmploymentSID and x.RegistrantEmploymentPracticeAreaSID <> @RegistrantEmploymentPracticeAreaSID
			
			if @recordSID is not null
			begin
			
				update
					dbo.RegistrantEmploymentPracticeArea
				set
					 IsPrimary  = @OFF
					,UpdateUser = @UpdateUser
					,UpdateTime = sysdatetimeoffset()
				where
					RegistrantEmploymentPracticeAreaSID = @recordSID								-- unique index ensures only 1 record needs to be unset
				
			end
		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		--  insert pre-update logic here ...
		--! </PreUpdate>
	
		-- call the extended version of the procedure (if it exists) for "update.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pRegistrantEmploymentPracticeArea'
		)
		begin
		
			exec @errorNo = ext.pRegistrantEmploymentPracticeArea
				 @Mode                                = 'update.pre'
				,@RegistrantEmploymentPracticeAreaSID = @RegistrantEmploymentPracticeAreaSID
				,@RegistrantEmploymentSID             = @RegistrantEmploymentSID output
				,@PracticeAreaSID                     = @PracticeAreaSID output
				,@IsPrimary                           = @IsPrimary output
				,@UserDefinedColumns                  = @UserDefinedColumns output
				,@RegistrantEmploymentPracticeAreaXID = @RegistrantEmploymentPracticeAreaXID output
				,@LegacyKey                           = @LegacyKey output
				,@UpdateUser                          = @UpdateUser
				,@RowStamp                            = @RowStamp
				,@IsReselected                        = @IsReselected
				,@IsNullApplied                       = @IsNullApplied
				,@zContext                            = @zContext
				,@PracticeAreaName                    = @PracticeAreaName
				,@PracticeAreaCode                    = @PracticeAreaCode
				,@PracticeAreaCategory                = @PracticeAreaCategory
				,@IsPracticeScopeRequired             = @IsPracticeScopeRequired
				,@PracticeAreaIsDefault               = @PracticeAreaIsDefault
				,@PracticeAreaIsActive                = @PracticeAreaIsActive
				,@PracticeAreaRowGUID                 = @PracticeAreaRowGUID
				,@RegistrantSID                       = @RegistrantSID
				,@OrgSID                              = @OrgSID
				,@RegistrationYear                    = @RegistrationYear
				,@EmploymentTypeSID                   = @EmploymentTypeSID
				,@EmploymentRoleSID                   = @EmploymentRoleSID
				,@PracticeHours                       = @PracticeHours
				,@PracticeScopeSID                    = @PracticeScopeSID
				,@AgeRangeSID                         = @AgeRangeSID
				,@IsOnPublicRegistry                  = @IsOnPublicRegistry
				,@Phone                               = @Phone
				,@SiteLocation                        = @SiteLocation
				,@EffectiveTime                       = @EffectiveTime
				,@ExpiryTime                          = @ExpiryTime
				,@Rank                                = @Rank
				,@OwnershipPercentage                 = @OwnershipPercentage
				,@IsEmployerInsurance                 = @IsEmployerInsurance
				,@InsuranceOrgSID                     = @InsuranceOrgSID
				,@InsurancePolicyNo                   = @InsurancePolicyNo
				,@InsuranceAmount                     = @InsuranceAmount
				,@RegistrantEmploymentRowGUID         = @RegistrantEmploymentRowGUID
				,@IsDeleteEnabled                     = @IsDeleteEnabled
		
		end

		-- update the record

		update
			dbo.RegistrantEmploymentPracticeArea
		set
			 RegistrantEmploymentSID = @RegistrantEmploymentSID
			,PracticeAreaSID = @PracticeAreaSID
			,IsPrimary = @IsPrimary
			,UserDefinedColumns = @UserDefinedColumns
			,RegistrantEmploymentPracticeAreaXID = @RegistrantEmploymentPracticeAreaXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			RegistrantEmploymentPracticeAreaSID = @RegistrantEmploymentPracticeAreaSID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from dbo.RegistrantEmploymentPracticeArea where RegistrantEmploymentPracticeAreaSID = @registrantEmploymentPracticeAreaSID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'dbo.RegistrantEmploymentPracticeArea'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'dbo.RegistrantEmploymentPracticeArea'
					,@Arg2        = @registrantEmploymentPracticeAreaSID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'dbo.RegistrantEmploymentPracticeArea'
				,@Arg3        = @rowsAffected
				,@Arg4        = @registrantEmploymentPracticeAreaSID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>
	
		-- call the extended version of the procedure for update.post - if it exists
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pRegistrantEmploymentPracticeArea'
		)
		begin
		
			exec @errorNo = ext.pRegistrantEmploymentPracticeArea
				 @Mode                                = 'update.post'
				,@RegistrantEmploymentPracticeAreaSID = @RegistrantEmploymentPracticeAreaSID
				,@RegistrantEmploymentSID             = @RegistrantEmploymentSID
				,@PracticeAreaSID                     = @PracticeAreaSID
				,@IsPrimary                           = @IsPrimary
				,@UserDefinedColumns                  = @UserDefinedColumns
				,@RegistrantEmploymentPracticeAreaXID = @RegistrantEmploymentPracticeAreaXID
				,@LegacyKey                           = @LegacyKey
				,@UpdateUser                          = @UpdateUser
				,@RowStamp                            = @RowStamp
				,@IsReselected                        = @IsReselected
				,@IsNullApplied                       = @IsNullApplied
				,@zContext                            = @zContext
				,@PracticeAreaName                    = @PracticeAreaName
				,@PracticeAreaCode                    = @PracticeAreaCode
				,@PracticeAreaCategory                = @PracticeAreaCategory
				,@IsPracticeScopeRequired             = @IsPracticeScopeRequired
				,@PracticeAreaIsDefault               = @PracticeAreaIsDefault
				,@PracticeAreaIsActive                = @PracticeAreaIsActive
				,@PracticeAreaRowGUID                 = @PracticeAreaRowGUID
				,@RegistrantSID                       = @RegistrantSID
				,@OrgSID                              = @OrgSID
				,@RegistrationYear                    = @RegistrationYear
				,@EmploymentTypeSID                   = @EmploymentTypeSID
				,@EmploymentRoleSID                   = @EmploymentRoleSID
				,@PracticeHours                       = @PracticeHours
				,@PracticeScopeSID                    = @PracticeScopeSID
				,@AgeRangeSID                         = @AgeRangeSID
				,@IsOnPublicRegistry                  = @IsOnPublicRegistry
				,@Phone                               = @Phone
				,@SiteLocation                        = @SiteLocation
				,@EffectiveTime                       = @EffectiveTime
				,@ExpiryTime                          = @ExpiryTime
				,@Rank                                = @Rank
				,@OwnershipPercentage                 = @OwnershipPercentage
				,@IsEmployerInsurance                 = @IsEmployerInsurance
				,@InsuranceOrgSID                     = @InsuranceOrgSID
				,@InsurancePolicyNo                   = @InsurancePolicyNo
				,@InsuranceAmount                     = @InsuranceAmount
				,@RegistrantEmploymentRowGUID         = @RegistrantEmploymentRowGUID
				,@IsDeleteEnabled                     = @IsDeleteEnabled
		
		end

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.RegistrantEmploymentPracticeAreaSID
			from
				dbo.vRegistrantEmploymentPracticeArea ent
			where
				ent.RegistrantEmploymentPracticeAreaSID = @RegistrantEmploymentPracticeAreaSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.RegistrantEmploymentPracticeAreaSID
				,ent.RegistrantEmploymentSID
				,ent.PracticeAreaSID
				,ent.IsPrimary
				,ent.UserDefinedColumns
				,ent.RegistrantEmploymentPracticeAreaXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.PracticeAreaName
				,ent.PracticeAreaCode
				,ent.PracticeAreaCategory
				,ent.IsPracticeScopeRequired
				,ent.PracticeAreaIsDefault
				,ent.PracticeAreaIsActive
				,ent.PracticeAreaRowGUID
				,ent.RegistrantSID
				,ent.OrgSID
				,ent.RegistrationYear
				,ent.EmploymentTypeSID
				,ent.EmploymentRoleSID
				,ent.PracticeHours
				,ent.PracticeScopeSID
				,ent.AgeRangeSID
				,ent.IsOnPublicRegistry
				,ent.Phone
				,ent.SiteLocation
				,ent.EffectiveTime
				,ent.ExpiryTime
				,ent.Rank
				,ent.OwnershipPercentage
				,ent.IsEmployerInsurance
				,ent.InsuranceOrgSID
				,ent.InsurancePolicyNo
				,ent.InsuranceAmount
				,ent.RegistrantEmploymentRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
			from
				dbo.vRegistrantEmploymentPracticeArea ent
			where
				ent.RegistrantEmploymentPracticeAreaSID = @RegistrantEmploymentPracticeAreaSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
