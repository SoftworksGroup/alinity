SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pPracticeRegisterSection#Update]
	 @PracticeRegisterSectionSID          int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@PracticeRegisterSID                 int               = null -- table column values to update:
	,@PracticeRegisterSectionLabel        nvarchar(35)      = null
	,@IsDefault                           bit               = null
	,@IsDisplayedOnLicense                bit               = null
	,@Description                         varbinary(max)    = null
	,@IsActive                            bit               = null
	,@UserDefinedColumns                  xml               = null
	,@PracticeRegisterSectionXID          varchar(150)      = null
	,@LegacyKey                           nvarchar(50)      = null
	,@UpdateUser                          nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                            timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected                        tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied                       bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                            xml               = null -- other values defining context for the update (if any)
	,@PracticeRegisterTypeSID             int               = null -- not a base table column
	,@RegistrationScheduleSID             int               = null -- not a base table column
	,@PracticeRegisterName                nvarchar(65)      = null -- not a base table column
	,@PracticeRegisterLabel               nvarchar(35)      = null -- not a base table column
	,@IsActivePractice                    bit               = null -- not a base table column
	,@IsPublicRegistryEnabled             bit               = null -- not a base table column
	,@IsRenewalEnabled                    bit               = null -- not a base table column
	,@IsLearningPlanEnabled               bit               = null -- not a base table column
	,@IsNextCEFormAutoAdded               bit               = null -- not a base table column
	,@IsEligibleSupervisor                bit               = null -- not a base table column
	,@IsSupervisionRequired               bit               = null -- not a base table column
	,@IsEmploymentTerminated              bit               = null -- not a base table column
	,@IsGroupMembershipTerminated         bit               = null -- not a base table column
	,@TermPermitDays                      int               = null -- not a base table column
	,@RegisterRank                        smallint          = null -- not a base table column
	,@LearningModelSID                    int               = null -- not a base table column
	,@ReasonGroupSID                      int               = null -- not a base table column
	,@PracticeRegisterIsDefault           bit               = null -- not a base table column
	,@IsDefaultInactivePractice           bit               = null -- not a base table column
	,@PracticeRegisterIsActive            bit               = null -- not a base table column
	,@PracticeRegisterRowGUID             uniqueidentifier  = null -- not a base table column
	,@IsDeleteEnabled                     bit               = null -- not a base table column
	,@PracticeRegisterSectionDisplayLabel nvarchar(71)      = null -- not a base table column
	,@ApplicationFormVersionSID           int               = null -- not a base table column
	,@AppVerificationFormVersionSID       int               = null -- not a base table column
	,@RenewalFormVersionSID               int               = null -- not a base table column
	,@IsApplicationFormDefined            bit               = null -- not a base table column
	,@IsAppVerificationFormDefined        bit               = null -- not a base table column
	,@IsRenewalFormDefined                bit               = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : dbo.pPracticeRegisterSection#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the dbo.PracticeRegisterSection table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the dbo.PracticeRegisterSection table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vPracticeRegisterSection entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pPracticeRegisterSection procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fPracticeRegisterSectionCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @PracticeRegisterSectionSID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@PracticeRegisterSectionSID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @PracticeRegisterSectionLabel = ltrim(rtrim(@PracticeRegisterSectionLabel))
		set @PracticeRegisterSectionXID = ltrim(rtrim(@PracticeRegisterSectionXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @PracticeRegisterName = ltrim(rtrim(@PracticeRegisterName))
		set @PracticeRegisterLabel = ltrim(rtrim(@PracticeRegisterLabel))
		set @PracticeRegisterSectionDisplayLabel = ltrim(rtrim(@PracticeRegisterSectionDisplayLabel))

		-- set zero length strings to null to avoid storing them in the record

		if len(@PracticeRegisterSectionLabel) = 0 set @PracticeRegisterSectionLabel = null
		if len(@PracticeRegisterSectionXID) = 0 set @PracticeRegisterSectionXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@PracticeRegisterName) = 0 set @PracticeRegisterName = null
		if len(@PracticeRegisterLabel) = 0 set @PracticeRegisterLabel = null
		if len(@PracticeRegisterSectionDisplayLabel) = 0 set @PracticeRegisterSectionDisplayLabel = null

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @PracticeRegisterSID                 = isnull(@PracticeRegisterSID,prs.PracticeRegisterSID)
				,@PracticeRegisterSectionLabel        = isnull(@PracticeRegisterSectionLabel,prs.PracticeRegisterSectionLabel)
				,@IsDefault                           = isnull(@IsDefault,prs.IsDefault)
				,@IsDisplayedOnLicense                = isnull(@IsDisplayedOnLicense,prs.IsDisplayedOnLicense)
				,@Description                         = isnull(@Description,prs.Description)
				,@IsActive                            = isnull(@IsActive,prs.IsActive)
				,@UserDefinedColumns                  = isnull(@UserDefinedColumns,prs.UserDefinedColumns)
				,@PracticeRegisterSectionXID          = isnull(@PracticeRegisterSectionXID,prs.PracticeRegisterSectionXID)
				,@LegacyKey                           = isnull(@LegacyKey,prs.LegacyKey)
				,@UpdateUser                          = isnull(@UpdateUser,prs.UpdateUser)
				,@IsReselected                        = isnull(@IsReselected,prs.IsReselected)
				,@IsNullApplied                       = isnull(@IsNullApplied,prs.IsNullApplied)
				,@zContext                            = isnull(@zContext,prs.zContext)
				,@PracticeRegisterTypeSID             = isnull(@PracticeRegisterTypeSID,prs.PracticeRegisterTypeSID)
				,@RegistrationScheduleSID             = isnull(@RegistrationScheduleSID,prs.RegistrationScheduleSID)
				,@PracticeRegisterName                = isnull(@PracticeRegisterName,prs.PracticeRegisterName)
				,@PracticeRegisterLabel               = isnull(@PracticeRegisterLabel,prs.PracticeRegisterLabel)
				,@IsActivePractice                    = isnull(@IsActivePractice,prs.IsActivePractice)
				,@IsPublicRegistryEnabled             = isnull(@IsPublicRegistryEnabled,prs.IsPublicRegistryEnabled)
				,@IsRenewalEnabled                    = isnull(@IsRenewalEnabled,prs.IsRenewalEnabled)
				,@IsLearningPlanEnabled               = isnull(@IsLearningPlanEnabled,prs.IsLearningPlanEnabled)
				,@IsNextCEFormAutoAdded               = isnull(@IsNextCEFormAutoAdded,prs.IsNextCEFormAutoAdded)
				,@IsEligibleSupervisor                = isnull(@IsEligibleSupervisor,prs.IsEligibleSupervisor)
				,@IsSupervisionRequired               = isnull(@IsSupervisionRequired,prs.IsSupervisionRequired)
				,@IsEmploymentTerminated              = isnull(@IsEmploymentTerminated,prs.IsEmploymentTerminated)
				,@IsGroupMembershipTerminated         = isnull(@IsGroupMembershipTerminated,prs.IsGroupMembershipTerminated)
				,@TermPermitDays                      = isnull(@TermPermitDays,prs.TermPermitDays)
				,@RegisterRank                        = isnull(@RegisterRank,prs.RegisterRank)
				,@LearningModelSID                    = isnull(@LearningModelSID,prs.LearningModelSID)
				,@ReasonGroupSID                      = isnull(@ReasonGroupSID,prs.ReasonGroupSID)
				,@PracticeRegisterIsDefault           = isnull(@PracticeRegisterIsDefault,prs.PracticeRegisterIsDefault)
				,@IsDefaultInactivePractice           = isnull(@IsDefaultInactivePractice,prs.IsDefaultInactivePractice)
				,@PracticeRegisterIsActive            = isnull(@PracticeRegisterIsActive,prs.PracticeRegisterIsActive)
				,@PracticeRegisterRowGUID             = isnull(@PracticeRegisterRowGUID,prs.PracticeRegisterRowGUID)
				,@IsDeleteEnabled                     = isnull(@IsDeleteEnabled,prs.IsDeleteEnabled)
				,@PracticeRegisterSectionDisplayLabel = isnull(@PracticeRegisterSectionDisplayLabel,prs.PracticeRegisterSectionDisplayLabel)
				,@ApplicationFormVersionSID           = isnull(@ApplicationFormVersionSID,prs.ApplicationFormVersionSID)
				,@AppVerificationFormVersionSID       = isnull(@AppVerificationFormVersionSID,prs.AppVerificationFormVersionSID)
				,@RenewalFormVersionSID               = isnull(@RenewalFormVersionSID,prs.RenewalFormVersionSID)
				,@IsApplicationFormDefined            = isnull(@IsApplicationFormDefined,prs.IsApplicationFormDefined)
				,@IsAppVerificationFormDefined        = isnull(@IsAppVerificationFormDefined,prs.IsAppVerificationFormDefined)
				,@IsRenewalFormDefined                = isnull(@IsRenewalFormDefined,prs.IsRenewalFormDefined)
			from
				dbo.vPracticeRegisterSection prs
			where
				prs.PracticeRegisterSectionSID = @PracticeRegisterSectionSID

		end

		-- block changing FK values to parent records which are inactive (UI should prevent this)
		
		if (select x.PracticeRegisterSID from dbo.PracticeRegisterSection x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) <> @PracticeRegisterSID
		begin
			if (select x.IsActive from dbo.PracticeRegister x where x.PracticeRegisterSID = @PracticeRegisterSID) = @OFF
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'AssignmentToInactiveParent'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 assigned is marked inactive. Leave the record unchanged or set to an active %1.'
					,@Arg1        = N'practice register'
				
				raiserror(@errorText, 16, 1)
				
			end
		end
		
		-- unset previous default if record is being marked as the new default
		
		if @IsDefault = @ON
		begin
		
			select @recordSID = x.PracticeRegisterSectionSID from dbo.PracticeRegisterSection x where x.IsDefault = @ON and x.PracticeRegisterSID = @PracticeRegisterSID and x.PracticeRegisterSectionSID <> @PracticeRegisterSectionSID
			
			if @recordSID is not null
			begin
			
				update
					dbo.PracticeRegisterSection
				set
					 IsDefault  = @OFF
					,UpdateUser = @UpdateUser
					,UpdateTime = sysdatetimeoffset()
				where
					PracticeRegisterSectionSID = @recordSID													-- unique index ensures only 1 record needs to be unset
				
			end
		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		--  insert pre-update logic here ...
		--! </PreUpdate>
	
		-- call the extended version of the procedure (if it exists) for "update.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pPracticeRegisterSection'
		)
		begin
		
			exec @errorNo = ext.pPracticeRegisterSection
				 @Mode                                = 'update.pre'
				,@PracticeRegisterSectionSID          = @PracticeRegisterSectionSID
				,@PracticeRegisterSID                 = @PracticeRegisterSID output
				,@PracticeRegisterSectionLabel        = @PracticeRegisterSectionLabel output
				,@IsDefault                           = @IsDefault output
				,@IsDisplayedOnLicense                = @IsDisplayedOnLicense output
				,@Description                         = @Description output
				,@IsActive                            = @IsActive output
				,@UserDefinedColumns                  = @UserDefinedColumns output
				,@PracticeRegisterSectionXID          = @PracticeRegisterSectionXID output
				,@LegacyKey                           = @LegacyKey output
				,@UpdateUser                          = @UpdateUser
				,@RowStamp                            = @RowStamp
				,@IsReselected                        = @IsReselected
				,@IsNullApplied                       = @IsNullApplied
				,@zContext                            = @zContext
				,@PracticeRegisterTypeSID             = @PracticeRegisterTypeSID
				,@RegistrationScheduleSID             = @RegistrationScheduleSID
				,@PracticeRegisterName                = @PracticeRegisterName
				,@PracticeRegisterLabel               = @PracticeRegisterLabel
				,@IsActivePractice                    = @IsActivePractice
				,@IsPublicRegistryEnabled             = @IsPublicRegistryEnabled
				,@IsRenewalEnabled                    = @IsRenewalEnabled
				,@IsLearningPlanEnabled               = @IsLearningPlanEnabled
				,@IsNextCEFormAutoAdded               = @IsNextCEFormAutoAdded
				,@IsEligibleSupervisor                = @IsEligibleSupervisor
				,@IsSupervisionRequired               = @IsSupervisionRequired
				,@IsEmploymentTerminated              = @IsEmploymentTerminated
				,@IsGroupMembershipTerminated         = @IsGroupMembershipTerminated
				,@TermPermitDays                      = @TermPermitDays
				,@RegisterRank                        = @RegisterRank
				,@LearningModelSID                    = @LearningModelSID
				,@ReasonGroupSID                      = @ReasonGroupSID
				,@PracticeRegisterIsDefault           = @PracticeRegisterIsDefault
				,@IsDefaultInactivePractice           = @IsDefaultInactivePractice
				,@PracticeRegisterIsActive            = @PracticeRegisterIsActive
				,@PracticeRegisterRowGUID             = @PracticeRegisterRowGUID
				,@IsDeleteEnabled                     = @IsDeleteEnabled
				,@PracticeRegisterSectionDisplayLabel = @PracticeRegisterSectionDisplayLabel
				,@ApplicationFormVersionSID           = @ApplicationFormVersionSID
				,@AppVerificationFormVersionSID       = @AppVerificationFormVersionSID
				,@RenewalFormVersionSID               = @RenewalFormVersionSID
				,@IsApplicationFormDefined            = @IsApplicationFormDefined
				,@IsAppVerificationFormDefined        = @IsAppVerificationFormDefined
				,@IsRenewalFormDefined                = @IsRenewalFormDefined
		
		end

		-- update the record

		update
			dbo.PracticeRegisterSection
		set
			 PracticeRegisterSID = @PracticeRegisterSID
			,PracticeRegisterSectionLabel = @PracticeRegisterSectionLabel
			,IsDefault = @IsDefault
			,IsDisplayedOnLicense = @IsDisplayedOnLicense
			,Description = @Description
			,IsActive = @IsActive
			,UserDefinedColumns = @UserDefinedColumns
			,PracticeRegisterSectionXID = @PracticeRegisterSectionXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			PracticeRegisterSectionSID = @PracticeRegisterSectionSID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from dbo.PracticeRegisterSection where PracticeRegisterSectionSID = @practiceRegisterSectionSID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'dbo.PracticeRegisterSection'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'dbo.PracticeRegisterSection'
					,@Arg2        = @practiceRegisterSectionSID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'dbo.PracticeRegisterSection'
				,@Arg3        = @rowsAffected
				,@Arg4        = @practiceRegisterSectionSID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>
		
		-- ensure a default record is identified on the table
		
		if not exists
		(
			select 1 from	dbo.PracticeRegisterSection x where x.IsDefault = @ON
		)
		begin
		
			exec sf.pMessage#Get
				 @MessageSCD  = 'MissingDefault'
				,@MessageText = @errorText output
				,@DefaultText = N'A default %1 record is required by the application. (Setting another record as the new default automatically un-sets the previous one.)'
				,@Arg1        = 'Practice Register Section'
			
			raiserror(@errorText, 16, 1)
		end
	
		-- call the extended version of the procedure for update.post - if it exists
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pPracticeRegisterSection'
		)
		begin
		
			exec @errorNo = ext.pPracticeRegisterSection
				 @Mode                                = 'update.post'
				,@PracticeRegisterSectionSID          = @PracticeRegisterSectionSID
				,@PracticeRegisterSID                 = @PracticeRegisterSID
				,@PracticeRegisterSectionLabel        = @PracticeRegisterSectionLabel
				,@IsDefault                           = @IsDefault
				,@IsDisplayedOnLicense                = @IsDisplayedOnLicense
				,@Description                         = @Description
				,@IsActive                            = @IsActive
				,@UserDefinedColumns                  = @UserDefinedColumns
				,@PracticeRegisterSectionXID          = @PracticeRegisterSectionXID
				,@LegacyKey                           = @LegacyKey
				,@UpdateUser                          = @UpdateUser
				,@RowStamp                            = @RowStamp
				,@IsReselected                        = @IsReselected
				,@IsNullApplied                       = @IsNullApplied
				,@zContext                            = @zContext
				,@PracticeRegisterTypeSID             = @PracticeRegisterTypeSID
				,@RegistrationScheduleSID             = @RegistrationScheduleSID
				,@PracticeRegisterName                = @PracticeRegisterName
				,@PracticeRegisterLabel               = @PracticeRegisterLabel
				,@IsActivePractice                    = @IsActivePractice
				,@IsPublicRegistryEnabled             = @IsPublicRegistryEnabled
				,@IsRenewalEnabled                    = @IsRenewalEnabled
				,@IsLearningPlanEnabled               = @IsLearningPlanEnabled
				,@IsNextCEFormAutoAdded               = @IsNextCEFormAutoAdded
				,@IsEligibleSupervisor                = @IsEligibleSupervisor
				,@IsSupervisionRequired               = @IsSupervisionRequired
				,@IsEmploymentTerminated              = @IsEmploymentTerminated
				,@IsGroupMembershipTerminated         = @IsGroupMembershipTerminated
				,@TermPermitDays                      = @TermPermitDays
				,@RegisterRank                        = @RegisterRank
				,@LearningModelSID                    = @LearningModelSID
				,@ReasonGroupSID                      = @ReasonGroupSID
				,@PracticeRegisterIsDefault           = @PracticeRegisterIsDefault
				,@IsDefaultInactivePractice           = @IsDefaultInactivePractice
				,@PracticeRegisterIsActive            = @PracticeRegisterIsActive
				,@PracticeRegisterRowGUID             = @PracticeRegisterRowGUID
				,@IsDeleteEnabled                     = @IsDeleteEnabled
				,@PracticeRegisterSectionDisplayLabel = @PracticeRegisterSectionDisplayLabel
				,@ApplicationFormVersionSID           = @ApplicationFormVersionSID
				,@AppVerificationFormVersionSID       = @AppVerificationFormVersionSID
				,@RenewalFormVersionSID               = @RenewalFormVersionSID
				,@IsApplicationFormDefined            = @IsApplicationFormDefined
				,@IsAppVerificationFormDefined        = @IsAppVerificationFormDefined
				,@IsRenewalFormDefined                = @IsRenewalFormDefined
		
		end

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.PracticeRegisterSectionSID
			from
				dbo.vPracticeRegisterSection ent
			where
				ent.PracticeRegisterSectionSID = @PracticeRegisterSectionSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.PracticeRegisterSectionSID
				,ent.PracticeRegisterSID
				,ent.PracticeRegisterSectionLabel
				,ent.IsDefault
				,ent.IsDisplayedOnLicense
				,ent.Description
				,ent.IsActive
				,ent.UserDefinedColumns
				,ent.PracticeRegisterSectionXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.PracticeRegisterTypeSID
				,ent.RegistrationScheduleSID
				,ent.PracticeRegisterName
				,ent.PracticeRegisterLabel
				,ent.IsActivePractice
				,ent.IsPublicRegistryEnabled
				,ent.IsRenewalEnabled
				,ent.IsLearningPlanEnabled
				,ent.IsNextCEFormAutoAdded
				,ent.IsEligibleSupervisor
				,ent.IsSupervisionRequired
				,ent.IsEmploymentTerminated
				,ent.IsGroupMembershipTerminated
				,ent.TermPermitDays
				,ent.RegisterRank
				,ent.LearningModelSID
				,ent.ReasonGroupSID
				,ent.PracticeRegisterIsDefault
				,ent.IsDefaultInactivePractice
				,ent.PracticeRegisterIsActive
				,ent.PracticeRegisterRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
				,ent.PracticeRegisterSectionDisplayLabel
				,ent.ApplicationFormVersionSID
				,ent.AppVerificationFormVersionSID
				,ent.RenewalFormVersionSID
				,ent.IsApplicationFormDefined
				,ent.IsAppVerificationFormDefined
				,ent.IsRenewalFormDefined
			from
				dbo.vPracticeRegisterSection ent
			where
				ent.PracticeRegisterSectionSID = @PracticeRegisterSectionSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
