SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pRegistration#Default]
	 @zContext                         xml               = null                -- default values provided from client-tier (if any)
	,@SetFKDefaults                    bit               = 0                   -- when 1, mandatory FK's are returned as -1 instead of NULL
as
/*********************************************************************************************************************************
Procedure : dbo.pRegistration#Default
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : provides a blank row with default values for presentation in the UI for "new" dbo.Registration records
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used in maintenance of the dbo.Registration table. When a new record is to be added from the UI, this procedure
is called to return a blank record with default values. If the client-tier is providing the context for the insert, such as a parent
key value for the new record, it must be passed in the @zContext XML parameter. Multiple values may be passed. The standard format
is: <Parameters MyParameter="1000001"/>.

The @SetFKDefaults parameter can be set to 1 to cause the procedure to return mandatory FK values as -1 rather than NULL. This avoids
the need to create complex types for the procedure on architectures which are not using RIA services.

Note that default values for text, ntext and binary type columns is not supported.  These data types are not permitted as local
variables in the current version of SQL Server and should be replaced by varchar(max) and nvarchar(max) where possible.

Some default values are built-in to the shell of the sproc.  The base table column defaults set in the variable declarations below
were obtained from database default constraints which existed at the time the procedure was generated. The declarations include all
columns of the vRegistration entity view, however, only some values (as noted above) are eligible for default setting.  The other
parameters are included for setting context for the table-specific or client-specific logic of the procedure (if any). Default values
returning a question mark "?", system date, or 0 are provided for non-base table columns which are mandatory.  This is done to avoid
compilation errors from the Entity Framework, however, the values will not be applied since they are not in the base table row.

Two levels of customization of the procedure shell are supported. Table-specific logic can be added through the tagged section and a
call to an extended procedure supports client-specific customization. Logic implemented within the code tags is part of the base
product and applies to all client configurations. Client-specific customizations must be implemented in the ext.pRegistration
procedure. The extended procedure is only called where it exists in database. The parameter "@Mode" is set to "default.pre" to
advise ext.pRegistration of the context of the call. All other parameters are also passed, however, only those parameters eligible
for default setting are passed for "output". All parameters corresponding to entity view columns are returned through a SELECT statement.

In order to simplify working with the XML parameter values, logic in the procedure parses the XML and assigns values to variables where
the variable name matches the column name in the XML (assumes single row).  The variables are then available to the table-specific and
client-specific logic.  The @zContext parameter is also passed, unmodified, to the extended procedure to support situations where values
are passed that are not mapped to column names.


-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@ON                               bit = cast(1 as bit)								-- constant for bit comparisons
		,@OFF                              bit = cast(0 as bit)								-- constant for bit comparisons
		,@registrationSID                  int               = -1							-- specific default required by EF - do not override
		,@registrantSID                    int               = null						-- no default provided from DB constraint - OK to override
		,@practiceRegisterSectionSID       int               = null						-- no default provided from DB constraint - OK to override
		,@registrationNo                   nvarchar(50)      = null						-- no default provided from DB constraint - OK to override
		,@registrationYear                 smallint          = null						-- no default provided from DB constraint - OK to override
		,@effectiveTime                    datetime          = null						-- no default provided from DB constraint - OK to override
		,@expiryTime                       datetime          = null						-- no default provided from DB constraint - OK to override
		,@cardPrintedTime                  datetime          = null						-- no default provided from DB constraint - OK to override
		,@invoiceSID                       int               = null						-- no default provided from DB constraint - OK to override
		,@reasonSID                        int               = null						-- no default provided from DB constraint - OK to override
		,@formGUID                         uniqueidentifier  = null						-- no default provided from DB constraint - OK to override
		,@userDefinedColumns               xml               = null						-- no default provided from DB constraint - OK to override
		,@registrationXID                  varchar(150)      = null						-- no default provided from DB constraint - OK to override
		,@legacyKey                        nvarchar(50)      = null						-- no default provided from DB constraint - OK to override
		,@isDeleted                        bit               = (0)						-- default provided from DB constraint - OK to override
		,@createUser                       nvarchar(75)      = suser_sname()	-- default value ignored (value set by UI)
		,@createTime                       datetimeoffset(7) = sysdatetimeoffset()											-- default value ignored (set to system time)
		,@updateUser                       nvarchar(75)      = suser_sname()	-- default value ignored (value set by UI)
		,@updateTime                       datetimeoffset(7) = sysdatetimeoffset()											-- default value ignored (set to system time)
		,@rowGUID                          uniqueidentifier  = newid()				-- default value ignored (value set by system)
		,@rowStamp                         timestamp         = null						-- default value ignored (value set by system)
		,@practiceRegisterSID              int               = 0							-- not a base table column (default ignored)
		,@practiceRegisterSectionLabel     nvarchar(35)      = N'?'						-- not a base table column (default ignored)
		,@practiceRegisterSectionIsDefault bit               = 0							-- not a base table column (default ignored)
		,@isDisplayedOnLicense             bit               = 0							-- not a base table column (default ignored)
		,@practiceRegisterSectionIsActive  bit               = 0							-- not a base table column (default ignored)
		,@practiceRegisterSectionRowGUID   uniqueidentifier  = newid()				-- not a base table column (default ignored)
		,@registrantPersonSID              int               = 0							-- not a base table column (default ignored)
		,@registrantNo                     varchar(50)       = '?'						-- not a base table column (default ignored)
		,@yearOfInitialEmployment          smallint														-- not a base table column (default ignored)
		,@isOnPublicRegistry               bit               = 0							-- not a base table column (default ignored)
		,@cityNameOfBirth                  nvarchar(30)												-- not a base table column (default ignored)
		,@countrySID                       int																-- not a base table column (default ignored)
		,@directedAuditYearCompetence      smallint														-- not a base table column (default ignored)
		,@directedAuditYearPracticeHours   smallint														-- not a base table column (default ignored)
		,@lateFeeExclusionYear             smallint														-- not a base table column (default ignored)
		,@isRenewalAutoApprovalBlocked     bit               = 0							-- not a base table column (default ignored)
		,@renewalExtensionExpiryTime       datetime														-- not a base table column (default ignored)
		,@archivedTime                     datetimeoffset(7)									-- not a base table column (default ignored)
		,@registrantRowGUID                uniqueidentifier  = newid()				-- not a base table column (default ignored)
		,@invoicePersonSID                 int																-- not a base table column (default ignored)
		,@invoiceDate                      date																-- not a base table column (default ignored)
		,@tax1Label                        nvarchar(8)												-- not a base table column (default ignored)
		,@tax1Rate                         decimal(4,4)												-- not a base table column (default ignored)
		,@tax1GLAccountCode                varchar(50)												-- not a base table column (default ignored)
		,@tax2Label                        nvarchar(8)												-- not a base table column (default ignored)
		,@tax2Rate                         decimal(4,4)												-- not a base table column (default ignored)
		,@tax2GLAccountCode                varchar(50)												-- not a base table column (default ignored)
		,@tax3Label                        nvarchar(8)												-- not a base table column (default ignored)
		,@tax3Rate                         decimal(4,4)												-- not a base table column (default ignored)
		,@tax3GLAccountCode                varchar(50)												-- not a base table column (default ignored)
		,@invoiceRegistrationYear          smallint														-- not a base table column (default ignored)
		,@cancelledTime                    datetimeoffset(7)									-- not a base table column (default ignored)
		,@invoiceReasonSID                 int																-- not a base table column (default ignored)
		,@isRefund                         bit																-- not a base table column (default ignored)
		,@complaintSID                     int																-- not a base table column (default ignored)
		,@invoiceRowGUID                   uniqueidentifier										-- not a base table column (default ignored)
		,@reasonGroupSID                   int																-- not a base table column (default ignored)
		,@reasonName                       nvarchar(50)												-- not a base table column (default ignored)
		,@reasonCode                       varchar(25)												-- not a base table column (default ignored)
		,@reasonSequence                   smallint														-- not a base table column (default ignored)
		,@toolTip                          nvarchar(500)											-- not a base table column (default ignored)
		,@reasonIsActive                   bit																-- not a base table column (default ignored)
		,@reasonRowGUID                    uniqueidentifier										-- not a base table column (default ignored)
		,@isActive                         bit																-- not a base table column (default ignored)
		,@isPending                        bit																-- not a base table column (default ignored)
		,@isDeleteEnabled                  bit																-- not a base table column (default ignored)
		,@isReselected                     tinyint           = 1							-- specific default required by EF - do not override
		,@isNullApplied                    bit               = 1							-- specific default required by EF - do not override
		,@registrantLabel                  nvarchar(75)												-- not a base table column (default ignored)
		,@registrationYearLabel            varchar(25)												-- not a base table column (default ignored)
		,@practiceRegisterName             nvarchar(65)												-- not a base table column (default ignored)
		,@practiceRegisterLabel            nvarchar(35)												-- not a base table column (default ignored)
		,@registrationLabel                nvarchar(85)												-- not a base table column (default ignored)
		,@isReadEnabled                    bit																-- not a base table column (default ignored)
		,@firstName                        nvarchar(30)      = N'?'						-- not a base table column (default ignored)
		,@middleNames                      nvarchar(30)												-- not a base table column (default ignored)
		,@lastName                         nvarchar(35)      = N'?'						-- not a base table column (default ignored)
		,@addressBlockForPrint             nvarchar(512)											-- not a base table column (default ignored)
		,@addressBlockForHTML              nvarchar(512)											-- not a base table column (default ignored)
		,@futureRegistrationLabel          nvarchar(85)												-- not a base table column (default ignored)
		,@futureRegistrationYear           smallint														-- not a base table column (default ignored)
		,@futurePracticeRegisterSID        int																-- not a base table column (default ignored)
		,@futurePracticeRegisterLabel      nvarchar(35)												-- not a base table column (default ignored)
		,@futurePracticeRegisterSectionSID int																-- not a base table column (default ignored)
		,@futureRegisterSectionLabel       nvarchar(35)												-- not a base table column (default ignored)
		,@futureEffectiveTime              datetime														-- not a base table column (default ignored)
		,@futureExpiryTime                 datetime														-- not a base table column (default ignored)
		,@futureCardPrintedTime            datetime														-- not a base table column (default ignored)
		,@futureInvoiceSID                 int																-- not a base table column (default ignored)
		,@futureReasonSID                  int																-- not a base table column (default ignored)
		,@futureFormGUID                   uniqueidentifier										-- not a base table column (default ignored)

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end
		-- set mandatory FK values to -1 where requested
		
		if @SetFKDefaults = @ON
		begin
			set @registrantSID = -1
			set @practiceRegisterSectionSID = -1
		end

		-- assign literal defaults passed through @zContext where
		-- provided otherwise leave database default in place
		
		select
			 @registrantSID               = isnull(context.node.value('@RegistrantSID'             ,'int'             ),@registrantSID)
			,@practiceRegisterSectionSID  = isnull(context.node.value('@PracticeRegisterSectionSID','int'             ),@practiceRegisterSectionSID)
			,@registrationNo              = isnull(context.node.value('@RegistrationNo'            ,'nvarchar(50)'    ),@registrationNo)
			,@registrationYear            = isnull(context.node.value('@RegistrationYear'          ,'smallint'        ),@registrationYear)
			,@effectiveTime               = isnull(context.node.value('@EffectiveTime'             ,'datetime'        ),@effectiveTime)
			,@expiryTime                  = isnull(context.node.value('@ExpiryTime'                ,'datetime'        ),@expiryTime)
			,@cardPrintedTime             = isnull(context.node.value('@CardPrintedTime'           ,'datetime'        ),@cardPrintedTime)
			,@invoiceSID                  = isnull(context.node.value('@InvoiceSID'                ,'int'             ),@invoiceSID)
			,@reasonSID                   = isnull(context.node.value('@ReasonSID'                 ,'int'             ),@reasonSID)
			,@formGUID                    = isnull(context.node.value('@FormGUID'                  ,'uniqueidentifier'),@formGUID)
			,@registrationXID             = isnull(context.node.value('@RegistrationXID'           ,'varchar(150)'    ),@registrationXID)
			,@legacyKey                   = isnull(context.node.value('@LegacyKey'                 ,'nvarchar(50)'    ),@legacyKey)
		from
			@zContext.nodes('Parameters') as context(node)
		
		set @effectiveTime = sf.fToday()																			-- default effective time to current date
		
		-- set default value on foreign keys where configured
		-- and where no DB or literal value was passed for it
		
		if isnull(@practiceRegisterSectionSID,0) = 0 select @practiceRegisterSectionSID = x.PracticeRegisterSectionSID from dbo.PracticeRegisterSection x where x.IsDefault = @ON and x.PracticeRegisterSID = @practiceRegisterSID

		--! <Overrides>
		--  insert default value logic here ...
		--! </Overrides>
	
		-- call the extended version of the procedure (if it exists) for "default.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pRegistration'
		)
		begin
		
			exec @errorNo = ext.pRegistration
				 @Mode                             = 'default.pre'
				,@RegistrationSID = @registrationSID
				,@RegistrantSID = @registrantSID output
				,@PracticeRegisterSectionSID = @practiceRegisterSectionSID output
				,@RegistrationNo = @registrationNo output
				,@RegistrationYear = @registrationYear output
				,@EffectiveTime = @effectiveTime output
				,@ExpiryTime = @expiryTime output
				,@CardPrintedTime = @cardPrintedTime output
				,@InvoiceSID = @invoiceSID output
				,@ReasonSID = @reasonSID output
				,@FormGUID = @formGUID output
				,@UserDefinedColumns = @userDefinedColumns output
				,@RegistrationXID = @registrationXID output
				,@LegacyKey = @legacyKey output
				,@IsDeleted = @isDeleted
				,@CreateUser = @createUser
				,@CreateTime = @createTime
				,@UpdateUser = @updateUser
				,@UpdateTime = @updateTime
				,@RowGUID = @rowGUID
				,@RowStamp = @rowStamp
				,@PracticeRegisterSID = @practiceRegisterSID
				,@PracticeRegisterSectionLabel = @practiceRegisterSectionLabel
				,@PracticeRegisterSectionIsDefault = @practiceRegisterSectionIsDefault
				,@IsDisplayedOnLicense = @isDisplayedOnLicense
				,@PracticeRegisterSectionIsActive = @practiceRegisterSectionIsActive
				,@PracticeRegisterSectionRowGUID = @practiceRegisterSectionRowGUID
				,@RegistrantPersonSID = @registrantPersonSID
				,@RegistrantNo = @registrantNo
				,@YearOfInitialEmployment = @yearOfInitialEmployment
				,@IsOnPublicRegistry = @isOnPublicRegistry
				,@CityNameOfBirth = @cityNameOfBirth
				,@CountrySID = @countrySID
				,@DirectedAuditYearCompetence = @directedAuditYearCompetence
				,@DirectedAuditYearPracticeHours = @directedAuditYearPracticeHours
				,@LateFeeExclusionYear = @lateFeeExclusionYear
				,@IsRenewalAutoApprovalBlocked = @isRenewalAutoApprovalBlocked
				,@RenewalExtensionExpiryTime = @renewalExtensionExpiryTime
				,@ArchivedTime = @archivedTime
				,@RegistrantRowGUID = @registrantRowGUID
				,@InvoicePersonSID = @invoicePersonSID
				,@InvoiceDate = @invoiceDate
				,@Tax1Label = @tax1Label
				,@Tax1Rate = @tax1Rate
				,@Tax1GLAccountCode = @tax1GLAccountCode
				,@Tax2Label = @tax2Label
				,@Tax2Rate = @tax2Rate
				,@Tax2GLAccountCode = @tax2GLAccountCode
				,@Tax3Label = @tax3Label
				,@Tax3Rate = @tax3Rate
				,@Tax3GLAccountCode = @tax3GLAccountCode
				,@InvoiceRegistrationYear = @invoiceRegistrationYear
				,@CancelledTime = @cancelledTime
				,@InvoiceReasonSID = @invoiceReasonSID
				,@IsRefund = @isRefund
				,@ComplaintSID = @complaintSID
				,@InvoiceRowGUID = @invoiceRowGUID
				,@ReasonGroupSID = @reasonGroupSID
				,@ReasonName = @reasonName
				,@ReasonCode = @reasonCode
				,@ReasonSequence = @reasonSequence
				,@ToolTip = @toolTip
				,@ReasonIsActive = @reasonIsActive
				,@ReasonRowGUID = @reasonRowGUID
				,@IsActive = @isActive
				,@IsPending = @isPending
				,@IsDeleteEnabled = @isDeleteEnabled
				,@IsReselected = @isReselected
				,@IsNullApplied = @isNullApplied
				,@zContext = @zContext output
				,@RegistrantLabel = @registrantLabel
				,@RegistrationYearLabel = @registrationYearLabel
				,@PracticeRegisterName = @practiceRegisterName
				,@PracticeRegisterLabel = @practiceRegisterLabel
				,@RegistrationLabel = @registrationLabel
				,@IsReadEnabled = @isReadEnabled
				,@FirstName = @firstName
				,@MiddleNames = @middleNames
				,@LastName = @lastName
				,@AddressBlockForPrint = @addressBlockForPrint
				,@AddressBlockForHTML = @addressBlockForHTML
				,@FutureRegistrationLabel = @futureRegistrationLabel
				,@FutureRegistrationYear = @futureRegistrationYear
				,@FuturePracticeRegisterSID = @futurePracticeRegisterSID
				,@FuturePracticeRegisterLabel = @futurePracticeRegisterLabel
				,@FuturePracticeRegisterSectionSID = @futurePracticeRegisterSectionSID
				,@FutureRegisterSectionLabel = @futureRegisterSectionLabel
				,@FutureEffectiveTime = @futureEffectiveTime
				,@FutureExpiryTime = @futureExpiryTime
				,@FutureCardPrintedTime = @futureCardPrintedTime
				,@FutureInvoiceSID = @futureInvoiceSID
				,@FutureReasonSID = @futureReasonSID
				,@FutureFormGUID = @futureFormGUID
		
		end

		select
			 @registrationSID RegistrationSID
			,@registrantSID RegistrantSID
			,@practiceRegisterSectionSID PracticeRegisterSectionSID
			,@registrationNo RegistrationNo
			,@registrationYear RegistrationYear
			,@effectiveTime EffectiveTime
			,@expiryTime ExpiryTime
			,@cardPrintedTime CardPrintedTime
			,@invoiceSID InvoiceSID
			,@reasonSID ReasonSID
			,@formGUID FormGUID
			,@userDefinedColumns UserDefinedColumns
			,@registrationXID RegistrationXID
			,@legacyKey LegacyKey
			,@isDeleted IsDeleted
			,@createUser CreateUser
			,@createTime CreateTime
			,@updateUser UpdateUser
			,@updateTime UpdateTime
			,@rowGUID RowGUID
			,@rowStamp RowStamp
			,@practiceRegisterSID PracticeRegisterSID
			,@practiceRegisterSectionLabel PracticeRegisterSectionLabel
			,@practiceRegisterSectionIsDefault PracticeRegisterSectionIsDefault
			,@isDisplayedOnLicense IsDisplayedOnLicense
			,@practiceRegisterSectionIsActive PracticeRegisterSectionIsActive
			,@practiceRegisterSectionRowGUID PracticeRegisterSectionRowGUID
			,@registrantPersonSID RegistrantPersonSID
			,@registrantNo RegistrantNo
			,@yearOfInitialEmployment YearOfInitialEmployment
			,@isOnPublicRegistry IsOnPublicRegistry
			,@cityNameOfBirth CityNameOfBirth
			,@countrySID CountrySID
			,@directedAuditYearCompetence DirectedAuditYearCompetence
			,@directedAuditYearPracticeHours DirectedAuditYearPracticeHours
			,@lateFeeExclusionYear LateFeeExclusionYear
			,@isRenewalAutoApprovalBlocked IsRenewalAutoApprovalBlocked
			,@renewalExtensionExpiryTime RenewalExtensionExpiryTime
			,@archivedTime ArchivedTime
			,@registrantRowGUID RegistrantRowGUID
			,@invoicePersonSID InvoicePersonSID
			,@invoiceDate InvoiceDate
			,@tax1Label Tax1Label
			,@tax1Rate Tax1Rate
			,@tax1GLAccountCode Tax1GLAccountCode
			,@tax2Label Tax2Label
			,@tax2Rate Tax2Rate
			,@tax2GLAccountCode Tax2GLAccountCode
			,@tax3Label Tax3Label
			,@tax3Rate Tax3Rate
			,@tax3GLAccountCode Tax3GLAccountCode
			,@invoiceRegistrationYear InvoiceRegistrationYear
			,@cancelledTime CancelledTime
			,@invoiceReasonSID InvoiceReasonSID
			,@isRefund IsRefund
			,@complaintSID ComplaintSID
			,@invoiceRowGUID InvoiceRowGUID
			,@reasonGroupSID ReasonGroupSID
			,@reasonName ReasonName
			,@reasonCode ReasonCode
			,@reasonSequence ReasonSequence
			,@toolTip ToolTip
			,@reasonIsActive ReasonIsActive
			,@reasonRowGUID ReasonRowGUID
			,@isActive IsActive
			,@isPending IsPending
			,@isDeleteEnabled IsDeleteEnabled
			,@isReselected IsReselected
			,@isNullApplied IsNullApplied
			,@zContext zContext
			,@registrantLabel RegistrantLabel
			,@registrationYearLabel RegistrationYearLabel
			,@practiceRegisterName PracticeRegisterName
			,@practiceRegisterLabel PracticeRegisterLabel
			,@registrationLabel RegistrationLabel
			,@isReadEnabled IsReadEnabled
			,@firstName FirstName
			,@middleNames MiddleNames
			,@lastName LastName
			,@addressBlockForPrint AddressBlockForPrint
			,@addressBlockForHTML AddressBlockForHTML
			,@futureRegistrationLabel FutureRegistrationLabel
			,@futureRegistrationYear FutureRegistrationYear
			,@futurePracticeRegisterSID FuturePracticeRegisterSID
			,@futurePracticeRegisterLabel FuturePracticeRegisterLabel
			,@futurePracticeRegisterSectionSID FuturePracticeRegisterSectionSID
			,@futureRegisterSectionLabel FutureRegisterSectionLabel
			,@futureEffectiveTime FutureEffectiveTime
			,@futureExpiryTime FutureExpiryTime
			,@futureCardPrintedTime FutureCardPrintedTime
			,@futureInvoiceSID FutureInvoiceSID
			,@futureReasonSID FutureReasonSID
			,@futureFormGUID FutureFormGUID

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
