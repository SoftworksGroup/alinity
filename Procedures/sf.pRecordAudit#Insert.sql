SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [sf].[pRecordAudit#Insert]
	 @RecordAuditSID                 int               = null output				-- identity value assigned to the new record
	,@ApplicationUserSessionSID      int               = null								-- required! if not passed value must be set in custom logic prior to insert
	,@ApplicationPageSID             int               = null								-- required! if not passed value must be set in custom logic prior to insert
	,@AuditActionSID                 int               = null								-- required! if not passed value must be set in custom logic prior to insert
	,@IsGlassBreak                   bit               = null								-- default: (0)
	,@ContextGUID                    uniqueidentifier  = null								-- required! if not passed value must be set in custom logic prior to insert
	,@UserDefinedColumns             xml               = null								
	,@RecordAuditXID                 varchar(150)      = null								
	,@LegacyKey                      nvarchar(50)      = null								
	,@CreateUser                     nvarchar(75)      = null								-- default: suser_sname()
	,@IsReselected                   tinyint           = null								-- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@zContext                       xml               = null								-- other values defining context for the insert (if any)
	,@ApplicationPageLabel           nvarchar(35)      = null								-- not a base table column (default ignored)
	,@ApplicationPageURI             varchar(150)      = null								-- not a base table column (default ignored)
	,@ApplicationRoute               varchar(150)      = null								-- not a base table column (default ignored)
	,@IsSearchPage                   bit               = null								-- not a base table column (default ignored)
	,@ApplicationEntitySID           int               = null								-- not a base table column (default ignored)
	,@ApplicationPageRowGUID         uniqueidentifier  = null								-- not a base table column (default ignored)
	,@ApplicationUserSID             int               = null								-- not a base table column (default ignored)
	,@ApplicationUserSessionIsActive bit               = null								-- not a base table column (default ignored)
	,@IPAddress                      varchar(45)       = null								-- not a base table column (default ignored)
	,@ApplicationUserSessionRowGUID  uniqueidentifier  = null								-- not a base table column (default ignored)
	,@AuditActionSCD                 varchar(25)       = null								-- not a base table column (default ignored)
	,@AuditActionName                nvarchar(50)      = null								-- not a base table column (default ignored)
	,@AuditActionIsDefault           bit               = null								-- not a base table column (default ignored)
	,@AuditActionRowGUID             uniqueidentifier  = null								-- not a base table column (default ignored)
	,@IsDeleteEnabled                bit               = null								-- not a base table column (default ignored)
	,@PersonSID                      int               = null								-- not a base table column (default ignored)
	,@UserName                       nvarchar(75)      = null								-- not a base table column (default ignored)
	,@LastReviewTime                 datetimeoffset(7) = null								-- not a base table column (default ignored)
	,@LastReviewUser                 nvarchar(75)      = null								-- not a base table column (default ignored)
	,@ApplicationEntitySCD           varchar(50)       = null								-- not a base table column (default ignored)
	,@AuditTimeClientTZ              varchar(19)       = null								-- not a base table column (default ignored)
as
/*********************************************************************************************************************************
Procedure : sf.pRecordAudit#Insert
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : inserts 1 row into the sf.RecordAudit table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : March 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used in maintenance of the sf.RecordAudit table with parameter values represent columns of the table. Additional
parameters are provided for all columns in the vRecordAudit entity view, however, the base logic of the procedure inserts the
the columns of the table only.

Note: This procedure cannot be called directly from the MS Entity Framework! Use the wrapper procedure instead (...#EFInsert) that
excludes the primary key output parameter, but includes all other parameters.

Table-specific logic can be added through tagged sections (pre and post insert) and a call to an extended procedure supports
configuration (client) specific logic. Code implemented within code tags (table-specific logic) is part of the base product and
applies to all client configurations. Calls to the extended procedure occurs immediately after the table-specific logic in both
"pre-insert" and "post-insert" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pRecordAudit procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "insert.pre" or "insert.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls whether the entity row is returned as a dataset (SELECT). There are 3 settings:
   0 - no data set is returned
   1 - return the full entity
   2 - return only the SID (primary key) of the row inserted

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless a database default exists.  The default values
displayed as comments next to the parameter declarations above, and the list of columns returned from the entity view when
@IsReselected = 1, were obtained from the data dictionary at generation time. If the table or view design has been
updated since then, the procedure must be regenerated to keep comments up to date. In the StudioDB run dbo.pEFGen
to update all views and procedures which appear out-of-date.

The procedure does not accept a parameter for UpdateUser since the @CreateUser value is applied into both the user audit
columns.  Audit times are set automatically through database defaults and cannot be passed or overwritten.

If the @CreateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated inserts the user would not recognize as have caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

Business rule compliance is checked through a table constraint which calls fRecordAuditCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	set @RecordAuditSID = null																							-- initialize output parameter

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- remove leading and trailing spaces from character type columns

		set @RecordAuditXID = ltrim(rtrim(@RecordAuditXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @CreateUser = ltrim(rtrim(@CreateUser))
		set @ApplicationPageLabel = ltrim(rtrim(@ApplicationPageLabel))
		set @ApplicationPageURI = ltrim(rtrim(@ApplicationPageURI))
		set @ApplicationRoute = ltrim(rtrim(@ApplicationRoute))
		set @IPAddress = ltrim(rtrim(@IPAddress))
		set @AuditActionSCD = ltrim(rtrim(@AuditActionSCD))
		set @AuditActionName = ltrim(rtrim(@AuditActionName))
		set @UserName = ltrim(rtrim(@UserName))
		set @LastReviewUser = ltrim(rtrim(@LastReviewUser))
		set @ApplicationEntitySCD = ltrim(rtrim(@ApplicationEntitySCD))
		set @AuditTimeClientTZ = ltrim(rtrim(@AuditTimeClientTZ))

		-- set zero length strings to null to avoid storing them in the record

		if len(@RecordAuditXID) = 0 set @RecordAuditXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@CreateUser) = 0 set @CreateUser = null
		if len(@ApplicationPageLabel) = 0 set @ApplicationPageLabel = null
		if len(@ApplicationPageURI) = 0 set @ApplicationPageURI = null
		if len(@ApplicationRoute) = 0 set @ApplicationRoute = null
		if len(@IPAddress) = 0 set @IPAddress = null
		if len(@AuditActionSCD) = 0 set @AuditActionSCD = null
		if len(@AuditActionName) = 0 set @AuditActionName = null
		if len(@UserName) = 0 set @UserName = null
		if len(@LastReviewUser) = 0 set @LastReviewUser = null
		if len(@ApplicationEntitySCD) = 0 set @ApplicationEntitySCD = null
		if len(@AuditTimeClientTZ) = 0 set @AuditTimeClientTZ = null

		-- set the ID of the user

		if isnull(@CreateUser, 'x') = N'SystemUser' set @CreateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@CreateUser, 'x') <> N'SystemUser' set @CreateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- reset defaults on table parameters passed as NULL

		set @IsGlassBreak = isnull(@IsGlassBreak,(0))
		set @CreateUser = isnull(@CreateUser,suser_sname())
		set @IsReselected              = isnull(@IsReselected             ,(0))
		
		-- look up the FK value matching the system code if set; this allows
		-- FK values value to be set/updated based on system code values
		
		if @AuditActionSCD is not null
		begin
		
			select
				@AuditActionSID = x.AuditActionSID
			from
				sf.AuditAction x
			where
				x.AuditActionSCD = @AuditActionSCD
		
		end
		
		-- set default value on mandatory foreign keys where configured
		-- and if value not already assigned
		
		if @AuditActionSID  is null select @AuditActionSID  = x.AuditActionSID from sf.AuditAction  x where x.IsDefault = @ON

		-- apply the table-specific pre-insert logic (if any)

    --! <PreInsert>
    -- Tim Edlund | Nov 2012
    -- If SID values are not provided for the Application Page or Audit Action,
    -- look them up based on code values provided.  If no entry has been made
    -- in sf.ApplicationPage for the URI provided, add it on the fly.

    if @ApplicationUserSessionSID is null set @ApplicationUserSessionSID = sf.fApplicationUserSessionUserSID() -- in case user session not provided, set to current

		if @AuditActionSCD is null and @AuditActionSID is null																					-- if not action was provided, look for a default
		begin

			select
				@AuditActionSID = aa.AuditActionSID
			from
				sf.AuditAction aa
			where
				aa.IsDefault = cast(1 as bit)

		end

    if isnull(@ApplicationPageSID,-1) = -1 and @ApplicationPageURI is not null                      -- application page SID not provided: look up based on URI
    begin

      select
        @ApplicationPageSID = ap.ApplicationPageSID
      from
        sf.ApplicationPage ap
      where
        ap.ApplicationPageURI = @ApplicationPageURI

    end
    --! </PreInsert>

		-- insert the record

		insert
			sf.RecordAudit
		(
			 ApplicationUserSessionSID
			,ApplicationPageSID
			,AuditActionSID
			,IsGlassBreak
			,ContextGUID
			,UserDefinedColumns
			,RecordAuditXID
			,LegacyKey
			,CreateUser
			,UpdateUser
		)
		select
			 @ApplicationUserSessionSID
			,@ApplicationPageSID
			,@AuditActionSID
			,@IsGlassBreak
			,@ContextGUID
			,@UserDefinedColumns
			,@RecordAuditXID
			,@LegacyKey
			,@CreateUser
			,@CreateUser

		select
			 @rowsAffected   = @@rowcount
			,@RecordAuditSID = scope_identity()																	-- capture key value of new row

		-- check for errors

		if @rowsAffected <> 1																									-- ensure 1 row was inserted
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The "%1" operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'insert'
				,@Arg2        = 'sf.RecordAudit'
				,@Arg3        = @rowsAffected
				,@Arg4        = @RecordAuditSID
			
			raiserror(@errorText, 18, 1)
			
		end

		-- apply the table-specific post-insert logic (if any)

		--! <PostInsert>
		--  insert post-insert logic here ...
		--! </PostInsert>

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.RecordAuditSID
			from
				sf.vRecordAudit ent
			where
				ent.RecordAuditSID = @RecordAuditSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.RecordAuditSID
				,ent.ApplicationUserSessionSID
				,ent.ApplicationPageSID
				,ent.AuditActionSID
				,ent.IsGlassBreak
				,ent.ContextGUID
				,ent.UserDefinedColumns
				,ent.RecordAuditXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.ApplicationPageLabel
				,ent.ApplicationPageURI
				,ent.ApplicationRoute
				,ent.IsSearchPage
				,ent.ApplicationEntitySID
				,ent.ApplicationPageRowGUID
				,ent.ApplicationUserSID
				,ent.ApplicationUserSessionIsActive
				,ent.IPAddress
				,ent.ApplicationUserSessionRowGUID
				,ent.AuditActionSCD
				,ent.AuditActionName
				,ent.AuditActionIsDefault
				,ent.AuditActionRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
				,ent.PersonSID
				,ent.UserName
				,ent.LastReviewTime
				,ent.LastReviewUser
				,ent.ApplicationEntitySCD
				,ent.AuditTimeClientTZ
			from
				sf.vRecordAudit ent
			where
				ent.RecordAuditSID = @RecordAuditSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
