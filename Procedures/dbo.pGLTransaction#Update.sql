SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pGLTransaction#Update]
	 @GLTransactionSID            int               = null -- required! id of row to update - must be set in custom logic if not passed
	,@PaymentSID                  int               = null -- table column values to update:
	,@InvoicePaymentSID           int               = null
	,@DebitGLAccountCode          varchar(50)       = null
	,@CreditGLAccountCode         varchar(50)       = null
	,@Amount                      decimal(11,2)     = null
	,@GLPostingDate               date              = null
	,@PaymentCheckSum             int               = null
	,@InvoicePaymentCheckSum      int               = null
	,@ReversedGLTransactionSID    int               = null
	,@IsExcluded                  bit               = null
	,@UserDefinedColumns          xml               = null
	,@GLTransactionXID            varchar(150)      = null
	,@LegacyKey                   nvarchar(50)      = null
	,@UpdateUser                  nvarchar(75)      = null -- set to current application user unless "SystemUser" passed
	,@RowStamp                    timestamp         = null -- row time stamp - pass for preemptive check for overwrites
	,@IsReselected                tinyint           = 0    -- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@IsNullApplied               bit               = 0    -- when 1 null parameters overwrite corresponding columns with null
	,@zContext                    xml               = null -- other values defining context for the update (if any)
	,@PersonSID                   int               = null -- not a base table column
	,@PaymentTypeSID              int               = null -- not a base table column
	,@PaymentStatusSID            int               = null -- not a base table column
	,@GLAccountCode               varchar(50)       = null -- not a base table column
	,@PaymentGLPostingDate        date              = null -- not a base table column
	,@DepositDate                 date              = null -- not a base table column
	,@AmountPaid                  decimal(11,2)     = null -- not a base table column
	,@Reference                   varchar(25)       = null -- not a base table column
	,@NameOnCard                  nvarchar(150)     = null -- not a base table column
	,@PaymentCard                 varchar(20)       = null -- not a base table column
	,@TransactionID               varchar(50)       = null -- not a base table column
	,@LastResponseCode            varchar(50)       = null -- not a base table column
	,@VerifiedTime                datetime          = null -- not a base table column
	,@PaymentCancelledTime        datetimeoffset(7) = null -- not a base table column
	,@PaymentReasonSID            int               = null -- not a base table column
	,@PaymentRowGUID              uniqueidentifier  = null -- not a base table column
	,@InvoiceSID                  int               = null -- not a base table column
	,@InvoicePaymentPaymentSID    int               = null -- not a base table column
	,@AmountApplied               decimal(11,2)     = null -- not a base table column
	,@AppliedDate                 date              = null -- not a base table column
	,@InvoicePaymentGLPostingDate date              = null -- not a base table column
	,@InvoicePaymentCancelledTime datetimeoffset(7) = null -- not a base table column
	,@InvoicePaymentReasonSID     int               = null -- not a base table column
	,@InvoicePaymentRowGUID       uniqueidentifier  = null -- not a base table column
	,@IsDeleteEnabled             bit               = null -- not a base table column
	,@IsEditEnabled               bit               = null -- not a base table column
	,@FullDebitAccountLabel       nvarchar(86)      = null -- not a base table column
	,@FullCreditAccountLabel      nvarchar(86)      = null -- not a base table column
	,@IsReversing                 bit               = null -- not a base table column
	,@IsReversed                  bit               = null -- not a base table column
as
/*********************************************************************************************************************************
Procedure : dbo.pGLTransaction#Update
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : updates 1 row in the dbo.GLTransaction table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : March 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used to update the dbo.GLTransaction table. The procedure requires a primary key to locate the record to update.
Additional parameters are provided for all columns in the vGLTransaction entity view, however, the base logic of the procedure
updates the columns of the table only. Table-specific logic can be added through tagged sections (pre and post update) and a call
to an extended procedure supports client-specific logic. Logic implemented within code tags (table-specific logic) is part of the
base product and applies to all client configurations. Calls to the extended procedure occur immediately after the table-specific
logic in both "pre-update" and "post-update" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pGLTransaction procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "update.pre" or "update.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls output and "@IsNullApplied" controls whether or not parameters with null values overwrite
corresponding columns on the row.

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless @IsNullApplied is passed as 0. If @IsNullApplied = 1
any parameter with a null value overwrites the corresponding column value with null.  @IsNullApplied defaults to 0 but should be
passed as 1 when calling through the entity framework domain service since all columns are mapped to the procedure.

If the @UpdateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated updates the user would not recognize as having caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

The @RowStamp parameter should always be passed when calling from the user interface. The @RowStamp parameter is used to
preemptively check for an overwrite.  The value should be passed as the RowStamp value from the row when it was last
retrieved into the UI. If the RowStamp on the record changes from the value passed, this procedure will raise an exception and
avoid the overwrite.  For calls from back-end procedures, the @RowStamp parameter can be left blank and it will default to the
current time stamp on the record (avoiding the need to look up the value prior to calling.)

Business rule compliance is checked through a table constraint which calls fGLTransactionCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- check parameters

		if @GLTransactionSID is null
		begin

			exec sf.pMessage#Get
				 @MessageSCD  	= 'BlankParameter'
				,@MessageText 	= @errorText output
				,@DefaultText 	= N'A parameter (%1) required by the database procedure was left blank.'
				,@Arg1					= '@GLTransactionSID'

			raiserror(@errorText, 18, 1)
		end

		-- remove leading and trailing spaces from character type columns

		set @DebitGLAccountCode = ltrim(rtrim(@DebitGLAccountCode))
		set @CreditGLAccountCode = ltrim(rtrim(@CreditGLAccountCode))
		set @GLTransactionXID = ltrim(rtrim(@GLTransactionXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @UpdateUser = ltrim(rtrim(@UpdateUser))
		set @GLAccountCode = ltrim(rtrim(@GLAccountCode))
		set @Reference = ltrim(rtrim(@Reference))
		set @NameOnCard = ltrim(rtrim(@NameOnCard))
		set @PaymentCard = ltrim(rtrim(@PaymentCard))
		set @TransactionID = ltrim(rtrim(@TransactionID))
		set @LastResponseCode = ltrim(rtrim(@LastResponseCode))
		set @FullDebitAccountLabel = ltrim(rtrim(@FullDebitAccountLabel))
		set @FullCreditAccountLabel = ltrim(rtrim(@FullCreditAccountLabel))

		-- set zero length strings to null to avoid storing them in the record

		if len(@DebitGLAccountCode) = 0 set @DebitGLAccountCode = null
		if len(@CreditGLAccountCode) = 0 set @CreditGLAccountCode = null
		if len(@GLTransactionXID) = 0 set @GLTransactionXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@UpdateUser) = 0 set @UpdateUser = null
		if len(@GLAccountCode) = 0 set @GLAccountCode = null
		if len(@Reference) = 0 set @Reference = null
		if len(@NameOnCard) = 0 set @NameOnCard = null
		if len(@PaymentCard) = 0 set @PaymentCard = null
		if len(@TransactionID) = 0 set @TransactionID = null
		if len(@LastResponseCode) = 0 set @LastResponseCode = null
		if len(@FullDebitAccountLabel) = 0 set @FullDebitAccountLabel = null
		if len(@FullCreditAccountLabel) = 0 set @FullCreditAccountLabel = null

		-- set the ID of the user

		if isnull(@UpdateUser, 'x') = N'SystemUser' set @UpdateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@UpdateUser, 'x') <> N'SystemUser' set @UpdateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- avoid overwriting with null parameter values (unless specified)
		-- by retrieving existing values from the entity row for blank parameters

		if @IsNullApplied = 0
		begin

			select
				 @PaymentSID                  = isnull(@PaymentSID,glt.PaymentSID)
				,@InvoicePaymentSID           = isnull(@InvoicePaymentSID,glt.InvoicePaymentSID)
				,@DebitGLAccountCode          = isnull(@DebitGLAccountCode,glt.DebitGLAccountCode)
				,@CreditGLAccountCode         = isnull(@CreditGLAccountCode,glt.CreditGLAccountCode)
				,@Amount                      = isnull(@Amount,glt.Amount)
				,@GLPostingDate               = isnull(@GLPostingDate,glt.GLPostingDate)
				,@PaymentCheckSum             = isnull(@PaymentCheckSum,glt.PaymentCheckSum)
				,@InvoicePaymentCheckSum      = isnull(@InvoicePaymentCheckSum,glt.InvoicePaymentCheckSum)
				,@ReversedGLTransactionSID    = isnull(@ReversedGLTransactionSID,glt.ReversedGLTransactionSID)
				,@IsExcluded                  = isnull(@IsExcluded,glt.IsExcluded)
				,@UserDefinedColumns          = isnull(@UserDefinedColumns,glt.UserDefinedColumns)
				,@GLTransactionXID            = isnull(@GLTransactionXID,glt.GLTransactionXID)
				,@LegacyKey                   = isnull(@LegacyKey,glt.LegacyKey)
				,@UpdateUser                  = isnull(@UpdateUser,glt.UpdateUser)
				,@IsReselected                = isnull(@IsReselected,glt.IsReselected)
				,@IsNullApplied               = isnull(@IsNullApplied,glt.IsNullApplied)
				,@zContext                    = isnull(@zContext,glt.zContext)
				,@PersonSID                   = isnull(@PersonSID,glt.PersonSID)
				,@PaymentTypeSID              = isnull(@PaymentTypeSID,glt.PaymentTypeSID)
				,@PaymentStatusSID            = isnull(@PaymentStatusSID,glt.PaymentStatusSID)
				,@GLAccountCode               = isnull(@GLAccountCode,glt.GLAccountCode)
				,@PaymentGLPostingDate        = isnull(@PaymentGLPostingDate,glt.PaymentGLPostingDate)
				,@DepositDate                 = isnull(@DepositDate,glt.DepositDate)
				,@AmountPaid                  = isnull(@AmountPaid,glt.AmountPaid)
				,@Reference                   = isnull(@Reference,glt.Reference)
				,@NameOnCard                  = isnull(@NameOnCard,glt.NameOnCard)
				,@PaymentCard                 = isnull(@PaymentCard,glt.PaymentCard)
				,@TransactionID               = isnull(@TransactionID,glt.TransactionID)
				,@LastResponseCode            = isnull(@LastResponseCode,glt.LastResponseCode)
				,@VerifiedTime                = isnull(@VerifiedTime,glt.VerifiedTime)
				,@PaymentCancelledTime        = isnull(@PaymentCancelledTime,glt.PaymentCancelledTime)
				,@PaymentReasonSID            = isnull(@PaymentReasonSID,glt.PaymentReasonSID)
				,@PaymentRowGUID              = isnull(@PaymentRowGUID,glt.PaymentRowGUID)
				,@InvoiceSID                  = isnull(@InvoiceSID,glt.InvoiceSID)
				,@InvoicePaymentPaymentSID    = isnull(@InvoicePaymentPaymentSID,glt.InvoicePaymentPaymentSID)
				,@AmountApplied               = isnull(@AmountApplied,glt.AmountApplied)
				,@AppliedDate                 = isnull(@AppliedDate,glt.AppliedDate)
				,@InvoicePaymentGLPostingDate = isnull(@InvoicePaymentGLPostingDate,glt.InvoicePaymentGLPostingDate)
				,@InvoicePaymentCancelledTime = isnull(@InvoicePaymentCancelledTime,glt.InvoicePaymentCancelledTime)
				,@InvoicePaymentReasonSID     = isnull(@InvoicePaymentReasonSID,glt.InvoicePaymentReasonSID)
				,@InvoicePaymentRowGUID       = isnull(@InvoicePaymentRowGUID,glt.InvoicePaymentRowGUID)
				,@IsDeleteEnabled             = isnull(@IsDeleteEnabled,glt.IsDeleteEnabled)
				,@IsEditEnabled               = isnull(@IsEditEnabled,glt.IsEditEnabled)
				,@FullDebitAccountLabel       = isnull(@FullDebitAccountLabel,glt.FullDebitAccountLabel)
				,@FullCreditAccountLabel      = isnull(@FullCreditAccountLabel,glt.FullCreditAccountLabel)
				,@IsReversing                 = isnull(@IsReversing,glt.IsReversing)
				,@IsReversed                  = isnull(@IsReversed,glt.IsReversed)
			from
				dbo.vGLTransaction glt
			where
				glt.GLTransactionSID = @GLTransactionSID

		end

		-- apply the table-specific pre-update logic (if any)

		--! <PreUpdate>
		--  insert pre-update logic here ...
		--! </PreUpdate>
	
		-- call the extended version of the procedure (if it exists) for "update.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pGLTransaction'
		)
		begin
		
			exec @errorNo = ext.pGLTransaction
				 @Mode                        = 'update.pre'
				,@GLTransactionSID            = @GLTransactionSID
				,@PaymentSID                  = @PaymentSID output
				,@InvoicePaymentSID           = @InvoicePaymentSID output
				,@DebitGLAccountCode          = @DebitGLAccountCode output
				,@CreditGLAccountCode         = @CreditGLAccountCode output
				,@Amount                      = @Amount output
				,@GLPostingDate               = @GLPostingDate output
				,@PaymentCheckSum             = @PaymentCheckSum output
				,@InvoicePaymentCheckSum      = @InvoicePaymentCheckSum output
				,@ReversedGLTransactionSID    = @ReversedGLTransactionSID output
				,@IsExcluded                  = @IsExcluded output
				,@UserDefinedColumns          = @UserDefinedColumns output
				,@GLTransactionXID            = @GLTransactionXID output
				,@LegacyKey                   = @LegacyKey output
				,@UpdateUser                  = @UpdateUser
				,@RowStamp                    = @RowStamp
				,@IsReselected                = @IsReselected
				,@IsNullApplied               = @IsNullApplied
				,@zContext                    = @zContext
				,@PersonSID                   = @PersonSID
				,@PaymentTypeSID              = @PaymentTypeSID
				,@PaymentStatusSID            = @PaymentStatusSID
				,@GLAccountCode               = @GLAccountCode
				,@PaymentGLPostingDate        = @PaymentGLPostingDate
				,@DepositDate                 = @DepositDate
				,@AmountPaid                  = @AmountPaid
				,@Reference                   = @Reference
				,@NameOnCard                  = @NameOnCard
				,@PaymentCard                 = @PaymentCard
				,@TransactionID               = @TransactionID
				,@LastResponseCode            = @LastResponseCode
				,@VerifiedTime                = @VerifiedTime
				,@PaymentCancelledTime        = @PaymentCancelledTime
				,@PaymentReasonSID            = @PaymentReasonSID
				,@PaymentRowGUID              = @PaymentRowGUID
				,@InvoiceSID                  = @InvoiceSID
				,@InvoicePaymentPaymentSID    = @InvoicePaymentPaymentSID
				,@AmountApplied               = @AmountApplied
				,@AppliedDate                 = @AppliedDate
				,@InvoicePaymentGLPostingDate = @InvoicePaymentGLPostingDate
				,@InvoicePaymentCancelledTime = @InvoicePaymentCancelledTime
				,@InvoicePaymentReasonSID     = @InvoicePaymentReasonSID
				,@InvoicePaymentRowGUID       = @InvoicePaymentRowGUID
				,@IsDeleteEnabled             = @IsDeleteEnabled
				,@IsEditEnabled               = @IsEditEnabled
				,@FullDebitAccountLabel       = @FullDebitAccountLabel
				,@FullCreditAccountLabel      = @FullCreditAccountLabel
				,@IsReversing                 = @IsReversing
				,@IsReversed                  = @IsReversed
		
		end

		-- update the record

		update
			dbo.GLTransaction
		set
			 PaymentSID = @PaymentSID
			,InvoicePaymentSID = @InvoicePaymentSID
			,DebitGLAccountCode = @DebitGLAccountCode
			,CreditGLAccountCode = @CreditGLAccountCode
			,Amount = @Amount
			,GLPostingDate = @GLPostingDate
			,PaymentCheckSum = @PaymentCheckSum
			,InvoicePaymentCheckSum = @InvoicePaymentCheckSum
			,ReversedGLTransactionSID = @ReversedGLTransactionSID
			,IsExcluded = @IsExcluded
			,UserDefinedColumns = @UserDefinedColumns
			,GLTransactionXID = @GLTransactionXID
			,LegacyKey = @LegacyKey
			,UpdateUser = @UpdateUser
			,UpdateTime = sysdatetimeoffset()
		where
			GLTransactionSID = @GLTransactionSID
			and
			RowStamp = isnull(@RowStamp, RowStamp)

		set @rowsAffected = @@rowcount

		-- check for errors

		if @rowsAffected = 0
		begin
			
			if exists (select 1 from dbo.GLTransaction where GLTransactionSID = @gLTransactionSID)
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'UpdateBlocked'
					,@MessageText = @errorText output
					,@DefaultText = N'A change was made to the "%1" record since it was last retrieved. The overwrite was avoided. Refresh the record and try again.'
					,@Arg1        = 'dbo.GLTransaction'
				
				raiserror(@errorText, 16, 1)
			end
			else
			begin
				
				exec sf.pMessage#Get
					 @MessageSCD  = 'RecordNotFound'
					,@MessageText = @errorText output
					,@DefaultText = N'The %1 record was not found. Record ID = "%2". The record may have been deleted or the identifier is invalid.'
					,@Arg1        = 'dbo.GLTransaction'
					,@Arg2        = @gLTransactionSID
				
				raiserror(@errorText, 18, 1)
			end

		end
		else if @rowsAffected <> 1
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The %1 operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'update'
				,@Arg2        = 'dbo.GLTransaction'
				,@Arg3        = @rowsAffected
				,@Arg4        = @gLTransactionSID
			
			raiserror(@errorText, 18, 1)
		end

		-- apply the table-specific post-update logic (if any)

		--! <PostUpdate>
		--  insert post-update logic here ...
		--! </PostUpdate>
	
		-- call the extended version of the procedure for update.post - if it exists
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pGLTransaction'
		)
		begin
		
			exec @errorNo = ext.pGLTransaction
				 @Mode                        = 'update.post'
				,@GLTransactionSID            = @GLTransactionSID
				,@PaymentSID                  = @PaymentSID
				,@InvoicePaymentSID           = @InvoicePaymentSID
				,@DebitGLAccountCode          = @DebitGLAccountCode
				,@CreditGLAccountCode         = @CreditGLAccountCode
				,@Amount                      = @Amount
				,@GLPostingDate               = @GLPostingDate
				,@PaymentCheckSum             = @PaymentCheckSum
				,@InvoicePaymentCheckSum      = @InvoicePaymentCheckSum
				,@ReversedGLTransactionSID    = @ReversedGLTransactionSID
				,@IsExcluded                  = @IsExcluded
				,@UserDefinedColumns          = @UserDefinedColumns
				,@GLTransactionXID            = @GLTransactionXID
				,@LegacyKey                   = @LegacyKey
				,@UpdateUser                  = @UpdateUser
				,@RowStamp                    = @RowStamp
				,@IsReselected                = @IsReselected
				,@IsNullApplied               = @IsNullApplied
				,@zContext                    = @zContext
				,@PersonSID                   = @PersonSID
				,@PaymentTypeSID              = @PaymentTypeSID
				,@PaymentStatusSID            = @PaymentStatusSID
				,@GLAccountCode               = @GLAccountCode
				,@PaymentGLPostingDate        = @PaymentGLPostingDate
				,@DepositDate                 = @DepositDate
				,@AmountPaid                  = @AmountPaid
				,@Reference                   = @Reference
				,@NameOnCard                  = @NameOnCard
				,@PaymentCard                 = @PaymentCard
				,@TransactionID               = @TransactionID
				,@LastResponseCode            = @LastResponseCode
				,@VerifiedTime                = @VerifiedTime
				,@PaymentCancelledTime        = @PaymentCancelledTime
				,@PaymentReasonSID            = @PaymentReasonSID
				,@PaymentRowGUID              = @PaymentRowGUID
				,@InvoiceSID                  = @InvoiceSID
				,@InvoicePaymentPaymentSID    = @InvoicePaymentPaymentSID
				,@AmountApplied               = @AmountApplied
				,@AppliedDate                 = @AppliedDate
				,@InvoicePaymentGLPostingDate = @InvoicePaymentGLPostingDate
				,@InvoicePaymentCancelledTime = @InvoicePaymentCancelledTime
				,@InvoicePaymentReasonSID     = @InvoicePaymentReasonSID
				,@InvoicePaymentRowGUID       = @InvoicePaymentRowGUID
				,@IsDeleteEnabled             = @IsDeleteEnabled
				,@IsEditEnabled               = @IsEditEnabled
				,@FullDebitAccountLabel       = @FullDebitAccountLabel
				,@FullCreditAccountLabel      = @FullCreditAccountLabel
				,@IsReversing                 = @IsReversing
				,@IsReversed                  = @IsReversed
		
		end

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.GLTransactionSID
			from
				dbo.vGLTransaction ent
			where
				ent.GLTransactionSID = @GLTransactionSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.GLTransactionSID
				,ent.PaymentSID
				,ent.InvoicePaymentSID
				,ent.DebitGLAccountCode
				,ent.CreditGLAccountCode
				,ent.Amount
				,ent.GLPostingDate
				,ent.PaymentCheckSum
				,ent.InvoicePaymentCheckSum
				,ent.ReversedGLTransactionSID
				,ent.IsExcluded
				,ent.UserDefinedColumns
				,ent.GLTransactionXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.PersonSID
				,ent.PaymentTypeSID
				,ent.PaymentStatusSID
				,ent.GLAccountCode
				,ent.PaymentGLPostingDate
				,ent.DepositDate
				,ent.AmountPaid
				,ent.Reference
				,ent.NameOnCard
				,ent.PaymentCard
				,ent.TransactionID
				,ent.LastResponseCode
				,ent.VerifiedTime
				,ent.PaymentCancelledTime
				,ent.PaymentReasonSID
				,ent.PaymentRowGUID
				,ent.InvoiceSID
				,ent.InvoicePaymentPaymentSID
				,ent.AmountApplied
				,ent.AppliedDate
				,ent.InvoicePaymentGLPostingDate
				,ent.InvoicePaymentCancelledTime
				,ent.InvoicePaymentReasonSID
				,ent.InvoicePaymentRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
				,ent.IsEditEnabled
				,ent.FullDebitAccountLabel
				,ent.FullCreditAccountLabel
				,ent.IsReversing
				,ent.IsReversed
			from
				dbo.vGLTransaction ent
			where
				ent.GLTransactionSID = @GLTransactionSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
