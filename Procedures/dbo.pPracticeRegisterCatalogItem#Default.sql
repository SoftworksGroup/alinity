SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pPracticeRegisterCatalogItem#Default]
	 @zContext                                         xml               = null                          -- default values provided from client-tier (if any)
	,@SetFKDefaults                                    bit               = 0                             -- when 1, mandatory FK's are returned as -1 instead of NULL
as
/*********************************************************************************************************************************
Procedure : dbo.pPracticeRegisterCatalogItem#Default
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : provides a blank row with default values for presentation in the UI for "new" dbo.PracticeRegisterCatalogItem records
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used in maintenance of the dbo.PracticeRegisterCatalogItem table. When a new record is to be added from the UI, this procedure
is called to return a blank record with default values. If the client-tier is providing the context for the insert, such as a parent
key value for the new record, it must be passed in the @zContext XML parameter. Multiple values may be passed. The standard format
is: <Parameters MyParameter="1000001"/>.

The @SetFKDefaults parameter can be set to 1 to cause the procedure to return mandatory FK values as -1 rather than NULL. This avoids
the need to create complex types for the procedure on architectures which are not using RIA services.

Note that default values for text, ntext and binary type columns is not supported.  These data types are not permitted as local
variables in the current version of SQL Server and should be replaced by varchar(max) and nvarchar(max) where possible.

Some default values are built-in to the shell of the sproc.  The base table column defaults set in the variable declarations below
were obtained from database default constraints which existed at the time the procedure was generated. The declarations include all
columns of the vPracticeRegisterCatalogItem entity view, however, only some values (as noted above) are eligible for default setting.  The other
parameters are included for setting context for the table-specific or client-specific logic of the procedure (if any). Default values
returning a question mark "?", system date, or 0 are provided for non-base table columns which are mandatory.  This is done to avoid
compilation errors from the Entity Framework, however, the values will not be applied since they are not in the base table row.

Two levels of customization of the procedure shell are supported. Table-specific logic can be added through the tagged section and a
call to an extended procedure supports client-specific customization. Logic implemented within the code tags is part of the base
product and applies to all client configurations. Client-specific customizations must be implemented in the ext.pPracticeRegisterCatalogItem
procedure. The extended procedure is only called where it exists in database. The parameter "@Mode" is set to "default.pre" to
advise ext.pPracticeRegisterCatalogItem of the context of the call. All other parameters are also passed, however, only those parameters eligible
for default setting are passed for "output". All parameters corresponding to entity view columns are returned through a SELECT statement.

In order to simplify working with the XML parameter values, logic in the procedure parses the XML and assigns values to variables where
the variable name matches the column name in the XML (assumes single row).  The variables are then available to the table-specific and
client-specific logic.  The @zContext parameter is also passed, unmodified, to the extended procedure to support situations where values
are passed that are not mapped to column names.


-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@ON                                               bit = cast(1 as bit)													-- constant for bit comparisons
		,@OFF                                              bit = cast(0 as bit)													-- constant for bit comparisons
		,@practiceRegisterCatalogItemSID                   int               = -1												-- specific default required by EF - do not override
		,@practiceRegisterSID                              int               = null											-- no default provided from DB constraint - OK to override
		,@catalogItemSID                                   int               = null											-- no default provided from DB constraint - OK to override
		,@isAppliedOnApplication                           bit               = CONVERT(bit,(1))					-- default provided from DB constraint - OK to override
		,@isAppliedOnApplicationApproval                   bit               = CONVERT(bit,(0))					-- default provided from DB constraint - OK to override
		,@isAppliedOnRenewal                               bit               = CONVERT(bit,(0))					-- default provided from DB constraint - OK to override
		,@isAppliedOnReinstatement                         bit               = CONVERT(bit,(0))					-- default provided from DB constraint - OK to override
		,@isAppliedOnRegChange                             bit               = CONVERT(bit,(0))					-- default provided from DB constraint - OK to override
		,@isAppliedToPAPSubscribers                        bit               = (0)											-- default provided from DB constraint - OK to override
		,@practiceRegisterSectionSID                       int               = null											-- no default provided from DB constraint - OK to override
		,@practiceRegisterChangeSID                        int               = null											-- no default provided from DB constraint - OK to override
		,@feeSequence                                      smallint          = (10)											-- default provided from DB constraint - OK to override
		,@effectiveTime                                    datetime          = CONVERT(datetime,sf.fToday())-- default provided from DB constraint - OK to override
		,@expiryTime                                       datetime          = null											-- no default provided from DB constraint - OK to override
		,@userDefinedColumns                               xml               = null											-- no default provided from DB constraint - OK to override
		,@practiceRegisterCatalogItemXID                   varchar(150)      = null											-- no default provided from DB constraint - OK to override
		,@legacyKey                                        nvarchar(50)      = null											-- no default provided from DB constraint - OK to override
		,@isDeleted                                        bit               = (0)											-- default provided from DB constraint - OK to override
		,@createUser                                       nvarchar(75)      = suser_sname()						-- default value ignored (value set by UI)
		,@createTime                                       datetimeoffset(7) = sysdatetimeoffset()			-- default value ignored (set to system time)
		,@updateUser                                       nvarchar(75)      = suser_sname()						-- default value ignored (value set by UI)
		,@updateTime                                       datetimeoffset(7) = sysdatetimeoffset()			-- default value ignored (set to system time)
		,@rowGUID                                          uniqueidentifier  = newid()									-- default value ignored (value set by system)
		,@rowStamp                                         timestamp         = null											-- default value ignored (value set by system)
		,@catalogItemLabel                                 nvarchar(35)      = N'?'											-- not a base table column (default ignored)
		,@invoiceItemDescription                           nvarchar(500)     = N'?'											-- not a base table column (default ignored)
		,@isLateFee                                        bit               = 0												-- not a base table column (default ignored)
		,@imageAlternateText                               nvarchar(50)				-- not a base table column (default ignored)
		,@isAvailableOnClientPortal                        bit               = 0												-- not a base table column (default ignored)
		,@isComplaintPenalty                               bit               = 0												-- not a base table column (default ignored)
		,@gLAccountSID                                     int               = 0												-- not a base table column (default ignored)
		,@isTaxRate1Applied                                bit               = 0												-- not a base table column (default ignored)
		,@isTaxRate2Applied                                bit               = 0												-- not a base table column (default ignored)
		,@isTaxRate3Applied                                bit               = 0												-- not a base table column (default ignored)
		,@isTaxDeductible                                  bit               = 0												-- not a base table column (default ignored)
		,@catalogItemEffectiveTime                         datetime          = sf.fNow()								-- not a base table column (default ignored)
		,@catalogItemExpiryTime                            datetime						-- not a base table column (default ignored)
		,@fileTypeSCD                                      varchar(8)        = '?'											-- not a base table column (default ignored)
		,@fileTypeSID                                      int               = 0												-- not a base table column (default ignored)
		,@catalogItemRowGUID                               uniqueidentifier  = newid()									-- not a base table column (default ignored)
		,@practiceRegisterTypeSID                          int               = 0												-- not a base table column (default ignored)
		,@registrationScheduleSID                          int               = 0												-- not a base table column (default ignored)
		,@practiceRegisterName                             nvarchar(65)      = N'?'											-- not a base table column (default ignored)
		,@practiceRegisterLabel                            nvarchar(35)      = N'?'											-- not a base table column (default ignored)
		,@isActivePractice                                 bit               = 0												-- not a base table column (default ignored)
		,@isPublicRegistryEnabled                          bit               = 0												-- not a base table column (default ignored)
		,@isRenewalEnabled                                 bit               = 0												-- not a base table column (default ignored)
		,@isLearningPlanEnabled                            bit               = 0												-- not a base table column (default ignored)
		,@isNextCEFormAutoAdded                            bit               = 0												-- not a base table column (default ignored)
		,@isEligibleSupervisor                             bit               = 0												-- not a base table column (default ignored)
		,@isSupervisionRequired                            bit               = 0												-- not a base table column (default ignored)
		,@isEmploymentTerminated                           bit               = 0												-- not a base table column (default ignored)
		,@isGroupMembershipTerminated                      bit               = 0												-- not a base table column (default ignored)
		,@termPermitDays                                   int               = 0												-- not a base table column (default ignored)
		,@registerRank                                     smallint          = 0												-- not a base table column (default ignored)
		,@learningModelSID                                 int								-- not a base table column (default ignored)
		,@reasonGroupSID                                   int								-- not a base table column (default ignored)
		,@practiceRegisterIsDefault                        bit               = 0												-- not a base table column (default ignored)
		,@isDefaultInactivePractice                        bit               = 0												-- not a base table column (default ignored)
		,@practiceRegisterIsActive                         bit               = 0												-- not a base table column (default ignored)
		,@practiceRegisterRowGUID                          uniqueidentifier  = newid()									-- not a base table column (default ignored)
		,@practiceRegisterChangePracticeRegisterSID        int								-- not a base table column (default ignored)
		,@practiceRegisterChangePracticeRegisterSectionSID int								-- not a base table column (default ignored)
		,@practiceRegisterChangeIsActive                   bit								-- not a base table column (default ignored)
		,@toolTip                                          nvarchar(500)			-- not a base table column (default ignored)
		,@isEnabledForRegistrant                           bit								-- not a base table column (default ignored)
		,@practiceRegisterChangeRowGUID                    uniqueidentifier		-- not a base table column (default ignored)
		,@practiceRegisterSectionPracticeRegisterSID       int								-- not a base table column (default ignored)
		,@practiceRegisterSectionLabel                     nvarchar(35)				-- not a base table column (default ignored)
		,@practiceRegisterSectionIsDefault                 bit								-- not a base table column (default ignored)
		,@isDisplayedOnLicense                             bit								-- not a base table column (default ignored)
		,@practiceRegisterSectionIsActive                  bit								-- not a base table column (default ignored)
		,@practiceRegisterSectionRowGUID                   uniqueidentifier		-- not a base table column (default ignored)
		,@isActive                                         bit								-- not a base table column (default ignored)
		,@isPending                                        bit								-- not a base table column (default ignored)
		,@isDeleteEnabled                                  bit								-- not a base table column (default ignored)
		,@isReselected                                     tinyint           = 1												-- specific default required by EF - do not override
		,@isNullApplied                                    bit               = 1												-- specific default required by EF - do not override

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end
		-- set mandatory FK values to -1 where requested
		
		if @SetFKDefaults = @ON
		begin
			set @practiceRegisterSID = -1
			set @catalogItemSID = -1
		end

		-- assign literal defaults passed through @zContext where
		-- provided otherwise leave database default in place
		
		select
			 @practiceRegisterSID             = isnull(context.node.value('@PracticeRegisterSID'           ,'int'         ),@practiceRegisterSID)
			,@catalogItemSID                  = isnull(context.node.value('@CatalogItemSID'                ,'int'         ),@catalogItemSID)
			,@isAppliedOnApplication          = isnull(context.node.value('@IsAppliedOnApplication'        ,'bit'         ),@isAppliedOnApplication)
			,@isAppliedOnApplicationApproval  = isnull(context.node.value('@IsAppliedOnApplicationApproval','bit'         ),@isAppliedOnApplicationApproval)
			,@isAppliedOnRenewal              = isnull(context.node.value('@IsAppliedOnRenewal'            ,'bit'         ),@isAppliedOnRenewal)
			,@isAppliedOnReinstatement        = isnull(context.node.value('@IsAppliedOnReinstatement'      ,'bit'         ),@isAppliedOnReinstatement)
			,@isAppliedOnRegChange            = isnull(context.node.value('@IsAppliedOnRegChange'          ,'bit'         ),@isAppliedOnRegChange)
			,@isAppliedToPAPSubscribers       = isnull(context.node.value('@IsAppliedToPAPSubscribers'     ,'bit'         ),@isAppliedToPAPSubscribers)
			,@practiceRegisterSectionSID      = isnull(context.node.value('@PracticeRegisterSectionSID'    ,'int'         ),@practiceRegisterSectionSID)
			,@practiceRegisterChangeSID       = isnull(context.node.value('@PracticeRegisterChangeSID'     ,'int'         ),@practiceRegisterChangeSID)
			,@feeSequence                     = isnull(context.node.value('@FeeSequence'                   ,'smallint'    ),@feeSequence)
			,@effectiveTime                   = isnull(context.node.value('@EffectiveTime'                 ,'datetime'    ),@effectiveTime)
			,@expiryTime                      = isnull(context.node.value('@ExpiryTime'                    ,'datetime'    ),@expiryTime)
			,@practiceRegisterCatalogItemXID  = isnull(context.node.value('@PracticeRegisterCatalogItemXID','varchar(150)'),@practiceRegisterCatalogItemXID)
			,@legacyKey                       = isnull(context.node.value('@LegacyKey'                     ,'nvarchar(50)'),@legacyKey)
		from
			@zContext.nodes('Parameters') as context(node)
		
		-- set default value on foreign keys where configured
		-- and where no DB or literal value was passed for it
		
		if isnull(@practiceRegisterSID       ,0) = 0 select @practiceRegisterSID        = x.PracticeRegisterSID        from dbo.PracticeRegister        x where x.IsDefault = @ON
		if isnull(@practiceRegisterSectionSID,0) = 0 select @practiceRegisterSectionSID = x.PracticeRegisterSectionSID from dbo.PracticeRegisterSection x where x.IsDefault = @ON and x.PracticeRegisterSID = @practiceRegisterSID

		--! <Overrides>
		--  insert default value logic here ...
		--! </Overrides>
	
		-- call the extended version of the procedure (if it exists) for "default.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pPracticeRegisterCatalogItem'
		)
		begin
		
			exec @errorNo = ext.pPracticeRegisterCatalogItem
				 @Mode                                             = 'default.pre'
				,@PracticeRegisterCatalogItemSID = @practiceRegisterCatalogItemSID
				,@PracticeRegisterSID = @practiceRegisterSID output
				,@CatalogItemSID = @catalogItemSID output
				,@IsAppliedOnApplication = @isAppliedOnApplication output
				,@IsAppliedOnApplicationApproval = @isAppliedOnApplicationApproval output
				,@IsAppliedOnRenewal = @isAppliedOnRenewal output
				,@IsAppliedOnReinstatement = @isAppliedOnReinstatement output
				,@IsAppliedOnRegChange = @isAppliedOnRegChange output
				,@IsAppliedToPAPSubscribers = @isAppliedToPAPSubscribers output
				,@PracticeRegisterSectionSID = @practiceRegisterSectionSID output
				,@PracticeRegisterChangeSID = @practiceRegisterChangeSID output
				,@FeeSequence = @feeSequence output
				,@EffectiveTime = @effectiveTime output
				,@ExpiryTime = @expiryTime output
				,@UserDefinedColumns = @userDefinedColumns output
				,@PracticeRegisterCatalogItemXID = @practiceRegisterCatalogItemXID output
				,@LegacyKey = @legacyKey output
				,@IsDeleted = @isDeleted
				,@CreateUser = @createUser
				,@CreateTime = @createTime
				,@UpdateUser = @updateUser
				,@UpdateTime = @updateTime
				,@RowGUID = @rowGUID
				,@RowStamp = @rowStamp
				,@CatalogItemLabel = @catalogItemLabel
				,@InvoiceItemDescription = @invoiceItemDescription
				,@IsLateFee = @isLateFee
				,@ImageAlternateText = @imageAlternateText
				,@IsAvailableOnClientPortal = @isAvailableOnClientPortal
				,@IsComplaintPenalty = @isComplaintPenalty
				,@GLAccountSID = @gLAccountSID
				,@IsTaxRate1Applied = @isTaxRate1Applied
				,@IsTaxRate2Applied = @isTaxRate2Applied
				,@IsTaxRate3Applied = @isTaxRate3Applied
				,@IsTaxDeductible = @isTaxDeductible
				,@CatalogItemEffectiveTime = @catalogItemEffectiveTime
				,@CatalogItemExpiryTime = @catalogItemExpiryTime
				,@FileTypeSCD = @fileTypeSCD
				,@FileTypeSID = @fileTypeSID
				,@CatalogItemRowGUID = @catalogItemRowGUID
				,@PracticeRegisterTypeSID = @practiceRegisterTypeSID
				,@RegistrationScheduleSID = @registrationScheduleSID
				,@PracticeRegisterName = @practiceRegisterName
				,@PracticeRegisterLabel = @practiceRegisterLabel
				,@IsActivePractice = @isActivePractice
				,@IsPublicRegistryEnabled = @isPublicRegistryEnabled
				,@IsRenewalEnabled = @isRenewalEnabled
				,@IsLearningPlanEnabled = @isLearningPlanEnabled
				,@IsNextCEFormAutoAdded = @isNextCEFormAutoAdded
				,@IsEligibleSupervisor = @isEligibleSupervisor
				,@IsSupervisionRequired = @isSupervisionRequired
				,@IsEmploymentTerminated = @isEmploymentTerminated
				,@IsGroupMembershipTerminated = @isGroupMembershipTerminated
				,@TermPermitDays = @termPermitDays
				,@RegisterRank = @registerRank
				,@LearningModelSID = @learningModelSID
				,@ReasonGroupSID = @reasonGroupSID
				,@PracticeRegisterIsDefault = @practiceRegisterIsDefault
				,@IsDefaultInactivePractice = @isDefaultInactivePractice
				,@PracticeRegisterIsActive = @practiceRegisterIsActive
				,@PracticeRegisterRowGUID = @practiceRegisterRowGUID
				,@PracticeRegisterChangePracticeRegisterSID = @practiceRegisterChangePracticeRegisterSID
				,@PracticeRegisterChangePracticeRegisterSectionSID = @practiceRegisterChangePracticeRegisterSectionSID
				,@PracticeRegisterChangeIsActive = @practiceRegisterChangeIsActive
				,@ToolTip = @toolTip
				,@IsEnabledForRegistrant = @isEnabledForRegistrant
				,@PracticeRegisterChangeRowGUID = @practiceRegisterChangeRowGUID
				,@PracticeRegisterSectionPracticeRegisterSID = @practiceRegisterSectionPracticeRegisterSID
				,@PracticeRegisterSectionLabel = @practiceRegisterSectionLabel
				,@PracticeRegisterSectionIsDefault = @practiceRegisterSectionIsDefault
				,@IsDisplayedOnLicense = @isDisplayedOnLicense
				,@PracticeRegisterSectionIsActive = @practiceRegisterSectionIsActive
				,@PracticeRegisterSectionRowGUID = @practiceRegisterSectionRowGUID
				,@IsActive = @isActive
				,@IsPending = @isPending
				,@IsDeleteEnabled = @isDeleteEnabled
				,@IsReselected = @isReselected
				,@IsNullApplied = @isNullApplied
				,@zContext = @zContext output
		
		end

		select
			 @practiceRegisterCatalogItemSID PracticeRegisterCatalogItemSID
			,@practiceRegisterSID PracticeRegisterSID
			,@catalogItemSID CatalogItemSID
			,@isAppliedOnApplication IsAppliedOnApplication
			,@isAppliedOnApplicationApproval IsAppliedOnApplicationApproval
			,@isAppliedOnRenewal IsAppliedOnRenewal
			,@isAppliedOnReinstatement IsAppliedOnReinstatement
			,@isAppliedOnRegChange IsAppliedOnRegChange
			,@isAppliedToPAPSubscribers IsAppliedToPAPSubscribers
			,@practiceRegisterSectionSID PracticeRegisterSectionSID
			,@practiceRegisterChangeSID PracticeRegisterChangeSID
			,@feeSequence FeeSequence
			,@effectiveTime EffectiveTime
			,@expiryTime ExpiryTime
			,@userDefinedColumns UserDefinedColumns
			,@practiceRegisterCatalogItemXID PracticeRegisterCatalogItemXID
			,@legacyKey LegacyKey
			,@isDeleted IsDeleted
			,@createUser CreateUser
			,@createTime CreateTime
			,@updateUser UpdateUser
			,@updateTime UpdateTime
			,@rowGUID RowGUID
			,@rowStamp RowStamp
			,@catalogItemLabel CatalogItemLabel
			,@invoiceItemDescription InvoiceItemDescription
			,@isLateFee IsLateFee
			,@imageAlternateText ImageAlternateText
			,@isAvailableOnClientPortal IsAvailableOnClientPortal
			,@isComplaintPenalty IsComplaintPenalty
			,@gLAccountSID GLAccountSID
			,@isTaxRate1Applied IsTaxRate1Applied
			,@isTaxRate2Applied IsTaxRate2Applied
			,@isTaxRate3Applied IsTaxRate3Applied
			,@isTaxDeductible IsTaxDeductible
			,@catalogItemEffectiveTime CatalogItemEffectiveTime
			,@catalogItemExpiryTime CatalogItemExpiryTime
			,@fileTypeSCD FileTypeSCD
			,@fileTypeSID FileTypeSID
			,@catalogItemRowGUID CatalogItemRowGUID
			,@practiceRegisterTypeSID PracticeRegisterTypeSID
			,@registrationScheduleSID RegistrationScheduleSID
			,@practiceRegisterName PracticeRegisterName
			,@practiceRegisterLabel PracticeRegisterLabel
			,@isActivePractice IsActivePractice
			,@isPublicRegistryEnabled IsPublicRegistryEnabled
			,@isRenewalEnabled IsRenewalEnabled
			,@isLearningPlanEnabled IsLearningPlanEnabled
			,@isNextCEFormAutoAdded IsNextCEFormAutoAdded
			,@isEligibleSupervisor IsEligibleSupervisor
			,@isSupervisionRequired IsSupervisionRequired
			,@isEmploymentTerminated IsEmploymentTerminated
			,@isGroupMembershipTerminated IsGroupMembershipTerminated
			,@termPermitDays TermPermitDays
			,@registerRank RegisterRank
			,@learningModelSID LearningModelSID
			,@reasonGroupSID ReasonGroupSID
			,@practiceRegisterIsDefault PracticeRegisterIsDefault
			,@isDefaultInactivePractice IsDefaultInactivePractice
			,@practiceRegisterIsActive PracticeRegisterIsActive
			,@practiceRegisterRowGUID PracticeRegisterRowGUID
			,@practiceRegisterChangePracticeRegisterSID PracticeRegisterChangePracticeRegisterSID
			,@practiceRegisterChangePracticeRegisterSectionSID PracticeRegisterChangePracticeRegisterSectionSID
			,@practiceRegisterChangeIsActive PracticeRegisterChangeIsActive
			,@toolTip ToolTip
			,@isEnabledForRegistrant IsEnabledForRegistrant
			,@practiceRegisterChangeRowGUID PracticeRegisterChangeRowGUID
			,@practiceRegisterSectionPracticeRegisterSID PracticeRegisterSectionPracticeRegisterSID
			,@practiceRegisterSectionLabel PracticeRegisterSectionLabel
			,@practiceRegisterSectionIsDefault PracticeRegisterSectionIsDefault
			,@isDisplayedOnLicense IsDisplayedOnLicense
			,@practiceRegisterSectionIsActive PracticeRegisterSectionIsActive
			,@practiceRegisterSectionRowGUID PracticeRegisterSectionRowGUID
			,@isActive IsActive
			,@isPending IsPending
			,@isDeleteEnabled IsDeleteEnabled
			,@isReselected IsReselected
			,@isNullApplied IsNullApplied
			,@zContext zContext

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
