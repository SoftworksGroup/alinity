SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[pStateProvince#Insert]
	 @StateProvinceSID        int               = null output								-- identity value assigned to the new record
	,@StateProvinceName       nvarchar(30)      = null											-- required! if not passed value must be set in custom logic prior to insert
	,@StateProvinceCode       nvarchar(5)       = null											-- required! if not passed value must be set in custom logic prior to insert
	,@CountrySID              int               = null											-- required! if not passed value must be set in custom logic prior to insert
	,@ISONumber               smallint          = null											
	,@IsDisplayed             bit               = null											-- default: (1)
	,@IsDefault               bit               = null											-- default: (0)
	,@IsActive                bit               = null											-- default: (1)
	,@IsAdminReviewRequired   bit               = null											-- default: CONVERT(bit,(0))
	,@ChangeLog               xml               = null											-- default: CONVERT(xml,'<Changes />')
	,@UserDefinedColumns      xml               = null											
	,@StateProvinceXID        varchar(150)      = null											
	,@LegacyKey               nvarchar(50)      = null											
	,@CreateUser              nvarchar(75)      = null											-- default: suser_sname()
	,@IsReselected            tinyint           = null											-- when 1 all columns in entity view are returned, 2 PK only, 0 none
	,@zContext                xml               = null											-- other values defining context for the insert (if any)
	,@CountryName             nvarchar(50)      = null											-- not a base table column (default ignored)
	,@ISOA2                   char(2)           = null											-- not a base table column (default ignored)
	,@ISOA3                   char(3)           = null											-- not a base table column (default ignored)
	,@CountryISONumber        smallint          = null											-- not a base table column (default ignored)
	,@IsStateProvinceRequired bit               = null											-- not a base table column (default ignored)
	,@CountryIsDefault        bit               = null											-- not a base table column (default ignored)
	,@CountryIsActive         bit               = null											-- not a base table column (default ignored)
	,@CountryRowGUID          uniqueidentifier  = null											-- not a base table column (default ignored)
	,@IsDeleteEnabled         bit               = null											-- not a base table column (default ignored)
	,@StateProvinceSearch     nvarchar(81)      = null											-- not a base table column (default ignored)
as
/*********************************************************************************************************************************
Procedure : dbo.pStateProvince#Insert
Notice    : Copyright © 2019 Softworks Group Inc.
Summary   : inserts 1 row into the dbo.StateProvince table
-----------------------------------------------------------------------------------------------------------------------------------
Author    : Generated by DB Studio: pSprocGen | Designer: Tim Edlund
Version   : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------
This procedure is used in maintenance of the dbo.StateProvince table with parameter values represent columns of the table. Additional
parameters are provided for all columns in the vStateProvince entity view, however, the base logic of the procedure inserts the
the columns of the table only.

Note: This procedure cannot be called directly from the MS Entity Framework! Use the wrapper procedure instead (...#EFInsert) that
excludes the primary key output parameter, but includes all other parameters.

Table-specific logic can be added through tagged sections (pre and post insert) and a call to an extended procedure supports
configuration (client) specific logic. Code implemented within code tags (table-specific logic) is part of the base product and
applies to all client configurations. Calls to the extended procedure occurs immediately after the table-specific logic in both
"pre-insert" and "post-insert" contexts.  A transaction is used to commit/rollback all changes as a logical unit.

Client specific customizations must be implemented in the ext.pStateProvince procedure. The extended procedure is only called
where it exists in the DB. The first parameter passed @Mode is set to either "insert.pre" or "insert.post" to provide context for
the extended logic.

The @zContext parameter is an additional construct available to support overrides where different results are produced based on
content provided in the XML from the client tier. This parameter may contain multiple values.

The "@IsReselected" parameter controls whether the entity row is returned as a dataset (SELECT). There are 3 settings:
   0 - no data set is returned
   1 - return the full entity
   2 - return only the SID (primary key) of the row inserted

For client-tier calls using the Microsoft Entity Framework and RIA Services, the @IsReselected bit should be passed as 1 to
force re-selection of table columns + extended view columns (the entity view).

Values for parameters representing mandatory columns must be provided unless a database default exists.  The default values
displayed as comments next to the parameter declarations above, and the list of columns returned from the entity view when
@IsReselected = 1, were obtained from the data dictionary at generation time. If the table or view design has been
updated since then, the procedure must be regenerated to keep comments up to date. In the StudioDB run dbo.pEFGen
to update all views and procedures which appear out-of-date.

The procedure does not accept a parameter for UpdateUser since the @CreateUser value is applied into both the user audit
columns.  Audit times are set automatically through database defaults and cannot be passed or overwritten.

If the @CreateUser parameter is passed as the special value "SystemUser", then the system user established in sf.ConfigParam
is applied. This option is useful for conversion and system generated inserts the user would not recognize as have caused. Any
other value provided for the parameter (including null) is overwritten with the current application user.

Business rule compliance is checked through a table constraint which calls fStateProvinceCheck to test all rules.

-------------------------------------------------------------------------------------------------------------------------------- */

begin
	set nocount on

	declare
		 @errorNo                                      int = 0								-- 0 no error, <50000 SQL error, else business rule
		,@tranCount                                    int = @@trancount			-- determines whether a wrapping transaction exists
		,@sprocName                                    nvarchar(128) = object_name(@@procid)						-- name of currently executing procedure
		,@xState                                       int										-- error state detected in catch block
		,@errorText                                    nvarchar(4000)					-- message text (for business rule errors)
		,@rowsAffected                                 int = 0								-- tracks rows impacted by the operation (error check)
		,@recordSID                                    int										-- tracks primary key value for clearing current default
		,@ON                                           bit = cast(1 as bit)		-- constant for bit comparison and assignments
		,@OFF                                          bit = cast(0 as bit)		-- constant for bit comparison and assignments

	set @StateProvinceSID = null																						-- initialize output parameter

	begin try

		-- use a transaction so that any additional updates implemented through the extended
		-- procedure or through table-specific logic succeed or fail as a logical unit

		if @tranCount = 0																											-- no outer transaction
		begin
			begin transaction
		end
		else																																	-- outer transaction so create save point
		begin
			save transaction @sprocName
		end

		-- remove leading and trailing spaces from character type columns

		set @StateProvinceName = ltrim(rtrim(@StateProvinceName))
		set @StateProvinceCode = ltrim(rtrim(@StateProvinceCode))
		set @StateProvinceXID = ltrim(rtrim(@StateProvinceXID))
		set @LegacyKey = ltrim(rtrim(@LegacyKey))
		set @CreateUser = ltrim(rtrim(@CreateUser))
		set @CountryName = ltrim(rtrim(@CountryName))
		set @ISOA2 = ltrim(rtrim(@ISOA2))
		set @ISOA3 = ltrim(rtrim(@ISOA3))
		set @StateProvinceSearch = ltrim(rtrim(@StateProvinceSearch))

		-- set zero length strings to null to avoid storing them in the record

		if len(@StateProvinceName) = 0 set @StateProvinceName = null
		if len(@StateProvinceCode) = 0 set @StateProvinceCode = null
		if len(@StateProvinceXID) = 0 set @StateProvinceXID = null
		if len(@LegacyKey) = 0 set @LegacyKey = null
		if len(@CreateUser) = 0 set @CreateUser = null
		if len(@CountryName) = 0 set @CountryName = null
		if len(@ISOA2) = 0 set @ISOA2 = null
		if len(@ISOA3) = 0 set @ISOA3 = null
		if len(@StateProvinceSearch) = 0 set @StateProvinceSearch = null

		-- set the ID of the user

		if isnull(@CreateUser, 'x') = N'SystemUser' set @CreateUser = left(sf.fConfigParam#Value('SystemUser'),75)-- override for "SystemUser"
		if isnull(@CreateUser, 'x') <> N'SystemUser' set @CreateUser = sf.fApplicationUserSession#UserName()			-- application user - or DB user if no application session set

		-- reset defaults on table parameters passed as NULL

		set @IsDisplayed = isnull(@IsDisplayed,(1))
		set @IsDefault = isnull(@IsDefault,(0))
		set @IsActive = isnull(@IsActive,(1))
		set @IsAdminReviewRequired = isnull(@IsAdminReviewRequired,CONVERT(bit,(0)))
		set @ChangeLog = isnull(@ChangeLog,CONVERT(xml,'<Changes />'))
		set @CreateUser = isnull(@CreateUser,suser_sname())
		set @IsReselected          = isnull(@IsReselected         ,(0))
		
		-- set default value on mandatory foreign keys where configured
		-- and if value not already assigned
		
		if @CountrySID  is null select @CountrySID  = x.CountrySID from dbo.Country x where x.IsDefault = @ON
		
		-- unset previous default if record is being inserted as the new default
		
		if @IsDefault = @ON
		begin
		
			select @recordSID = x.StateProvinceSID from dbo.StateProvince x where x.IsDefault = @ON and x.CountrySID = @CountrySID
			
			if @recordSID is not null
			begin
			
				update
					dbo.StateProvince
				set
					 IsDefault  = @OFF
					,UpdateUser = @CreateUser
					,UpdateTime = sysdatetimeoffset()
				where
					StateProvinceSID = @recordSID																		-- unique index ensures only 1 record needs to be unset
				
			end
		end

		-- apply the table-specific pre-insert logic (if any)

		--! <PreInsert>
		--  insert pre-insert logic here ...
		--! </PreInsert>
	
		-- call the extended version of the procedure (if it exists) for "insert.pre" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pStateProvince'
		)
		begin
		
			exec @errorNo = ext.pStateProvince
				 @Mode                    = 'insert.pre'
				,@StateProvinceName       = @StateProvinceName output
				,@StateProvinceCode       = @StateProvinceCode output
				,@CountrySID              = @CountrySID output
				,@ISONumber               = @ISONumber output
				,@IsDisplayed             = @IsDisplayed output
				,@IsDefault               = @IsDefault output
				,@IsActive                = @IsActive output
				,@IsAdminReviewRequired   = @IsAdminReviewRequired output
				,@ChangeLog               = @ChangeLog output
				,@UserDefinedColumns      = @UserDefinedColumns output
				,@StateProvinceXID        = @StateProvinceXID output
				,@LegacyKey               = @LegacyKey output
				,@CreateUser              = @CreateUser
				,@IsReselected            = @IsReselected
				,@zContext                = @zContext
				,@CountryName             = @CountryName
				,@ISOA2                   = @ISOA2
				,@ISOA3                   = @ISOA3
				,@CountryISONumber        = @CountryISONumber
				,@IsStateProvinceRequired = @IsStateProvinceRequired
				,@CountryIsDefault        = @CountryIsDefault
				,@CountryIsActive         = @CountryIsActive
				,@CountryRowGUID          = @CountryRowGUID
				,@IsDeleteEnabled         = @IsDeleteEnabled
				,@StateProvinceSearch     = @StateProvinceSearch
		
		end

		-- insert the record

		insert
			dbo.StateProvince
		(
			 StateProvinceName
			,StateProvinceCode
			,CountrySID
			,ISONumber
			,IsDisplayed
			,IsDefault
			,IsActive
			,IsAdminReviewRequired
			,ChangeLog
			,UserDefinedColumns
			,StateProvinceXID
			,LegacyKey
			,CreateUser
			,UpdateUser
		)
		select
			 @StateProvinceName
			,@StateProvinceCode
			,@CountrySID
			,@ISONumber
			,@IsDisplayed
			,@IsDefault
			,@IsActive
			,@IsAdminReviewRequired
			,@ChangeLog
			,@UserDefinedColumns
			,@StateProvinceXID
			,@LegacyKey
			,@CreateUser
			,@CreateUser

		select
			 @rowsAffected     = @@rowcount
			,@StateProvinceSID = scope_identity()																-- capture key value of new row

		-- check for errors

		if @rowsAffected <> 1																									-- ensure 1 row was inserted
		begin

			exec sf.pMessage#Get
				 @MessageSCD  = 'RowCountUnexpected'
				,@MessageText = @errorText output
				,@DefaultText = N'The "%1" operation on table "%2" affected an unexpected number of rows (%3). Record ID = %4.'
				,@Arg1        = 'insert'
				,@Arg2        = 'dbo.StateProvince'
				,@Arg3        = @rowsAffected
				,@Arg4        = @StateProvinceSID
			
			raiserror(@errorText, 18, 1)
			
		end

		-- apply the table-specific post-insert logic (if any)

		--! <PostInsert>
		--  insert post-insert logic here ...
		--! </PostInsert>
	
		-- call the extended version of the procedure (if it exists) for "insert.post" mode
		
		if exists
		(
			select
				1
			from
				sf.vRoutine r
			where
				r.SchemaName = 'ext'
			and
				r.RoutineName = 'pStateProvince'
		)
		begin
		
			exec @errorNo = ext.pStateProvince
				 @Mode                    = 'insert.post'
				,@StateProvinceSID        = @StateProvinceSID
				,@StateProvinceName       = @StateProvinceName
				,@StateProvinceCode       = @StateProvinceCode
				,@CountrySID              = @CountrySID
				,@ISONumber               = @ISONumber
				,@IsDisplayed             = @IsDisplayed
				,@IsDefault               = @IsDefault
				,@IsActive                = @IsActive
				,@IsAdminReviewRequired   = @IsAdminReviewRequired
				,@ChangeLog               = @ChangeLog
				,@UserDefinedColumns      = @UserDefinedColumns
				,@StateProvinceXID        = @StateProvinceXID
				,@LegacyKey               = @LegacyKey
				,@CreateUser              = @CreateUser
				,@IsReselected            = @IsReselected
				,@zContext                = @zContext
				,@CountryName             = @CountryName
				,@ISOA2                   = @ISOA2
				,@ISOA3                   = @ISOA3
				,@CountryISONumber        = @CountryISONumber
				,@IsStateProvinceRequired = @IsStateProvinceRequired
				,@CountryIsDefault        = @CountryIsDefault
				,@CountryIsActive         = @CountryIsActive
				,@CountryRowGUID          = @CountryRowGUID
				,@IsDeleteEnabled         = @IsDeleteEnabled
				,@StateProvinceSearch     = @StateProvinceSearch
		
		end

		if @trancount = 0 and xact_state() = 1 commit transaction

		-- return all columns for entity (1), just the PK value (2), or no returned value (0)

		if @IsReselected = 2
		begin

			select
				 ent.StateProvinceSID
			from
				dbo.vStateProvince ent
			where
				ent.StateProvinceSID = @StateProvinceSID

		end
		else if @IsReselected = 1
		begin

			select
				 ent.StateProvinceSID
				,ent.StateProvinceName
				,ent.StateProvinceCode
				,ent.CountrySID
				,ent.ISONumber
				,ent.IsDisplayed
				,ent.IsDefault
				,ent.IsActive
				,ent.IsAdminReviewRequired
				,ent.ChangeLog
				,ent.UserDefinedColumns
				,ent.StateProvinceXID
				,ent.LegacyKey
				,ent.IsDeleted
				,ent.CreateUser
				,ent.CreateTime
				,ent.UpdateUser
				,ent.UpdateTime
				,ent.RowGUID
				,ent.RowStamp
				,ent.CountryName
				,ent.ISOA2
				,ent.ISOA3
				,ent.CountryISONumber
				,ent.IsStateProvinceRequired
				,ent.CountryIsDefault
				,ent.CountryIsActive
				,ent.CountryRowGUID
				,ent.IsDeleteEnabled
				,ent.IsReselected
				,ent.IsNullApplied
				,ent.zContext
				,ent.StateProvinceSearch
			from
				dbo.vStateProvince ent
			where
				ent.StateProvinceSID = @StateProvinceSID

		end

	end try

	begin catch
		set @xState = xact_state()
		
		if @tranCount > 0 and @xState = 1
		begin
			rollback transaction @sprocName																			-- committable wrapping trx exists: rollback to savepoint
		end
		else if @xState <> 0																									-- full rollback
		begin
			rollback
		end
		
		exec @errorNo = sf.pErrorRethrow																			-- process message text and re-throw the error
	end catch

	return(@errorNo)

end
GO
