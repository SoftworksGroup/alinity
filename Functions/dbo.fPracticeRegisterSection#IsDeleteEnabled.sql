SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO
CREATE FUNCTION [dbo].[fPracticeRegisterSection#IsDeleteEnabled]
	(
	@PracticeRegisterSectionSID int
	)
returns bit
as
/*********************************************************************************************************************************
ScalarF : dbo.fPracticeRegisterSection#IsDeleteEnabled
Notice  : Copyright © 2019 Softworks Group Inc.
Summary : Returns 1 (bit) when deletion of the record is allowed
-----------------------------------------------------------------------------------------------------------------------------------
Author  : Generated by DB Studio: pIsDeleteEnabledFcnGen | Designer: Tim Edlund
Version : April 2019
-----------------------------------------------------------------------------------------------------------------------------------
Comments
--------

The function is called by the entity view to set a calculated bit column "IsDeleteEnabled". The column is typically bound to a
delete button on the UI.  The results of the function determine whether or not deletion should be allowed.  The function tests
one or more of the following factors: 1) whether the table contains a system code column in which case deletion is
not allowed, 2) the security granted to the logged in user, and, 3) the existence of child records.

When a child table is related through a foreign key with cascade delete, and all descendants of that table are also related with
cascade delete constraints, then the child table is not considered blocking and will not appear in the SELECT statement.

Table-specific logic can be added through tagged sections (pre and post check). An extended version of the function is not
supported.  Code implemented within code tags is part of the base product and applies to all client configurations.

Example
-------

select top (10)
   x.PracticeRegisterSectionSID
  ,dbo.fPracticeRegisterSection#IsDeleteEnabled(x.PracticeRegisterSectionSID) IsDeleteEnabled
from
  dbo.PracticeRegisterSection x
order by
  newid()

-------------------------------------------------------------------------------------------------------------------------------- */

begin

	declare
		 @isDeleteEnabled                 bit																	-- return value
		,@ON                              bit = cast(1 as bit)								-- constant to eliminate repetitive casting syntax
		,@OFF                             bit = cast(0 as bit)								-- constant to eliminate repetitive casting syntax
	
	set @isDeleteEnabled = @ON																							-- set return value to ON then check for blocking conditions
	
	--! <PreCheck>
	--  insert pre-check logic here ...
	--! </PreCheck>
	
	if (select x.IsDefault from dbo.PracticeRegisterSection x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) = @ON set @isDeleteEnabled = @OFF	-- default records cannot be deleted
	
	if @isDeleteEnabled = @ON																								-- check for existence of child records
	begin
	
		if
		(
			select
				  count(x01.PracticeRegisterCatalogItemSID)
				+ count(x02.PracticeRegisterChangeSID)
				+ count(x03.PracticeRegisterRestrictionSID)
				+ count(x04.RegistrantAppSID)
				+ count(x05.RegistrantRenewalSID)
				+ count(x06.RegistrationSID)
				+ count(x07.RegistrationChangeSID)
				+ count(x08.ReinstatementSID)
			from
				dbo.PracticeRegisterSection     x00
			left outer join
				  (select top (1) x.PracticeRegisterCatalogItemSID, x.PracticeRegisterSectionSID from dbo.PracticeRegisterCatalogItem x where isnull(x.PracticeRegisterSectionSID, -1) = @PracticeRegisterSectionSID) x01 on x00.PracticeRegisterSectionSID =  x01.PracticeRegisterSectionSID
			left outer join
				  (select top (1) x.PracticeRegisterChangeSID, x.PracticeRegisterSectionSID from dbo.PracticeRegisterChange x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) x02 on x00.PracticeRegisterSectionSID =  x02.PracticeRegisterSectionSID
			left outer join
				  (select top (1) x.PracticeRegisterRestrictionSID, x.PracticeRegisterSectionSID from dbo.PracticeRegisterRestriction x where isnull(x.PracticeRegisterSectionSID, -1) = @PracticeRegisterSectionSID) x03 on x00.PracticeRegisterSectionSID =  x03.PracticeRegisterSectionSID
			left outer join
				  (select top (1) x.RegistrantAppSID, x.PracticeRegisterSectionSID from dbo.RegistrantApp x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) x04 on x00.PracticeRegisterSectionSID =  x04.PracticeRegisterSectionSID
			left outer join
				  (select top (1) x.RegistrantRenewalSID, x.PracticeRegisterSectionSID from dbo.RegistrantRenewal x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) x05 on x00.PracticeRegisterSectionSID =  x05.PracticeRegisterSectionSID
			left outer join
				  (select top (1) x.RegistrationSID, x.PracticeRegisterSectionSID from dbo.Registration x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) x06 on x00.PracticeRegisterSectionSID =  x06.PracticeRegisterSectionSID
			left outer join
				  (select top (1) x.RegistrationChangeSID, x.PracticeRegisterSectionSID from dbo.RegistrationChange x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) x07 on x00.PracticeRegisterSectionSID =  x07.PracticeRegisterSectionSID
			left outer join
				  (select top (1) x.ReinstatementSID, x.PracticeRegisterSectionSID from dbo.Reinstatement x where x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID) x08 on x00.PracticeRegisterSectionSID =  x08.PracticeRegisterSectionSID
			where
				x00.PracticeRegisterSectionSID = @PracticeRegisterSectionSID
		) > 0 set @isDeleteEnabled = @OFF
		
	end
	
	--! <PostCheck>
	if @isDeleteEnabled = @ON																								-- block unless current user inserted the row or is an administrator
	begin
	
		if
		(
			select
				x.CreateUser
			from
				dbo.PracticeRegisterSection x
			where
				x.PracticeRegisterSectionSID = @PracticeRegisterSectionSID
			) <> sf.fApplicationUserSession#UserName() set @isDeleteEnabled = sf.fIsGranted('ADMIN.BASE')
	
	end
	--! </PostCheck>
	
	return(@isDeleteEnabled)
end
GO
